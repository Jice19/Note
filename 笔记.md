reactive实现响应式  是针对对象的，

```
//定义
const count = reactive({value:0})
//使用
{{count.value}}
```

ref实现响应式，是针对**基础数据类型**的,因为响应式对象一定要是对象形式，实际是将基础数据声明成const count = ref({value:0})，编译时框架中的模板（template）中.value自动拆包，所以无需在template中使用.value的形 式，**但是在js代码里面需要使用.value的形式**

```
//定义
const c =ref(0)
//使用
{{c}}
```

计算属性computed用来处理一些响应式丢失的问题（解构reactive中属性会脱离proxy代理，可以通过computed来解决解构属性丢失的问题，因为computed是基于响应式对象的派生新值,也可以通过toRefs转换后再解构某个属性，toRefs将对象每个属性都加了代理）

监听数据状态API(watch) ： 

只有当值变化的时候才执行函数，异步执行

```
watch([price,count],[newPrice,newCount],[oldPrice,oldCount]=>{
    console.log(newPrice,newCount,oldPrice,oldCount)
})
```

WatchEffect:

组件首次渲染将会执行一次，同步检测值的变化，监听的数据变化时才进入Effect，异步执行

```
WatchEffect(() => {
    console.log('watchEffect',price.value,count.value)
})
```

WatchSyncEffect：

同步执行检测值得变化，会阻塞进程

```

```

WatchPostEffect

```

```

只有首次 执行顺序是：WatchEffect =>  WatchSyncEffect =>  WatchPostEffect

​         往后执行顺序是：WatchSyncEffect =>  WatchEffect =>  WatchPostEffect

列表和条件渲染：

v-for 和 v-if   ：vue2   v-for>v-if   vue3  v-if >v-for 

一起使用会带来什么问题：以前vue2的时候没有忧虑，现在vue3中有了这个限制，给我们带来了心智负担，不要一起使用

想在同一个对象当中声明多个name数据可以使用**symbol**标识每个name

symbol使用场景：声明对象属性名称----唯一且不可变

``` 
//使用symbol作为属性名，不能使用.来访问属性，否则将会当作字符串来处理
const id = symbol('id')
const user = {
	[id]:123,
	name:"jice"
}

cosole.log(user[id])  //不行使用User.id来访问属性，否则将会当作字符串来处理
```



属性隐藏    定义私有化属性，外部无法访问

```
const nameSymbol = Symbol("name")
const obj = {
    [nameSymbol]:"张三", //无法使用Object.getOwnPropertyNames(person)获取到，                 					   //getOwnPropertySymbles（person）获取
    regularName :"李四"
}
```

全局注册使用：同一窗口之内可以访问到全局注册表中的数据，不同窗口无法实现共享

```
const sym =symbol.for("data")   //注册了全局symbol
使用symbol.keyFor('sym')获取data
```



Set：散列，不重复数据,可用来数组去重，存储任何类型

map：存贮键值对，外部无法找到内部key，并且无法修改覆盖

weakSet:  主要存储对象 数据，当对象不使用的时候会被回收

weakMap:  主要存储对象数据 ，当对象不使用的时候会被回收



生命周期： 

Props宏函数，编译过程中可以理解为全局API，不需要导入代码

provide传递给子组件的是键值对形式，子组件可以通过inject接收传值名获得传值

第一层路由需要斜线，子路由在children中不需要斜线，匹配子路由的内容需要Routerview接收渲染

动态路由：   :id   模板中获取动态路由中的参数:  {{  $route.params.id  }}(动态匹配)

js中获取动态路由中的参数：引入useRoute和toRaw

```
const route = useRoute()
console.log(toRow(route))
const id = route.params.id
```

跳转问题：以前的多页面网站，是通过`window.location.herf`跳转切换

SPA中路由跳转API：`<router-link  :to={具名路由name, params:{id:i}} />`

如遇响应式丢失；解决办法：computed(() => route.params.id) 或者模板中使用{{route.params.id}}或者使用toRefs(),始终关注只有对象具有响应式

路由守卫：鉴权、动画过渡、请求。  钩子  (to,from,next)

```
router.beforeEach((to,from,next) => {
//可以进行身份验证操作
    next()   //放行
})
```

beforeEach钩子按照洋葱模型执行，先全局路由钩子，再独享路由钩子(beforeEnter)



## vue3源码：

以前是通过dom操作实现视图更新，但是mvvm出现之后，可以通过proxy实现响应式数据更新，开发者是没有感觉的，具体实现是由vue实现的，需要理解**proxy**如何实现对象的更新，vue是借鉴mvvm模式(model view controller -> model  view viewModel)，mvvm是从mvc中衍生出来的架构设计模式，由于前端界面的特殊需求：界面需要与数据同步，双向数据绑定，如果使用controller可能会导致臃肿、延时

vue3核心零部件：

vue3问题：

### 【编译时处理】vue文件编译成函数或对象，样式等

- compiler-core

  整体流程：

  1、vite/webpack启动（打包）vue项目，vite-plugin-vue/webpack vue-loader

  2、分词器（tokenizer）来解析模板文件中对应的分词，将标签属性、文本内容等等转换成json  Tokens，方便后续转换成Ast树

  ```
  [
    { type: 'tagOpen', content: 'div' },
    { type: 'attribute', name: 'class', value: 'container' },
    { type: 'tagClose' },
    { type: 'text', content: '\n  ' },
    { type: 'tagOpen', content: 'h1' },
    { type: 'tagClose' },
    { type: 'interpolation', content: 'message' },
    { type: 'tagOpenEnd', content: 'h1' },
    { type: 'text', content: '\n  ' },
    { type: 'tagOpen', content: 'button' },
    { type: 'directive', name: 'click', value: 'handleClick' },
    { type: 'tagClose' },
    { type: 'text', content: 'Click me' },
    { type: 'tagOpenEnd', content: 'button' },
    { type: 'text', content: '\n' },
    { type: 'tagOpenEnd', content: 'div' }
  ]
  ```

  

  3、通过parser解析器转换成vue3的ast:遇到开始标签就压入栈中，遇到结束标签就弹出栈回到父节点

  ```
  {
    "type": 1, // Element 类型
    "tag": "div",
    "props": [{ name: "id", value: "app" }],
    "children": [
      {
        "type": 5, // TextCall 类型
        "content": {
          "type": 4, // Interpolation 类型
          "content": {
            "type": 2, // Expression 类型
            "content": "message"
          }
        }
      }
    ]
  }
  ```

  

  Tokenizer 和 Parser 的协作流程：

  1. **Tokenizer** 将模板字符串分解为 Tokens（如标签、属性、文本）。
  2. **Parser** 按顺序读取 Tokens，递归构建 AST 节点。
  3. **每个 Token** 被转换为 AST 中的一个或多个节点，或影响节点的属性。

  

  AST 生成后，Vue 3 编译流程继续：

  1. **转换（Transform）**：对 AST 进行优化（如静态节点提升、事件处理函数缓存。
  2. **生成（Codegen）**：根据优化后的 AST 生成渲染函数代码（如 `h('div', { id: 'app' }, [text('hello')])`）

  4、transfomer将ast转换成新的ast（渲染函数、vNode），进行优化（静态节点提升、事件处理函数缓存）

  5、codegen将新ast生成js渲染函数

  ```
  h('div', { id: 'app' }, [text('hello')])
  ```

  

- compiler-dom

  1、浏览器侧差异补充，（react-dom、react-native）

  2、更多的是对一些指令的处理

  3、vHtml,对html的操作只有再客户端由，服务端渲染是没有的

  4、vOn:只在客户端有事件绑定逻辑

  

- compiler-sfc

   1、专门针对单文件组件的处理，需要将单文件组件中的template\script\style解析并且生成对应的函数或对象，或对应的处理逻辑

  2、template：使用compiler-core包中那一套实现编码转换

  3、script：可能需要babel转义

  4、style：提取样式，生成最终css

  

### 【响应式系统】将状态数据与视图更新链接起来

- reactivity：就是通过proxy来做对象 数据的劫持，通过Reflect做对象数据的处理

  1、dep:依赖，发布订阅:记录下触发订阅的值，当这个值修改之后就触发trigger

  ​           ---------track    trigger   notify

  2、 collection，依赖收集

  ​	当有些对象数据状态属性被访问时：就会触发proxy get，track【追踪】dep上的方法

  ​	

  ​	当有些对象数据状态属性被修改时：就会触发proxy set,trigger【数据变化通知】dep上的方法

  3、ref、reactive：

  ​             -reactive：最核心的代码是`new proxy`  292行

  ​	     -ref：用的get和set，使用自己的逻辑，没有用到reactive的使用proxy的方法   108行

  watch： 
  
  effect：onTrack和onTrigger,
  
  4、v-model如何实现动态绑定数据的
  
  :value （v-on的props实现事件监听）和@input监听事件的改变动态改变绑定的值

### 【运行时渲染器】数据更新时需要重新渲染到页面中

- runtime-core

- 为什么vue当中要加key标识dom？能不能用数组下标代替？

  renderer.ts:  patch

  diff算法：

  1、简单diff：旧的子节点有自己的key标识，然后新的子节点依次对比key标识，如果相同则匹配复用，如果不同则移动继续匹配，如果匹配完毕未找到则更新（新增diff过程）

  key用来标识某个节点在虚拟dom中的节点，这样标签位发生变化时可以直接复用

  2、双端diff（vue2）：四个指针，想办法让时间复杂度更低

  头头尾尾 -> 尾头头尾

  新子节点多出来的就是多的新增，旧子节点多的就是多的需删除

  3、**快速diff（最终版）最长递增子序列：**

  先头头尾尾，去头掐尾。剩下的根据索引值构建成source数组，定义最长递增子数组

  patch方法：

  证明新老节点一样：新老节点的type和key都相等

  ```
  function lengthOfLIS(nums){
  		const length = nums.length
  		let dp = new Array(length).fill(0),maxLen = 0
  		for(let i=0;i<length;i++){
  				for(let j=0;j<i;j++){
  						if(nums[j] < nums[i]){
  								dp[i] = Math.max(dp[i],dp[j]+1)
  						}
  				}
  				maxLen = Math.max(maxLen,dp[i])
  		}
  		return maxLen
  }
  ```

  

  

- runtime-dom



shared包：用来存储子包中公用的一些逻辑

【入口包】vue整个api的定义，提供给开发者使用的内容    vue



模板编译-》响应式-》diff



js里面不是对象就是函数   绝对！！！！

**vue3**当中采用proxy实现对象数据拦截属性的读取和写入操作，支持新增或删除属性时的响应式更新。

并且相对于vue2的Object.defineProperty，Proxy的性能和灵活性更高

**Object.defineProperty**实现响应式系统主要依赖data和computed，通过data定义响应式数据，vue递归遍历data对象的属性，通过defineProperty将每个属性转换成getter和settter，实现响应式。通过getter和setter实现依赖收集和追踪。



为什么vue更新使用vapor而不是虚拟dom节点：有些场景使用虚拟dom操作不好：比如千万行数据表格渲染，如果通过虚拟dom对应则有这么多个节点，如果需要遍历的话，需要做操作并且重新渲染，会造成性能消耗；原生开发

极致动画体验的要求：60FPS    原生开发,框架的虚拟DOM比较麻烦



Template =>AST=>Render函数=>vNode=>虚拟节点=>渲染



虚拟dom



项目：

介绍时一定要说需求：

无代码平台和saas平台区别

无代码平台：客户自定义页面、页表等等



DSL（JSON树）设计：用来约束平台页面内容的框架结构

通过输入的描述帮我们生成DSL，然后更具dsl进行渲染



代码层：物料就是一个一个组件，bolck.ts中每个类型对应一个物料，每个物料需要有自己的渲染器BlockRenderer,每个渲染器通过指定组件实现渲染，通过插件化形式进行的编写



路由层面的component来自：views



defineComponent



## 资源优化：

1、nrm ls  查看源路径   nrm use 切换源路径

2、使用terser压缩js代码：minify

读取js文件代码

```
import {join} from "node:path"
import {fs} from "node:fs"

const codeFilePath = join(import.meta.dirname,"../src/index.js")

const code = fs.readFileSync(codeFilePath,"utf-8")
```

压缩js文件代码

```
async function minifyCode(code){
    const minifiedCode = minify(code,{
        message:true
    })
    
   return minifiedCode  
}
```

输出js文件代码

### async和defer

`async` 和 `defer` 都异步加载脚本，不阻塞 HTML 解析；

`async` 加载完立即执行，顺序随机，可能阻塞 DOMContentLoaded；

`defer` 解析完成后执行，顺序固定，必在 DOMContentLoaded 前执行；

实战中，有依赖的脚本用 `defer`，无依赖的独立脚本用 `async`

压缩css文件：cssnano

压缩图片：转换成webp格式                             

文本文件压缩：gzip、brotli

采用相对路径在前端影响不大，但是在操作系统中如果进行文件切换，可能会找不到这个文件(如 Windows 用 `\`，Linux/macOS 用 `/`)，因此可以采用path模块中的join来实现文件路径的查找，join实现了拼接路径

## 缓存

资源缓存：

- [x] 为啥要把js、html、css文件传给后端处理呢？这不是服务端渲染吗？我们前端给后端传的不是一些表单数据之类的吗？缓存应该是缓存一些数据之类的哇？真搞不懂  


### 强缓存：

如果本地有缓存数据，就不会向服务端交互，拿的是本地缓存数据

**强缓存校验的判断依据是[文件名+缓存规则]，不是文件内容！！！**

  -响应头：Expire

  -响应头：Cache-Control ： public/private

js文件在有效期内发生变化，解决方案：

### 协商缓存：

每次都要进行交互，对比服务端最开始发送给客户端的Etag标识是否正确，正确则返回304拿本地缓存，错误则返回新数据

  -响应头：Last-Modified,请求头：If-Modefied-Since

初始阶段第一次请求头没有If-Modefied-Since，只有请求头带过去的数据有If-Modefied-Since,

 -响应头：ETag,    请求头：If-None-Match

后端给前端响应头里面携带ETag,然后前端返回里面if-none-match携带Etag的内容，如果有变化，则后端知道前端发生变化，返回新的数据

### service worker(HTTPS、离线缓存)

  -`Service  worker` : Server Work 通常指 **Service Worker**（服务工作线程），它是**浏览器**中运行在后台的**独立线程**，不依赖于网页主线程，主要用于实现离线缓存、推送通知等功能。在缓存策略中，Service Worker 是实现前端应用离线访问和优化网络请求的核心技术

通俗理解：没网的时候缓存之前的数据，并且记住你在没网时的操作（缓存），联网之后自动同步数据给服务端之后清理缓存，当用户不在网页的时候也可以通知状态。

````
/*
service  work如何开启使用
首先需要判断浏览器是否支持server work
*/

// app.js

// 检查浏览器是否支持 Service Worker
if ('serviceWorker' in navigator) {
  // 当页面加载完成后注册 Service Worker
  window.addEventListener('load', () => {
    navigator.serviceWorker.register('/sw.js')
      .then(registration => {
        // 注册成功
        console.log('ServiceWorker 注册成功，作用域:', registration.scope);
      })
      .catch(error => {
        // 注册失败
        console.log('ServiceWorker 注册失败:', error);
      });
  });
} else {
  console.log('浏览器不支持 Service Worker');
}
````

`Service  worker`除了缓存支援方面的优势之外，由于它适合浏览器主进程隔离开来的，还可以用来实现以下操作：

> - **后台同步 (Background Sync)**：在用户网络恢复后，自动同步之前失败的请求。
> - **推送通知 (Push Notifications)**：即使应用关闭，也能向用户发送通知

一种情况是离线应用处理

第二种是静态资源注册的缓存策略

### Navigator对象

**`navigator` 是浏览器提供的全局只读对象**，挂载在 `window` 上，用于**获取当前浏览器的运行环境信息**（包括浏览器类型、版本、系统平台、设备能力等），是前端实现「环境检测、兼容性适配」的核心 API 之一。



### bfcache

`bfcache` 是浏览器的 “前进 / 后退专属缓存”，目的是让页面导航像切换标签一样快，开发者只需避免踩坑就能享受这个优化。

**失效原因**：

1、存在未关闭的长轮询或者websocket连接

2、http设置了Cache: no-store

3、开启了隐私模式，有些浏览器在隐私模式下不支持bfcache

**工作原理 ：**

**浏览器在用户离开页面时，把页面的 “完整快照” 存到内存；用户返回时，直接恢复快照，跳过所有请求和渲染步骤，实现瞬间导航。**

**这个快照是会话级而不是存储在磁盘里面的，关闭页面这个缓存就会清空**



返回界面数据没有同步如何处理？

页面从 bfcache 恢复时，**不会触发 `load`、`DOMContentLoaded`，也不会触发框架的 `mounted` / `useEffect` 初始化钩子**，但一定会触发 `pageshow` 事件，且事件对象的 `persisted` 属性为 `true`。监听这个事件同时同步数据按需更新





### **Indexed** **DB**：前端需要存储大量数据

**特性**：键值对存储、异步操作、持久化存储、同源策略影响、大容量存储（几百兆到几兆）

海云项目使用到了这个浏览器数据库进行存储音频数据

比如离线应用像编辑器：用户离线编辑数据本地存储在Indexed DB 当中，连上网后同步到服务端



## 不同域之间共享数据

a.baidu.com和b.baidu.com之间不能共享localstorage中的数据，同源策略，二者主域名相同baidu.com,但是子域名不同，属于不同源

如果想要实现共享数据，可以使用以下几种通信方式

| 方法                      | 核心逻辑                                                     | 适用场景                     |
| ------------------------- | ------------------------------------------------------------ | ---------------------------- |
| 1. `document.domain`      | 两页面均设置 `document.domain = "主域名"`（如`.baidu.com`），统一域上下文 | 同主域、同协议端口，简单场景 |
| 2. `iframe + postMessage` | 主页面嵌入目标子域名 iframe，通过`postMessage`安全传递数据，目标页读写 localStorage | 通用跨子域，安全性高（推荐） |
| 3. Cookie 中转            | 设置 Cookie 的`domain=.主域名`，通过 Cookie 传递少量数据（≤4KB），`document.cookie = "key=val; domain=.baidu.com; path=/"` | 少量数据、兼容老浏览器       |



| 方法                   | 是否要求主域名相同 | 跨域范围                                    |
| ---------------------- | ------------------ | ------------------------------------------- |
| `document.domain`      | 是                 | “权限解锁方案”—— 解锁子域名间的直接访问权限 |
| Cookie 中转            | 是（共享场景）     | a.baidu.com可以拿到b.baidu.com的cookie信息  |
| `iframe + postMessage` | 否                 | 所有跨域场景（含不同主域）                  |



### iframe通信关键点 

1. **嵌入限制**：目标页需解除`X-Frame-Options`限制（服务器配置`ALLOW-FROM 主域名`/`SAMEORIGIN`），否则无法嵌入；
2. **协议端口**：主页面与 iframe 目标页必须协议（均 HTTP / 均 HTTPS）、端口一致，避免混合内容拦截；
3. 通信安全：
   - 发送`postMessage`时精准指定目标域名，禁止用`*`；
   - 接收消息必须验证`e.origin`，拒绝非可信来源；
4. **加载时机**：需监听 iframe 的`load`事件，确保目标页加载完成后再通过`postMessage`通信；
5. **访问限制**：主页面无法直接访问 iframe 的`localStorage`，必须通过`postMessage`中转，不可直接跨域操作；
6. **样式配置**：数据共享场景建议隐藏 iframe（`display: none; width:0; height:0`），不占用页面空间。

```
<!-- a.baidu.com 主页面 -->
<!DOCTYPE html>
<html>
<body>
  <!-- 嵌入目标子域名 b.baidu.com，可隐藏（数据通信无需展示） -->
  <iframe 
    id="targetIframe" 
    src="https://b.baidu.com" 
    style="display: none; width: 0; height: 0;"
    frameborder="0"
  ></iframe>

  <script>
    // 1. 获取iframe元素
    const iframe = document.getElementById('targetIframe');

    // 2. 监听iframe加载完成（确保目标页面就绪后再发消息）
    iframe.addEventListener('load', () => {
      // 3. 发送数据：参数1=要传递的数据，参数2=目标子域名（精准匹配，禁止用*）
      const sharedData = { type: 'user', data: { id: 123, name: '张三' } };
      iframe.contentWindow.postMessage(sharedData, 'https://b.baidu.com');
    });

    // 4. （可选）监听子域名页面的响应消息
    window.addEventListener('message', (e) => {
      // 验证来源，仅信任 b.baidu.com
      if (e.origin !== 'https://b.baidu.com') return;
      console.log('收到 b.baidu.com 响应：', e.data);
    });
  </script>
</body>
</html>
```



```
<!-- b.baidu.com 目标页面 -->
<!DOCTYPE html>
<html>
<body>
  <script>
    // 监听主页面（a.baidu.com）发送的消息
    window.addEventListener('message', (e) => {
      // 关键：验证消息来源，拒绝非可信域名
      if (e.origin !== 'https://a.baidu.com') return;

      // 1. 接收并处理数据
      const receivedData = e.data;
      console.log('收到 a.baidu.com 数据：', receivedData);
      // 可将数据写入 localStorage 实现本地存储
      localStorage.setItem('sharedData', JSON.stringify(receivedData));

      // 2. （可选）向主页面反馈接收结果
      e.source.postMessage('数据已接收并存储', e.origin);
    });
  </script>
</body>
</html>
```



## Vite

### 核心设计：

1、模块化规范的演变：commonJS->ESM、amd、cmd

2、bundleless，取决于浏览器原生ESM支持

3、本地开发  esbuild提升性能，线上构建rollup ->rolldown统一



插件化体系能够简化开发，并且vite也做了很多优化编译的事情（js文件等文件不做处理实现高效打包）

### 区分配置生产/开发

1️⃣   defineconfig 的 command  === “server”

```
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig(({ command, mode }) => {
  // --- 共享配置 ---
  // 这部分配置对 dev 和 build 都生效
  const sharedConfig = {
    plugins: [react()],
    resolve: {
      alias: {
        '@': '/src',
      },
    },
  };

  // --- 条件配置 ---
  if (command === 'serve') {
    // DEV 环境专属配置
    return {
      ...sharedConfig,
      server: {
        port: 3000,
        proxy: {
          '/api': 'http://localhost:8080',
        },
      },
    };
  } else {
    // BUILD 环境专属配置
    return {
      ...sharedConfig,
      build: {
        sourcemap: true, // 仅在 build 时生成 sourcemap
        minify: 'terser',
        rollupOptions: {
          // ...精细化打包配置
        },
      },
    };
  }
});
```

2️⃣   一、目录命名区分

- vite.config.base.ts

- vite.config.dev.ts

- Vite.config.prod.ts

​	二、package.json指定 --config参数

```
{
  "scripts": {
    "dev": "vite --config vite.config.dev.ts",
    "build": "vite build --config vite.config.prod.ts"
  }
}
```



### 插件执行

每个插件存在不同生命周期，分成前置与后置阶段

```
//vite.config.js
// 测试插件 A（模拟 vue 插件）
const pluginA = {
  name: 'plugin-A',
  // 前置阶段钩子（正序执行）
  buildStart() {
    console.log('✅ [前置阶段] plugin-A: buildStart 执行')
  },
  resolveId(id) {
    console.log('✅ [前置阶段] plugin-A: resolveId 执行 →', id)
    return null // 不修改路径，仅打印
  },
  load(id) {
    console.log('✅ [前置阶段] plugin-A: load 执行 →', id)
    return null // 不加载代码，仅打印
  },
  // 后置阶段钩子（倒序执行）
  transform(code) {
    console.log('🔄 [后置阶段] plugin-A: transform 执行')
    return code // 不修改代码，仅打印
  },
  generateBundle(options, bundle) {
    console.log('🔄 [后置阶段] plugin-A: generateBundle 执行')
  },
  writeBundle() {
    console.log('🔄 [后置阶段] plugin-A: writeBundle 执行')
  }
}

export default definConfig({
	plugins:[
		pluginA,
		pluginB,
		pluginC
	]
})
```



**前置阶段（buildStart/resolveId/load）**：严格按照 `plugins` 数组顺序（A→B→C）执行，核心是 “读取 / 解析代码”；

**后置阶段（transform/generateBundle/writeBundle）**：严格按照数组倒序（C→B→A）执行，核心是 “修改 / 生成产物”；

#### 总结

1. 同一个插件会同时包含**前置 + 后置**钩子，不是分开的两个插件；
2. 前置钩子正序执行（数组从头到尾），后置钩子倒序执行（数组从尾到头）；
3. 配置 Sentry 时，要把它放在压缩插件**前面**（数组中），才能让压缩先执行、Sentry 后处理。



为什么开发环境使用esbuild，生产环境使用rollup

因为esbuild是基于go语言写的，并行和优化算法的加持，可以让开发时编译构建打包速度更快

生产环境使用rollup，虽然慢，但是它的代码质量和优化程度有独特的优势

​    -tree-shaking   卓越的tree-shaking，可以生成更小的、更高效的生产包 

​    -插件生态：插件机制灵活

   -splitChunks：代码拆分  --一种优化策略，rollup在这方面更加成熟

### rolldown更新

核心目标：解决 Vite 老版本 “**开发 / 生产构建不一致**” 的痛点，同时提升打包性能；

核心优势：

- 开发 / 生产用同一内核，消除环境差异导致的 bug；
- Rust 编写，比 Rollup 快 5-10 倍，还兼容 Rollup 生态；
- 降低 Vite 团队的维护成本，让框架更稳定；

对你的影响：用 Vite 开发项目时，更少遇到 “开发正常、生产报错” 的问题，打包速度更快，学习 / 迁移成本几乎为 0。



### 基本使用

1、脚手架初始化规范

2、工程化、规范、自动化

3、配置vite体系，哪些需要打包，哪些需要编译



插件体系：拓展vite能力的功能集合

resolve：alias：{'@':'/src'}   设置绝对路径

①并且在tsconfig.json里面配置

```
"baseUrl":"."
"paths":{
    "@/*":["src/*"]
}
```

②：安装插件：”vite-tsconfig-paths“:"5.1.4"

 			     并且在plugins中使用插件

 sourcemap可以监测打包构建产物的错误定位（pnpm preview）

测试环境可以打开，但是开发环境是通过`sentry`管理监测

minify：压缩丑化代码但不影响功能，具体配置

```
minify:"terser",
terserOptions:{
    compress:{
        //配置压缩细节
        drop_console:true //删除console
    }
}
```



build里面静态拆包：

```
rollupOptions:{
    output:{
        munualChunks(id){
            if(id.includes("src")){
                return "home"
            }
        }
    }
}
```

动态拆包：模块懒加载

开头导入是静态导入，动态导入是按需导入，将 导入放到代码中，代码执行的话才导入



define用于定义全局变量

```
define:{ 
	"process.env.NODE_env":JSON.string(process.env.NODE_ENV)
}
```

.env实现声明全局变量

在html中使用：%VITE_ENV_VERSION%

vite环境变量设置好之后，需要在vite-env.d.ts中定义类型，不然使用的时候无法监测到全局变量

```
import.meta.env.属性
```



插件需要导入使用

vue-plugin-inspect插件：构建过程的插件的执行过程内容，构建前后文件的区别

做一个文件类型转换：

1、vite的插件本质是什么：vite为了将生态扩充交给开发者，因此采用了插件化设计的思想，定义基础插件化协议，开发者通过协议来开发插件，依次丰富生态

2、vite插件的本质是函数，有返回值

```
export default function viteJicePlugin(){
    return{
        name:"插件名称',
        //插件操作方法
        .....
        transform(src,id){
            //做内容转换
        }
    }
}
```



正则？怎么来的。

尝试:smile:写一个插件

编译时插件：transform

配置时插件：config

插件操作html使用html钩子：transformIndexHtml可以实现修改



tsconfig.app.json主要用于前端应用的配置

tsconfig.node.json主要用于前端脚本的配置



构建工具链在server的node层，build.ts

optimizer.ts对构建优化进行的处理

插件机制：node/plugins/index.ts插件机制的实现

服务端无法直接处理前端ts代码，需要使用esbuild.transformSync进行转换(开发环境)



接触一个新的知识面：我们需要明确学习目标，把官网内容理解并敲出来，然后再进行深入



## webpack

### 请说说你用过的loader和plugins

错误回答❌：我用过File Loader \ css Loader \ babel Loader ....

正确回答✔：webpack中Loader主要是用来编译转换的，我过往的构建工程化过程中使用的编译和转化相关的loader，比如：    File Loader \ css Loader \ babel Loader 

（然后介绍webpack中plugin的作用）：规范内容输出



需要特别去留意版本号（version Lens插件）

构建：本地构建              产物构建

### 打包：

（entry）打包构建时都需要入口文件，为了从某个文件开始收集依赖完成构建

​     -单入口

​     -多入口

​      -对象形式指定具名入口

 （output）输出：编译后产物的输出配置



### 模块化规范：

esm："import x from..."         commonJs   require()



线上（生产环境）：会进行Treeshaking      将没有用到的代码删除，es模块的导入导出在编译时就确定了，在webpack只要配置好环境，默认帮我做tree  shaking。

```
mode:"production"
```

出口文件配置

```
output:{
   // filename:"[name].[hash].js"    输出的文件名与js文件名一样
   environment:{
       arrowfuncion : false         //设置打包后不输出箭头函数
   }
}
```



commonJs（node环境）中  __dirname  拿到文件名

Esmodule（前端环境）中   import.meta.dirname 



模块处理(module)：js文件处理 、 ts文件处理、 css文件处理

需要通过额外的工具来做 （Loader做模块转换解析）

-js  ->  babel-loader

-ts  -> babel-loader + preset-typescript \ ts-loader

-image ->  raw-loader \ url-loader

-css ->css-loader \ style-loader \postcss



```
//babel核心包
@babel/core | @babel/preset-env | babel-loader
```



```
//webpack.config.js
module:{
    //规则配置，不同文件需要通过正则匹配
    rules:[
        {
            test:/\.js/,
            use:['babel-loader']
        },{
            test:/\.css/,
            use:[mini-css-extract-plugin.loader,/*"style-				 								loader"*/,'css-loader']
        }
    ]
}
//sytle-loader是将css通过脚本转化成js代码，将样式也放入js
```

```
//babel.config.js
export default {
    preset:["@babel/preset-env"]
}
```

使用babel编译后的代码可以更多地适配



css-loader:入口文件如果有引入，添加css-loader将会一并解析,在js文件当中，将其提取：

```
//mini-css-extract-plugin
将css文件提取出来,既需要在rule中use，也需要new 一个插件配置出口
								和style-loader有冲突
style-loader是想要把css解析到js中并渲染实现效果
但mini-css-extract-plugin是想要将css单独提取出来，通过link引入css样式
```







### 插件（plugin）：

1、工作流的辅助，HtmlWebpackPlugin 可以将webpack打包生成的资源自动加到html文件中script中

2、可以做代码压缩丑化

3、css文件提取

```
//这个插件可以实现自动将js添加到html中script标签引用
plugins:[
    new HtmlWebpackPlugin({
        template:"./src/index.html"
    })，
    new mini-css-extract-plugin({
        filename:"css/[name].css"
    })
]
```

 

优化：我们需要区分开发和生产环境的webpack配置，所以把公共的放在

`webpack.config.base.js`

然后开发环境在`webpack.config.dev.js`,生产环境在`webpack.config.prod.js`

```
//在实际文件中使用
import baseConfig from "./webpack.config.base.js"
import {merge} from "webpack-merge"

//浅拷贝不合适，这个方法不好，容易出bug
export default {
    mode:"具体环境",
    ...baseConfig
}

//采用这种方式进行合并
export defaut merge(baseConfig,{
    mode:"具体环境"
})
```



loader的本质是函数，在rules里面指定路径便可以使用loader



### webpack构建优化策略：

js、css代码压缩（loader、plugins）、图片资源压缩（image-webpack-loader）、内联关键css、js减少请求、

懒加载（按需加载）、启用tree-shaking移除未使用的代码、代码分割、热更新（devserver/hot）



## 模块化规范

```
//ESM  编译时   导出值共享内存
import  {} form ""
export default {}
```

```
commonjs  运行时  导出值拷贝内存
const path = require('path')
module.exports = {}
```



为什么需要定义模块化规范？



commonJs  支持原生nodejs，没办法做tree-shaking ，因为服务端不需要考虑动态切分、tree shaking



amd：require.js

esm：虽然浏览器

node是支持 commonJS规范的，想要其也支持esm规范，可以

1、package.json里面添加"type":"module"

2、将js文件后缀改成`mjs`

umd:通用模块化规范



rollup：Vue  Compositon库、Vue组件库方面首先考虑使用

不自动tree-shaking，需要设置

```
//rollup.config.js
//可以构建成不同形态的产物类型：esm|commonJS|umd

{
input:""   //入口文件
output:{
	clean: true  //打包前清理原文件
    file:""   //出口文件地址
    format:"es"  //构建成es规范的
  }  
}

{
input:""   //入口文件
output:{
	clean: true  //打包前清理原文件
    file:""   //出口文件地址
    format:"cjs"  //构建成es规范的
  }  
}

```

处理ts文件：使用插件处理ts（rollup-plugin-dts|rollup-plugin-flat-dts）

```
import {dts} from "rollup-plugin-dts"
import flatDts from "rollup-plugin-flat-dts"

{
input:"index.ts",   //入口文件
output:{
	clean: true  //打包前清理原文件
    file:""   //出口文件地址
    format:"es"  //构建成es规范的
  },
  plugins[dts({
  			compilerOptions:{
                declaration:true,
                //将类型提取输出到指定文件夹
                declarationDir:"es/types"
  			}    
  }),
  flatDts()]
}

{
input:"",   //入口文件
output:{
	clean: true  //打包前清理原文件
    file:""   //出口文件地址
    format:"cjs"  //构建成es规范的
  }  
}
```

`处理ts文件：（rollup-plugin-swc）处理ts的效率速度快多了`

ts正在使用go重写，性能至少提升十倍



esbuild: 非常灵活，编译速度快

缺点：没办法生成ts  dts类型文件声明文件，需要通过单独输出文件

【esbuild + rollup】



## 前端工程化设计

monorepo：把多个项目融合到一个里面来

需要引用到其他同级的包，下载版本设置"workspace:*",方便本地开发

必须git先初始化，再npx husky init

配置好脚本之后，我进行commit提交的时候，husky进行拦截并且可以执行pre-commit 中的指令



## 编译构建和打包和优化

### webpack5  开发环境

:::webpack尽量使用**commonjs规范**，不用将type设置为module，碰到问题：

设置导入文件后缀省略的时候resolve.extensions，严格模式下会有内部配置报

错，使用commonjs规范可以解决，如果使用的是esm，就把文件名改为.cjs后缀



1、开发模式：开发模式将mode设置成为development

2、将模式设置成production可以实现tree-shaking

3、输入输出配置：path、filename

4、source-map

```
//开启source-map
devtool:"source-map"
```

设置sourcemap生成的.js.map文件需要关注sources（协议下面文件的映射关系）和sourcesContent（原文件的具体内容特定转换后内容），动态导入就是切片（chunck）逻辑，动态导入(切片)的内容有自己的map文件

5、devServer：port端口号、proxy代理实现，用来设置开发环境启动命令

```
proxy：{
    "/api":{
        target:"域名/api"
    }
}
```

6、导入文件后缀问题：resolve.extensions解析文件才能实现省略后缀

```
resolve:{
    //注意：webpack配置文件尽量按照commonjs规范
    extensions:[".js",".json",".ts",".vue"],
    //设置别名
    alias:{
        '@':path.resolve(__dirname,"src")
    }
}
```

```
//配置完成之后才能省略后缀
import {sum} from "./sum"
```

7、resolve.module：明确模块路径，避免递归查找，如果模块存在循环引用的话就比较麻烦

8、缓存优化：cache   采用配置缓存，空间换取时间，多线程打包充分利用cpu

```
cache:{
    type:"filesystem" //会在node_modules下生成.cache文件，缓存之后速度变快（缓存命中）
    name:"AppbuildCache"
}
```

多线程打包可以使用thread-loader，配置option.worker设置进程数,多线程打包不一定会加快打包速度，主要还是看缓存命中情况和项目文件大小，太小的文件可能没效果



9、减少监听文件：watchOptions排除不必要的文件或依赖，降低系统资源消耗

```
watchOptions:{
    ignored:/node_modules/,   //排除node_modules目录
}
```



### webpack5 生产环境

1、切片压缩丑化代码     optimization.splitChunks | optimization.minimize

分chunck之后可以实现按需加载，加速首屏渲染过程

2、devtool："cheap-module-source-map"



## 函数式编程思想

函数式编程思想最大的特点是不可变性、无副作用（不修改全局变量和外部函数）以及纯函数（输出值只和输入值有关，无副作用）

1、纯函数，比如数组的slice方法和splice方法

slice：纯函数，输出指定数据，左闭右开

splice：不是纯函数，array.splice是将指定数据切割舍弃

### 纯函数的特点

（1）可缓存：由于输入值与输出值一一对应，所以可以缓存已经输出过的数据

3、函数柯里化

```
function curry(fn){
	function curried(...args){
		if(args.length >= fn.length){
			return fn.apply(this,args)
		}else{
			return function(...moreArgs){
				return curried.apply(this,args.concat(moreArgs))
			}
		}
	}
	return curried
}
```

4、compose函数，实现将多个函数组合成一个新函数，每个函数的输出作为下一个函数接收的参数

```
function compose(...fns){
    return function(x){
        return fns.ruduceRight((value,fn) =>fn(value),x)
    }
}

const double = x => x*2
const increment = x => x +1

cosnt doubleThenIncrement = compose(increment,double)
doubleThenIncrement(3)  //执行 3*2+1 = 7
```

5、pipe函数，从左往右顺序执行

```
function pipe(...fns){
    return function(x){
        return fns.ruduce((value,fn) =>fn(value),x)
    }
}

const double = x => x*2
const increment = x => x +1

cosnt doubleThenIncrement = pipe(increment,double)
doubleThenIncrement(3)  //执行 (3+1) *2 = 8
```

## vue性能优化

-识别性能瓶颈

-组件设计优化

-状态优化

-性能监控



项目带插件好一些：vite-plugin-vue-devtools

浏览器带插件有些功能省略

推荐用无痕模式打开：减少浏览器插件的影响

产物构建分析：rollup-plugin-visualizer：整体分析产物情况，进行优化，首次加载尽可能少



自动切分：按需导入---可以通过defineAsycComponent实现分包，在需要的位置放导入import，可以通过事件定义动态导入:

可以设置一个响应式的值去标识组件的状态，当事件处理中将组件的状态修改为true的时候，就可以实现事件中的动态导入并渲染,抽离hooks

```
interface UseDamicComponent {
    componentUrl : string
}


export const useDynamicComponent = (params:useDynamicComponentParams)=>{
    const {componenntUrl} = params
    const compLoaded = ref(false)
    
    let comp : any;
    
    const loadComp = () =>{
        compLoaded = true,
        comp = defineAsyncComponent (() => import(componentUrl))
    }
    
    return{
        Component: Comp,
        loaded:compLoaded,
        load:loadComp
    }
}
```



手动切分：配置chunk



## vue  use库

### 搭建流程：

1、设计monorope架构

-基于pnpm  workspace

-两个大的结构：app、packages

2、完成规范化处理

-eslint9

-stylelint

-cspell

-提交规范 cz-git+commitizen

-commitlint

3、搭建完整产品工程化内容

-packages子包设计

-apps业务相关包设计

4、打包构建方案调研和落地

-产物：rollup

-文档：vitepress

5、流程化、自动化处理

-github  actions





vue-demi的兼容性更好，更加轻量

vue包含视图渲染层、编译处理比较复杂，我们封装的不需要这么复杂



## git操作: 鄙人认为GUI工具是真的好用

基本：init初始化  add 添加暂存区  commit 输入信息

status修改状态   git remote set-url  origin https://设置远程仓库地址  git push -u origin 分支 提交代码

cherry-pick  选取某次commit进行操作

rebase:  调整head指向，合并过往提交commit history

新分支：git checkout -b feature/basic

git merge 并行合并子分支到主分支，会保留子分支，与主分支同步进行，合并之后可能不清楚先后顺序

git rebase 直接将子分支掰直回到主分支并保留操作，实现单线程进行，分有先后顺序

记得注意从哪个分支切出小分支

线上出现问题可以切一个hotfix分支进行修改



### **版本控制：**

版本号规范，反推迭代功能点 -> 发布计划

如何定义版本号：1.0.0

主版本.新增功能.修复

MAJOR.MINOR.PATCH

如果有一些测试版本怎么定义，1.0.0-beta.1

git 版本    直接打tag

npm 包版本号 : 可以使用`np`实现自动化修改版本号



### 构建缓存

-nx   

-turbo

目的：

-任务缓存，构建任务，产物进行缓存，下次如果命中缓存的话直接跳过构建

-任务并行化

-依赖管理：子包和子包的关系，各种依赖的关系

-monorepo之间的





## 计网

### OSI七层模型：

1、物理层：直接和物理介质打交道的，传输**比特流**

2、数据链路层：把比特流拼接成**数据帧**

3、网络层：寻址（IP作为媒介）+  路由

4、传输层：定义端口号，拥有TCP （三次握手四次挥手） UDP（速度快但是不稳定）

5、会话层（报文）：类似电话通话，在发送方和接收方进行通信时创建、维持的地方

6、表示层：解码

7、应用层：例如ajax调用接口发送http请求

### TCP|IP  五层|实际是四层模型

五层模型： 物理层  数据链路层  网络层  传输层  应用层

四层模型： 网络接口层  网际联络层  传输层  应用层



TCP & UDP 都属于OSI的传输层，

TCP面向连接，UDP无连接

TCP传输字节流，UDP面向报文

TCP安全传输慢 ，UDP尽快传输但是不保证交付给对方

TCP全双工通信，UDP支持一对一、一对多、多对一和多对多交互通信

### 三次握手

tcp是面向连接的：

SYN:发起新连接       ACK：确认序列号有效      seq:随机生成的序列号   ack：确认号 = 对方 seq + 1

![75457401676](assets/1754574016763.png)



### 四次挥手

![75457434686](assets/1754574346861.png)



**1、**客户端向服务端发送FIN包，并携带随机数序列号进入wait1阶段，  **2、**服务端接收之后返回ACK标识，并返回序列号+1，客户端进入进入wait2阶段（期间处理未完成的请求之类的），处理完之后  **3、**服务端才会进行第三次挥手，客户端接收到第三次挥手的包之后进入超时等待状态（持续几分钟，如果第四次挥手返回的ACK丢失的话，这段期间可以处理好服务端再次发送的第三次挥手包），**4、**最后客户端发送第四次挥手发送ACK标记



### CDN内容分发

主要是优化网络资源优化时间的

负载均衡：往相近的服务器分散压力



### 跨域

为什么有跨域：浏览器同源策略限制，浏览器会拒绝跨域请求

协议 + 域名 +端口

解决方案：

1、前后端协商jsonp：

```
//通过script的src不受同源策略限制，可以跨域请求数据，只能发送get请求
//后端返回的是函数 这个函数是在前端定义的  他把值注入到函数的参数中

const jsonp = (name) => {
    let script = document.creatElement("script")
    script.src = "http://localhost:3000/api/jsonp?callback=" + name
    document.body.appendChild(script)
    return new Promise((resolve) => {
        window[name] = (data) =>{
            resolve(data)
        }
    })
}

jsonp(`callback${new Date().getTime()}`).then(res=> {
    console.log(res)
})
```

2、前端代理proxy:借助构建工具(webpack/vite)

```
//vite.config.js

import {defineConfig} from "vite"

export default defineConfig({
    server:{
        proxy:{
            "api":{
                target:"http://localhost:3000"
                changeOrigin:true
            }
        }
    }
})
```

3、后端设置请求头:设置请求头（这样不安全，可以把后面的*指定IP）CORS

```
app.get("/",(req,res) => {
    res.setHeader("Access-Control-Allow-Origin","*")
	res.send("Hello Jice")
})
```

4、nginx代理

```
通过ubutu下载nginx :  apt-get install nginx
在计算机Linux下面的etc里面找到nginx配置default
代理内容
location /api {
    proxy_pass http://主机iP：端口；
}

windows本地IP： cat /etc/resolv.conf

启动nginx服务： nginx
```

### Ajax

异步更新页面内容，提高用户体验

对SEO不是很友好，爬虫无法抓取到Ajax中的内容

```
//readyState 4: 完成，服务器已完成数据传输
//readyState 3：正在接收，服务器正在处理

const sendAjax = () => {
    const xhr = new XMLHttpRequest()
    xhr.open("get","请求地址"，true是否异步)
    
    xhr.onreadyStatechange = () => {
        if(xhr.readyState === 4 && xhr.status === 200){
            console.log(xhr.responseText)
        }
    }
    
    xhr.send(发送信息给后端)
}
```

### fetch api

```
发送请求，默认是get，返回的是promise对象，可以在第二个参数设置对象包含请求方法method，
const abort = new AbortController()
const sendFetch = () => {
    fetch("http://localhost:3000/api",{
        signal:abort.signal,
    }).then(async res => {
        return res.json()
    }).then(data => {
        console.log(data)
    })
}

const stop = () => {
    abort.abort()  //打断施法
}
```

### SSE

### GET和POST的区别

**本质**上都是`TCP链接`，并无差别

区别：

1. GET在浏览器回退时时无害的，而POST会再次提交请求

2. GET请求会被浏览器主动Cache，而POST不会，除非手动设置（主要由服务端的响应头决定，服务端设置：

   ```
   app.post('/api/data', (req, res) => {
     // 处理 POST 请求逻辑...
     
     // 设置响应头允许缓存
     res.setHeader('Cache-Control', 'public, max-age=3600');
     res.setHeader('Expires', new Date(Date.now() + 3600 * 1000).toUTCString());
     res.setHeader('ETag', 'data-version-1'); // 可根据数据版本动态生成
     
     res.json({ data: '缓存的 POST 响应' });
   });
   ```

3. GET请求的参数可以完整被存放在浏览器历史记录里，但是POST不可以存放在浏览器历史记录里面

4. GET没有POST安全，因为请求参数暴露在url上，不能用来传递敏感信息

5. GET通过URL传参，POST放在Request body里面

   ![1b2028062c73898d45c67de60146b0eb](assets/1b2028062c73898d45c67de60146b0eb.png)



![ffc32c974a2637116e96690a197400a0](assets/ffc32c974a2637116e96690a197400a0.png)

基本数据类型：number null  boolean  Bigint  symbol  string undefined



## 函数式编程（防抖节流柯里化）

```
<body>
	<button onclick="throttleFn">执行节流</button>
	
	<script>
		function throttle(fn,timeout){
		   let timer = null
            return function(...args){
                if(timer) return
                timer = setTimeout(() => {
                    fn.apply(this,args)
                    timer = null
                },timeout)
            }
		}
		
		const throttleFn = throttle(function(){
            console.log("throttle执行了")
		},3000)
	</script>
</body>
```

```
function debounce(fn,timeout){
    let timer = null
    return function(...args){
        clearTimeout(timer)
        timer = setTimeout(() =>{
		fn.apply(this,args)
		},timeout)
    }
}
```



```
function curry(fn){
    return fucntion curried(...args){
        if(args.length >= fn.length){
            return fn.apply(this,args)
        }else{
            return function(...args2){
                return currid.apply(this,args.concat(args2))
            }
        }
    }
}
```

```
//防抖
funtion debounce(fn,delay){
	let timer =null
	return funtion(...args){
		clearTimeout(timer)
		timer = setTimeout(() => {
			fn.apply(this,args)
		},delay)
	}
}

//节流
funtion throttle(fn,delay){
	let timer =null
	return funtion(...args){
		if(timer) return
		timer = setTimeout(() => {
			fn.apply(this,args)
			timer = null
		},delay)
	}
}
```



```
function compose(...fns){
    return function(x){
        return fns.reduceRight((value,fn) => fn(value),x)
    }
}
```

## 关于vue2/3的原型链

1、在vue2当中使用的是Vue.prototype来全局挂载方法进行使用，例封装axios

```
//Vue2
// src/main.js

axiosInstance = axios.create({
	baseURL: 'http://api.example.com/',
	timeout:5000,
	headers:{'x-Custom-Header': 'foobar'}
})

//全局挂载http方法，可以通过原型this.$http.get('URL')使用
//$命名是vue生态挂载载原型上的命名约定
vue.protype.$http = axiosInstance
```

好处：创建独立的axios对象是一个好习惯，有助于统一管理拦截器和基地址等等

坏处：任何对vue2原型上的修改都可能导致**变量污染，测试困难**：变量污染可能导致测试结果不稳定



2、vue3当中的app.config.globalProperties解决vue2的原型方法问题

Vue 3引入了`createApp`函数，取代了Vue 2中的`new Vue()`。`createApp`返回一个**应用实例（application instance）** ，这个实例是独立的、自包含的。与Vue 2中所有实例共享同一个全局`Vue`构造函数不同，Vue 3中的每个应用实例都有自己独立的配置和作用域 

```
//Vue3
//src/main.js

const app = createApp(App)

// 注册一个全局的格式化函数
app.config.globalProperties.$formatCurrency = (value) => {
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: 'USD'
  }).format(value);
};

app.mount('#app');
```

在Vue3当中集成axios

```
// src/main.js (Vue 3)
import { createApp } from 'vue';
import App from './App.vue';
import axios from 'axios';

const app = createApp(App);

// 将 Axios 挂载到全局属性
app.config.globalProperties.$http = axios.create({
  baseURL: 'https://api.example.com/'
});

app.mount('#app');
```

使用

```
<script setup>
import { onMounted, getCurrentInstance } from 'vue';

// 1. 获取当前组件实例
const { appContext } = getCurrentInstance();

// 2. 从实例的上下文中访问全局属性
const $http = appContext.config.globalProperties.$http;

onMounted(async () => {
  try {
    const response = await $http.get('/users');
    console.log(response.data);
  } catch (error) {
    console.error('Failed to fetch users:', error);
  }
});
</script>

```

3、精炼解释原型链



**“核心回答”：** 原型链的最大好处是提升内存效率；允许成千上万个对象实例共享同一套定义在单个原型对象上的公共方法，从而避免每个实例重复创建每个实例方法



**“拓展解释”：**举个例子，当我们使用Array创建一个数组实例的时候，不需要去给每个数组对象挂载方法（如.map,.filter等等），因为这些数组实例都通过原型链，共同指向并使用定义在Array.prototype这个全局唯一的原型对象上的那一套方法

## CSS相关

### BFC

同一个BFC(块级格式上下文)相邻的子元素的margin会发生重叠（塌陷），选取大的margin

- 防止坍塌：包裹容器分成两个BFC

2、浮动元素也会导致内容塌陷：

- 防止塌陷：overflow:hidden  ※

### CSS响应式布局

是什么：同时适配多端，网站根据视口调整模块的大小和位置

实现：通过媒体查询检测

①前提： 页面头部必须有**meta**声明**viewport**

```
<meta name="viewport" 
	  content="
	  		width=device-width,   //自适应手机屏幕
	  		maximum-scale=1,      //缩放最大比例
	  		initial-scale=1,      //缩放的初始比例
	  		user-scalable=no      //用户是否可以缩放网页
		"
		>
```

②实现方式：

- 媒体查询：可以设置不同类型的媒体条件 （@Media）

  当视口在375px - 600px之间，设置特定字体⼤⼩18px

  ```
  @media screen (min-width: 375px) and (max-width: 600px) {
  body {
  font-size: 18px;
  }
  }
  ```

- 百分比：%

  宽高（width、height）依托父标签的宽高

​       border-radius设置成百分比：相对于自身宽度

**注：！**如果使用百分比，会形成布局的复杂度，不建议使用百分比来实现响应式

- vw/vh：表示相对于视图窗口的宽度，vh表示相对于视图窗口的高度

  1vw表示视图宽度的百分之一

- rem:相对于根元素**html**的font-size,相对根元素的font-size属性，默认情况是浏览器字体大小位16px

  ### 元素水平居中

  **！！！！定位都是子绝父相**

  - 利用定位 + margin:auto

    子绝父相，子元素使用margin:auto

    ```
    //注意！！！
    子元素的top、right、bottom、left都需置为0 || inset:0
    // inset可以同时设置四个属性
    ```
  
  - 利用定位+margin: - (width/2) px,移动是以子元素的左上角定位
  
    ```
    //注意！！！
    子元素需要margin-left && margin-right =  - (width/2) px
    ```
  
  - 利用定位+transform:子元素top&&left: 50%
  
    ```
    //注意！！！
    //子元素使用transform(-50%,-50%)
    // x、y平移
    ```
  
  - flex弹性布局：父元素使用**display:flex**,并且配置：子元素居中
  
    ```
    .father{
    	display:flex;
    	justify-content:center;  //子元素垂直居中
    	align-content:center;	//子元素水平居中
    }
    ```
  
  - grid网格布局：父元素使用display:grid,并且配置：子元素居中
  
    ```
    .father{
    	display:grid;
    	justify-content:center;  //子元素垂直居中
    	align-content:center;	//子元素水平居中
    }
    ```
  
    
  
  
  
  ## 基于Chrome浏览器原理
  
  

1、**重排重绘分析工具**：more tool  -> Rendering   打开观察DOM元素repainted  情况

2、**面试回答：先整体（宏观）再细节（微观)**

​      例如：请描述页面渲染的过程

​       **宏观**：浏览器层接收URL，然后建立TCP连接通过网络层操作像DNS解析、发送request给服务端，接收response回来，然后客户端处理返回response的字符串进行构建DOM树和CSSOM树处理，并且之后处理交互操作等等

​       **细节**：详细描述一遍从：DNS解析 ->TCP连接-> 握手-> 客户端发送请求-> 服务端处理请求->返回HT TP响应->浏览器渲染页面->执行js脚本->事件处理与交互





## js数据结构与算法

1、链表反转

```
//个人理解，相当于从头到尾反向绕了一圈，然后从p1.next(也就是最开始的head断开：指向temp代表的null，temp没有指向)

function reverseList(head) {
	//p1表示原链表
	let p1 = head;
	
	//p2用来存储新链表
	let p2 = null;
	
	//终止条件：头节点指向null
	while(p1){
	
	//声明临时变量temp存储反转链表
	//head节点的下一个节点指向temp
	const temp = p1.next;
	p1.next = p2;
	p2 = p1;
	p1 = temp;
	}
	
	return p2
}
```

图示![image-20251029211218196](assets/image-20251029211218196.png)

2、有效括号字符串，也可以使用纯栈的方法求解

```
function isValid(s){
	const map = new Map()
	map.set('(',')'),
	map.set('{','}'),
	map.set('[',']')
	
	const stack = []
	for(let i = 0;i<s.length;i++){
		const c = s[i]
		if(map.has(c)){
			stack.push(c)
		}else{
			cosnt t = stack[stack.length -1]
			if(map.get(t) === c){
			stack.pop()
			}else{
				//否则不是有效括号
				return false
			}
		}
		
	}
	return stack.length === 0
}
```

3、删除链表相同元素

```
function deleteSame(head){
	let p = head;
	while(p&&p.next){
		if(p.val == p.next.val){
			//删除下一个节点
			p.next == p.next.next
		}else{
			//继续遍历
			p = p.next
		}
	}
	//返回传入的链表
	return head
}
```

4、反转链表

```
function reverseList(head){
	let p1 = head
	let p2 = null
	while(p1.next !== null){
		let temp = p1.next
			p1.next = p2
			p2 = p1
			p1 = temp
	}
	return p2
}
```

5、两数之和（map

```
function SumToTarget(arr,target){
	const map = new Map()
	for(let i = 0;i<arr.length -1;i++){
		const toSum = target - arr[i]
		if(map.has(toSum)){
			return [map.get(toSum),i]
		}else{
			map.set(arr[i],i)
		}
	}
}
```

6、数组去重（map

```
function interSection(nums1 , nums2){
	const map = new Map()
	//将nums1传入字典
	nums1.forEach(n => map.set(n,true))

		let res = []
		nums2.forEach( n => {
			if(map.has(n)){
				res.push(n)
				
				map.delete(n)
			}
	}
	return res
}
```

7、字符的有效括号

```
function isActiveCludes (s){
	if (s.length%2 !==0) return

	const map = new Map()
	map.set("(",")")
	map.set("{","}")
	map.set("[","]")
	
	const stack = []
	for(let i = 0;i<s.length-1;i++){
		const temp = s[i]
		if(map.has(temp)){
			stack.push(temp)
		}else{
			//不能直接pop，应该是取出栈顶元素
			const t = stack[stack.length - 1]
			if(map.get(t) === temp){
				stack.pop()
			}
		}
	}
	return stack.length ===0
}
```

8、最小覆盖字串(难看懂，后续处理)

```
```

9、树的遍历

```
//1、深度优先遍历(dfs)
const dfs = (tree) => {
	tree.children.forEach(dfs)
}

//2、广度优先遍历(bfs)

const bfs = (tree) => {
	const queue = [tree]
	
	while(queue.length>0){
		const temp = queue.shift() //取出根节点
		console.log(temp.val)     //模拟访问根节点
		temp.forEach(n => queue.push(n))  //将所有子节点&&子节点的子节点入队列，先进先出
	}
}
```

10、二叉树的遍历

```
//1、先序遍历（根左右）
//①递归
const preOrder = (tree) => {
	if(!tree) return
	
	console.log(tree.val)
	preOrder(tree.left)
	preOrder(tree.right)
}

//2、中序遍历（左根右）
const midOrder = (tree) => {
	if(!tree) return
	
	midOrder(tree.left)
	console.log(tree.val)
	migOder(tree.right)
}


//3、后序遍历（左右根）
const postOrder = (tree) => {
	if(!tree) return
	
	postOrder(tree.left)
	postOrder(tree.right)
	console.log(tree.val)
}
```

11、二叉树的最大深度

```
 function GetMaxLength (root) {  //n表示当前节点，l表示深度
	let res = 0
	
	//使用深度优先遍历
	const bfs = (n,l) => {
		if(!n) return
		
		//如果没有左右子节点就更新深度
		if(!n.left && !n.right){
			res= Math.max(l,1)
		}
		
		bfs(n.left,l+1)
		bfs(n.right,l+1)
	}
	
	bfs(root,1)
	return res
}
```

12、层序遍历

和广度优先遍历的区别是层序遍历只是针对二叉树，广度优先遍历的对象支持图和树

13、目前的数据结构js表示

```
//链表
const List = {
	val:1
	next{
		val:2
		next{
			val:3
			next{
				null
			}
		}
	}
}


//树 .....
const tree = {
	val:1
	left:{ //根节点左子树
		val:1
		right:{
			val:1
		}
	}
	right:{//根节点右子树
	  val:2
	}
}


//图:有向图就是对应的指向
const graph = {
	0:[1,3]
	1:[0,2]
	2:[1,3]
}
```

14、创建各种数据结构的类对象声明

```
//1、链表
class createList = {
	constructor(val,next){
		this.val = val
		this.next = next
	}
}

//声明一个链表
const head = new CreateList(1,node2)
const node2 = new CreateList(2,node3)
const node3 = new CreateList(3,null)   //链表结构： 1->2->3->null
```

15、图的遍历

```\
//深度优先遍历
function dfsGtaph(graph,start){
	const visited = new Set()
	const result = []
	
	function def(node){
		visited.add(node)
		result.push(node)
		
		
		for(const neighbor of graph[node]){
			if(!visited.has(neighbor)){
				dfs(neighbor)
			}
		}
	}
	dfs(start)
	return result
}


//广度优先遍历
function bfs(graph,start){
	const visited = new Set()
	visited.add(start)
	const queue = [start]  //将首节点放入队列
	
	const result = []  //接收已访问的节点
	while(queue.length>0){
		const node = queue.shift() //移除队首元素
		result.push(node)
		
		for(const neighbor of graph[node]){
			if(!visited.has(neighbor)){
				visited.add(neighbor)
				queue.push(neighbor)
			}
		}
	}
	return result
}
```

16、常见的算法思想

1、冒泡排序：前一个比后一个元素大就交换二者位置，遍历所有元素

```
Array.prototype.buffer = function(){
	//i是控制每个数组需要排序的轮数
	for(let i = 0;i<this.length-1;i++){
	//j是表示每个元素需要对比几次
		for(let j = 0;j<this.length-1-i;j++){
			if(this[j] > this[j+1]){
				[this[j],this[j+1]] = [this[j+1],this[i]]
			}
		}
	}
}
```

2、选择排序：遍历所有元素，将最小的元素放置到最前面

```
Array.prototype.selectorIndexMin = function () {
	for(let i = 0 ; i< this.length-1 ; i++){
		let MinIndex = i
		for(let j = i;j<this.length;j++){
			if(this[j] < this[MinIndex]){
				MinIndex = j
			}
			
			if( MinIndex !== i){
				return (this[j],this[MinIndex]) = (this[MinIndex],this[j])
			}
			
		}
	}
}
```

3、插入排序：两两比较，大的往后排，最后形成从小到大的排序

```
Array.prototype.Insertation = function () {
	//从第二个元素开始，和前一个元素比较，如果当前大于前一个不动，如果小于前一个就交换，循环
	for(let i = 1; i<this.length;i++){
		let temp = this[i]
		
		let j = i
		while(j>0){ 
			if(this[j - 1] > temp){
				(this[j],this[j-1]) = (this[j-1],this[j])
			}else{
				break
			}
			j--
		}
	}
	this[j] = temp
}
```

4、快速排序：选择基准，大于基准push到右侧，小于基准push到左侧

```
Array.prototype.quickSort = function () {
	const rec = (arr) => {
		if(arr.length === 0) return arr
		
		let left = [];
		let right = [];
		
		//选取基准（以arr[0]为例）
		const mid = arr[0];
		for(let i = 0;i<arr.length;i++){
			if(arr[i]>arr[0]){
				right.push(arr[i])
			}else{
				left.push(arr[i])
			}
		}
		return [...rec[left],...rec[right]]
	}
	const res = rec(this);
	res.forEach((n, i) => this[i] = n);
}
```

17、二分查找

```
Array.prototype.SelectByMid = function () {
	let min = 0;
	let high = this.length - 1
	
	while(min < high){
		//取中间值
		let mid = (min + high) >> 1     //是Math.floor((min+high)/2)的优化写法
		if(this[mid] > this[min]){
		//值在左侧
			min++
		}else if{
			high--
		}else{
			return this[mid]
		}
	}
	return -1          //表示没有找到这个值
}
```

17、猜数字大小

```
function GuessNumber(num){
	
	let min = 0;
	let heigh = num;
	
	let mid = (min + heigh) >> 1
	guess(num)
	
	if (res === 0) {
            return mid; // 猜对了，返回结果
        } else if (res === 1) {
            low = mid + 1; // 猜小了，缩小范围到右半部分
        } else {
            high = mid - 1; // 猜大了，缩小范围到左半部分
        }
    }
    
    return -1; // 理论上不会执行，因为目标值一定在1到n之间
	}
}
```

18、动态规划：举得例子没有搞懂

19、





## ES6核心特性 vs ES5

### 声明变量

1、es5使用var来进行声明变量，var声明的变量存在变量提升，var允许重新声明变量，并且var声明的变量在统一作用域当中存在变量覆盖的问题，可能会导致数据丢失

2、es6新增const和let进行变量声明，这两个声明方式存在块级作用域以及暂时性死区，令变量声明更加规范，不可重复进行声明

### 对数据的操作方法更新

1、使用[...arr]将数组元素展开，使用的是浅拷贝，将引用对象的值拷贝到新数组

2、Array.from()类似于map将键取出来、Array.of()将一些数字转换为数组

### 函数的更新

1、函数允许给参数绑定默认值

2、fn(a,b,c=1).length =2  可以检测出未赋值的参数个数,如果是fn(a=0,b,c).length =0

3、只要函数参数使⽤了默认值、解构赋值、或者扩展运算符，那么函数内部就不能显式设定为严格模

式，否则会报错

4、新增箭头函数：更简洁优雅、没有自己的this指向、不能用来构造实例对象、没有arguments对象

### 对象的更新



## 面试项目亮点总结

### 实习项目

1. 如何处理websocket实时处理音频流数据？（流式 接收弹幕音频

   答：使用vuex当中监听给流式弹幕定义的事件，为此封装了一个hook，核心思路是

   - 使用vuex监听wss消息，筛选出弹幕音频事件
   - 使用队列机制管理收到的音频，以免乱序播放（队列先进先出
   - 利用web Audio API 进行音频解码播放
   - 实现完整的播放生命周期，包括前置、后置回调处理

   2、为什么使用队列播放？直接播放不行吗？
   答：

    顺序保证：队列先进先出的特性确保了音频的正常播放，避免了由于服务端返回音频信息时间不同导致的乱序

    并发控制：避免多个音频同时播放造成混乱

   完整性操作：通过completed标识音频片段完整性，确保完整音频播放完成之后再执行后置操作

​	3、如何实现人工接管的

​        答：监听直播状态判断是否需要继续播放

​				人工接管式立即暂停音频和数据接收

​				恢复接受时延时2s重新开始接收，避免上一段数据没有传输完全

​	4、这个hook的性能优化点有哪些

​		答：

1. **资源管理**：正确释放AudioContext和AudioBufferSourceNode，避免内存泄漏
2. **状态控制**：通过isPlaying状态避免重复触发播放流程
3. **队列优化**：使用数组shift方法实现先进先出队列，时间复杂度O(1)
4. **错误处理**：对音频解码失败等异常情况进行捕获和处理，保证系统稳定性

​	5、这个hook的可拓展性如何？

​	答：

1. **回调机制**：通过beforeFn和afterFn参数支持自定义前置和后置处理逻辑
2. **模块化设计**：将音频处理、队列管理、状态控制等逻辑分离
3. **类型安全**：使用TypeScript接口定义数据结构，提高代码可维护性

#### 弹幕音频流程

**播放流程：**

1. WebSocket接收到弹幕音频数据时触发回调函数

2. 调用`initAudioContext()`初始化音频上下文（如果尚未初始化）

3. 调用

   ```
   handleAudioData()
   ```

   处理音频数据：

   - 将音频数据加入播放队列`audioQueue`
   - 如果当前没有在播放，则调用前置处理函数并启动播放流程

4. ```
   playNextInQueue()
   ```

   函数负责实际播放：

   - 从队列中取出音频数据
   - 解码音频数据
   - 创建音频源并连接到输出设备
   - 开始播放音频

**递归播放下一个：**

1. 当前音频播放完成后，会自动调用`playNextInQueue()`播放下一个音频
2. 如果队列为空：
   - 如果标记为已完成(`completed=true`)，直接调用后置处理函数
   - 如果未完成，延迟2秒后执行后置处理函数（防止新音频快速到达）

**停止播放：**

1. 组件卸载或主动停止时调用`stopDanmuPlayAudioStream()`
2. 清理WebSocket监听器
3. 停止当前音频播放
4. 断开音频源连接
5. 关闭音频上下文

```
我想分享一下我在项目中实现的实时弹幕音频流播放功能。这是一个利用现代Web技术（主要是Web Audio API）来实现在直播过程中无缝播放观众发送的语音弹幕的功能。

1. 技术选型与核心API： 我选择了Web Audio API而不是传统的<audio>标签来处理这些实时音频流。主要原因在于Web Audio API提供了更强大的底层控制能力，能够精确地处理、解码和播放短小的音频片段，这对于实时性要求很高的弹幕场景非常合适。

2. 核心处理机制：

音频上下文管理 (AudioContext):

我首先创建了一个AudioContext实例作为所有音频处理的核心环境。
遵循浏览器的安全策略，我在用户首次交互（如点击）时激活了这个上下文，确保能正常播放音频。
数据接收与队列化 (handleAudioData):

通过WebSocket监听后端推送的live_audio_stream事件来接收音频数据（通常是AAC格式的二进制数据）。
接收到的数据会被包装成一个对象（包含音频数据、文本、点位信息等），然后推入一个本地的播放队列 (audioQueue) 中。这保证了即使网络有波动，也能按顺序播放。
异步解码与播放 (playNextInQueue):

这是核心逻辑。我会从队列中取出一个音频项。
使用audioContext.decodeAudioData()异步地将原始的AAC二进制数据解码成浏览器可以处理的AudioBuffer。
创建一个AudioBufferSourceNode作为音频源，将其buffer属性设置为解码后的AudioBuffer。
通过source.connect(audioContext.destination)将音频源连接到输出设备（扬声器）。
调用source.start(0)立即开始播放。
链式播放与状态同步:

利用AudioBufferSourceNode的onended事件，在当前音频播放完成后，自动调用playNextInQueue函数来播放队列中的下一个音频，形成一个连续的播放链。
通过isPlaying状态来管理播放流程，避免并发播放或重复触发。
与主流程的集成 (Callbacks):

在播放弹幕音频之前，我需要暂停主直播脚本的音频播放，以免声音冲突。为此，我设计了beforeFn回调，在开始播放队列前由主组件执行暂停脚本音频的操作。
在所有弹幕音频播放完毕后，通过afterFn回调通知主组件，使其恢复脚本音频的播放。
特殊场景处理:

人工接管: 监听了Vuex中的“人工接管”状态。当主播手动接管直播时，会清空音频队列并暂停接收新数据；接管结束后再恢复。
点位联动: 在播放弹幕音频前，会根据携带的点位信息 (point) 调用机器人移动函数，实现声音与机器人的动作联动。为了避免频繁移动，我还加入了基于时间的冷却机制（如15秒内不重复移动）。
3. 亮点总结：

低延迟与流畅性: 通过Web Audio API和队列机制，实现了近乎实时的音频播放体验。
模块化与解耦: 将复杂的音频播放逻辑封装成一个独立的Vue组合式函数 (usePlayDanmuAudioStreamCtrl)，通过回调函数与主组件交互，提高了代码的可维护性和复用性。
健壮性: 考虑了各种边界情况，如空音频数据、播放中断、状态同步等，增强了系统的稳定性。
通过这个功能，我不仅深入学习了Web Audio API的应用，也提升了对复杂状态管理和异步流程控制的理解。
```

```
在实现这个功能的过程中，我也遇到了几个比较核心的技术挑战：

浏览器自动播放策略限制：

问题：现代浏览器为了用户体验，禁止网页在未经用户交互的情况下自动播放音频。这导致我们的弹幕音频在首次尝试播放时可能会失败。
解决方案：我通过监听用户的首次交互事件（如页面上的任意点击），在该事件回调中显式调用 audioContext.resume() 来激活被挂起的 AudioContext。这确保了后续的弹幕音频可以顺利播放。
实时音频流的连续性与稳定性：

问题：弹幕是高频且随机到达的，如何保证这些短音频片段能够无缝、稳定地播放，而不出现卡顿或重叠，是一个难点。直接使用<audio>标签难以精细控制每个片段的衔接。
解决方案：我采用了队列 (audioQueue) + Web Audio API链式播放的机制。每当收到新的音频数据，就将其推入队列。播放函数 (playNextInQueue) 会依次从队列中取出数据进行解码和播放，并利用 AudioBufferSourceNode 的 onended 事件来触发下一次播放。这种方式天然支持了先进先出和播放完成后的自动衔接，有效保证了播放的连续性和稳定性。
与主直播音频的协调与冲突避免：

问题：系统同时存在主直播脚本的音频播放和弹幕音频播放。如果两者同时发声，会造成声音混乱，影响用户体验。
解决方案：我设计了前置 (beforeFn) 和后置 (afterFn) 回调机制。在开始播放弹幕音频队列之前，通过 beforeFn 回调通知主组件（AudioPlay.vue）暂停当前的脚本音频播放。在弹幕音频队列全部播放完毕后，通过 afterFn 回调通知主组件恢复脚本音频的播放。这样就巧妙地解决了两种音频源之间的资源竞争问题。
复杂状态管理与边缘情况处理：

问题：需要管理多种状态，如播放/暂停、队列是否为空、是否处于人工接管模式等。同时还要处理一些边缘情况，比如某个弹幕音频数据包可能为空。
解决方案：我利用 Vue 的 ref 来管理响应式状态（如 isPlaying, audioQueue）。对于边缘情况，我在代码中加入了明确的判断和处理逻辑，例如在 handleAudioData 中检查数据是否为空，在 playNextInQueue 中处理空队列和空数据的情况，确保程序在各种情况下都能稳健运行。对于人工接管，我通过监听 Vuex 状态来清空队列并暂停数据接收。
通过解决这些问题，我不仅加深了对 Web Audio API 和异步编程的理解，也提升了处理复杂前端交互和状态管理的能力。
```



### 无代码

1、为什么使用`defineAsyncComponent`方案来实现远程物料的加载？还有哪些方案可以实现远程物料的加载？有什么区别？

 	答：这是vue内置的异步加载方案，目标是优化单应用内的组件加载性能，这个方案相对灵活，只需要加载物料对应资源，结合**vue 中的h函数**就可以实现物料接入（补充：h函数将获取到的远程物料创建虚拟节点，由于vue渲染限制必须是规范的组件对象，h函数创建的虚拟dom节点无法直接渲染，`defineAsyncComponent`可以实现将异步任务返回的虚拟节点包装成vue可识别的组件对象。）

​			还有个方案是webpack/vite构建工具提供的`Module Federation`实现的支持**跨应用的远程共享**，将远程应用的模块暴露模块然后在项目种导入

```
//Federation实现远程项目宿主项目物料共享
1、推荐使用vite官方推荐的插件 @originjs/vite-plugin-federation
2、vite.config.js里面配置
plugins[vue(),
		fedetation({
			name:'xxx',
			fileName:'远程入口文件名',
			exposes:{
				//格式为 './路径':'实际文件路径'
			}
		})]
3、写完组件之后打包部署到服务器之后供宿主应用使用
4、宿主组件也需要使用该插件，然后动态导入远程组件，使用defineAsyncComponent加载接收
5、宿主组件成功使用
```



## 工程化相关配置

```
//简版展示Eslint.config.js
import js from '@eslint/js'
import pluginVue from 'eslint-plugin-vue'
import vueEslintParser from 'vue-eslint-parser'
import tsParser from '@typescript-eslint/parser'

export default [
  {
    files: ['**/*.{js,jsx,ts,tsx,vue}'],
    rules: {
      ...js.configs.recommended.rules,
      ...pluginVue.configs["flat/recommend"].rules,
      "no-console": "error",
    },
    languageOptions: {
      parser: vueEslintParser,
      parserOptions: {
        parser: tsParser,
      }
    },
  }
]

```

```
//cz-git  限制提交规范commit 内容
//配置文件在node_moudules/bin/git-cz

//自定义配置commitlint.config.js
// Emoji
/** @type {import('cz-git').UserConfig} */
export default {
    extends: ['@commitlint/config-conventional'], // extends can be nested
    parserPreset: 'conventional-changelog-conventionalcommits',
    prompt: {
        settings: {},
        messages: {
            skip: ':skip',
            max: 'upper %d chars',
            min: '%d chars at least',
            emptyWarning: 'can not be empty',
            upperLimitWarning: 'over limit',
            lowerLimitWarning: 'below limit'
        },
        types: [
            { value: 'feat', name: 'feat:     ✨  A new feature', emoji: '✨ ' },
            { value: 'fix', name: 'fix:      🐛  A bug fix', emoji: '🐛 ' },
            { value: 'docs', name: 'docs:     📝  Documentation only changes', emoji: '📝 ' },
            {
                value: 'style',
                name: 'style:    💄  Changes that do not affect the meaning of the code',
                emoji: '💄 '
            },
            {
                value: 'refactor',
                name: 'refactor: 📦️   A code change that neither fixes a bug nor adds a feature',
                emoji: '📦️ '
            },
            {
                value: 'perf',
                name: 'perf:     🚀  A code change that improves performance',
                emoji: '🚀 '
            },
            {
                value: 'test',
                name: 'test:     🚨  Adding missing tests or correcting existing tests',
                emoji: '🚨 '
            },
            {
                value: 'build',
                name: 'build:    🛠   Changes that affect the build system or external dependencies',
                emoji: '🛠 '
            },
            {
                value: 'ci',
                name: 'ci:       🎡  Changes to our CI configuration files and scripts',
                emoji: '🎡 '
            },
            {
                value: 'chore',
                name: "chore:    🔨  Other changes that don't modify src or test files",
                emoji: '🔨 '
            },
            { value: 'revert', name: 'revert:   ⏪️  Reverts a previous commit', emoji: ':rewind:' }
        ],
        useEmoji: true,
        confirmColorize: true,
        emojiAlign: 'center',
        questions: {
            scope: {
                description: 'What is the scope of this change (e.g. component or file name)'
            },
            subject: {
                description: 'Write a short, imperative tense description of the change'
            },
            body: {
                description: 'Provide a longer description of the change'
            },
            isBreaking: {
                description: 'Are there any breaking changes?'
            },
            breakingBody: {
                description:
                    'A BREAKING CHANGE commit requires a body. Please enter a longer description of the commit itself'
            },
            breaking: {
                description: 'Describe the breaking changes'
            },
            isIssueAffected: {
                description: 'Does this change affect any open issues?'
            },
            issuesBody: {
                description:
                    'If issues are closed, the commit requires a body. Please enter a longer description of the commit itself'
            },
            issues: {
                description: 'Add issue references (e.g. "fix #123", "re #123".)'
            }
        }
    }
}
```

```
//husky实现提交过程中拦截：自动校验提交信息、代码测试逻辑
//设置scirpt指令 -》 npx husky init
//在.husky/pre-commit 里面写校验指令
//.husky/commit-msg 里面写死信息
```

```
//lint-staged依赖只校验暂存区的文件
//git 分为三个阶段提交  本地代码 -》 本地仓库 -》 远程仓库
//本地代码 -》本地仓库    中间有一个暂存区 也就是git add . 存的地方
//我们只校验本地代码到暂存区里面的内容
```

```
//tsconfig.json
//命令行配置 npx tsc --init
```

```
//如何一步一步创建完整的vue项目架构
npm init -y  -> 创建package.json
src目录下：
1、入口：main.ts
2、入口组件：App.vue
3、安装相关依赖 （devDependecvies:vite，@vitejs/plugin-						vue,vue-router,pinia
			  （dependencies:vue
4、定义脚本：
	("script":{
		"dev":"vite",
		"build": "vite build",
		"preview": "vite preview"
	})
5、根目录创建index.html作为真正的入口文件
(Vite 的 UNB 方案 指的是 Unbundle（非打包）开发模式，核心是开发阶段不进行完整打包，直接通过浏览器原生 ES Module 支持加载模块，从而实现极速的热更新（HMR）和启动速度)

index.html核心代码块：
	<div id="app"></div>  //挂载节点
	<script type="module" src="./src/main.ts"></scirpt>

6、定义基本的App.vue
7、main.ts
	import {createApp} from "vue"
	import {createPinia} from 'pinia'
	import {App} from "./App.vue"
	import {router} from "./router"
	const app = createApp(App)
	//基于vue插件化体系的初始化
	app.use(router)
	app.use(createPinia())
	app.mount('#app')
	
8、配置vite.config.ts

import vue from "@vitejs/plugin-vue"
import {defineConfig} from "vite"

export default defineConfig({
		plugins:[vue()]
})

9、src目录下其他分包：assets静态资源、blocks物料、components组件、composable自定义业务层hooks、mocks模拟数据、router路由、stores状态管理、utils工具、views/pages页面资源

10、stores/index.ts

import {createRouter,createWebHistory,RouteRecordRaw} from "vue-router"

import AppView from "../views/AppView.vue"

const routes: RouteRecordRaw[] = [
	{
		path:'/',
		component: AppView,
		children:[
			{
			path:'dataSource'  //不要斜线
			name: 'dataSource',
			component: () => {import }  //动态导入
			}
		]
	}
]

export const router = createRouter({
		history:createWebHistory()
		routes
})


//11、pinia状态管理：两种形式（选项式、组合式，嵌套路由需要在父组件当中加上<RouterView />
export const useCounterStore = defineStore({
	'counter',
	() => {
		const count = ref(0)
		function increment() {
			count.value++
		}
		return {count,increment}
	}
})

//12、根目录下的env.d.ts
/// <reference types="vite/clinet" />
简单来说，它就是 TypeScript 和 Vite 客户端能力之间的 “类型桥梁”，让你的 TS 项目能够 “认识” Vite 的特有功能
```



## Coze使用了解

### 创建智能体

**插件：**自定义专业方面的LLM

**工作流：**每个节点代表处理一个任务，最后按照流程连接起来，Coze创建Agent可以调用工作流

**触发器：**

- 用户使用agent时来定义任务：如定时任务之类的；
- 开发者支持 定时触发和事件触发触发器任务（如插件、工作流、机器人提示），需要给LLM提示词指定需要触发器干什么事情

**知识（文本、表格、照片）：**开发者上传知识库文档并给agent提示词查询知识库，用户使用时就会检索知识库

**记忆（变量、数据库、记忆、长期盒子）**：

- 变量：保存用户名字之类的信息，然后提示agent什么场景使用该变量
- 数据库：设置字段之后，用户输入信息中提取出数据然后存入数据库当中，方便后续使用

### 创建应用

可以创建Web UI 界面去使用工作流

主要分为  **业务逻辑**   和  **用户界面**





### 快慢指针找链表的相遇点和环起点

相遇点：同时从起点出发，快指针走两步，满指针走一步，这次相遇就是相遇点

环起点：慢指针从起点出发，快指针从相遇点出发，都只走一步，下一次相遇就是环的起点

![image-20251203091617297](assets/image-20251203091617297.png)



相遇点二者走的路程：慢指针：L+X         快指针： 2(L+X)  

推导公式：2(L+X)= L+X+nC

​				    L+X = nC  =》 L = nC - X  =》 L = (n-1)C + (C-X) =》 L = (n-1)C + Y  =》**起点到环起点的距离 `L`，等于 “相遇点到环起点的距离 `Y`” 加上 “环长度的 `(n-1)` 倍”**。



```
//两数之和
function twoSum(nums,target){
	const n = nums.length
	let map = new Map()
	
	for(let i=0;i<n;i++){
		let temp = target - nums[i]
		if(map.has(temp)){
			return [map.get(temp),i]
		}else{
			map.set(nums[i],i)
		}
	}
	return []
}
```

```
//合并有序数组
function merge(nums1,nums2){
	const m = nums1.length -1
	const n = nums2.length -1
	const k = m + n -1
	
	while(m >=0 && n>=0){
		if(nums1[m--]>nums2[n--]){
			nums1[k--] = nums1[m--]
		}else{
			nums1[k--] = nums2[n--]
		}
	}
	
	while(j>=0){
		nums1[k--] = nums2[j--]
	}
}
```

```
//寻找最长无重复字串的长度
function findMaxString(s){
	const map = new Map();
	let left = 0;
	let maxLen = 0;
	for(let right=0;right<s.length;right++){
		const char = s[right];
		if(map.has[char] && map.get[char] >= left){
			left = map.get(char) +1 //更新左指针避免重复串
		}
		map.set(char,right)
		maxLen = Math.max(maxLen,right-left+1)
	}
	return maxLen
}
```

```
//三数最接近的和
function threeSumCloset(nums,target){
		nums.sort((a,b) => a-b)
		let closet = Number.MAX_SAFE_INTEGER
		for(let i=0;i<nums.length-2;i++){
			let left = i+1;
			let right = nums.length -1
			
			while(left < right){
				let currentSum = nums[i] + nums[left] + nums[right]
				
				if(currentSum === target){
					return currentSum
				}
				
				if(Math.abs(currentSum - target)< Math.abs(closet - target)){
					closet = currentSum
				}
				
				if(currentSum < target){
					left++
				}else{
					right--
				}
			}
		}
		return closet
}
```

```
//删除字母匹配到对应单词
function deleteToFind(s,dinctionary){
	let result = ''
	for(let word of dinctionary){
		let i=0,
		let j =0,
		while(i < s.length && j < word.length){
			if(s[i] === word[j]){
				j++
			}
			i++
		}
		
		if(j === word.length){
			if(word.length > result.length || (word.length === result.length)&& word < result){
			result =word
			}
		}
	}
	return result
}
```

```
//滑动窗口最大值：单调队列存储索引
function findMax(nums,k){
	let deque = [] //单调队列存储索引
	let result = [] 
	
	for(let i =0;i<nums.length;i++){
		//队列队头元素超出窗口，直接弹出
		if(deque.length && deque[0] < i-k+1){
			deque.shift()
		}
		
		//保证队列中按照从大dao排列，移除小于当前元素的索引
		while(deque.length && nums[deque[deque.length-1 ]]<nums[i]){
			deque.pop()
		}
		
		deque.push(i)
		
		if(i>=k-1){
			result.push(nums[deque[0]])
		}
	}
	return result
}
```

```
//长度最小的子数组
function findMinLen(nums,target){
	let minLen = Infinity
	let n = nums.length
	let currentSum = 0
	let left =0
	
	for(let right =0;right<n;right++){
		currentSum += nums[right]
		
		while(currentSum >= target){
			minLen = Math.min(minLen,right-left+1)
			current -= nums[left]
			left++
		}
	}
	return minLen  === Infinity ? 0 : minLen
}
```

```
//定义二叉树节点
class TreeNode {
	val:number
	left: TreeNode || null
	right: TreeNode || null
	constructor(val,left,right){
		this.val : (val === undefined ? null : val)
		this.left: (left === undefined ? null : left)
		this.right: (right === undefined ? null : right)
	}
}
```

```
//寻找树是否有满足的叶检点和为target
function findSum (root,target){
	if(root.val === null) return false
	if(root.left === null && root.right === null){
		return root.val ===target
	}
	
	let newTargetSum = target - root.val
	reuturn findSum(root.left,newTargetSum) || findSum(root.right,newTargetSum)
}
```

```
//寻找所有叶节点组合数的和
function allSum(root){
	function dfs(node,currentSum){
		currentSum = currentSum *10 + node.val
		
		if(node.left === null && node.right === null){
			return currentSum 
		}
		
		//递归调用左子树和右子树
		const leftSum =  dfs(root.left,currentSum)
		const rightSum = dfs(root.right,currentSum)
		
		return leftSum + rightSum
	}
	
	dfs(root,0)
}
```

二叉树相关问题主要是使用递归的方法，不管是求和还是求长度之类的，定义好方法之后，分别递归左右子树可以求出对应的值



## 数组的所有操作方法

| 方法分类        | 方法名                                 | 作用描述                                                     | 是否修改原数组 | 返回值                                     |
| --------------- | -------------------------------------- | ------------------------------------------------------------ | -------------- | ------------------------------------------ |
| **增删元素**    | `push()`                               | 向数组末尾添加一个 / 多个元素                                | ✅ 是           | 新数组的长度                               |
|                 | `pop()`                                | 删除数组最后一个元素                                         | ✅ 是           | 被删除的元素（数组为空则返回`undefined`）  |
|                 | `unshift()`                            | 向数组开头添加一个 / 多个元素                                | ✅ 是           | 新数组的长度                               |
|                 | `shift()`                              | 删除数组第一个元素                                           | ✅ 是           | 被删除的元素（数组为空则返回`undefined`）  |
|                 | `splice(start, deleteCount, ...items)` | 删除 / 插入 / 替换元素（start：起始索引；deleteCount：删除个数；items：插入元素） | ✅ 是           | 被删除元素组成的数组（无删除则返回空数组） |
| **遍历 / 迭代** | `forEach(callback)`                    | 遍历数组，对每个元素执行回调函数（无返回值）                 | ❌ 否           | `undefined`                                |
|                 | `map(callback)`                        | 遍历数组，返回回调函数处理后的新数组                         | ❌ 否           | 新数组（长度与原数组一致）                 |
|                 | `filter(callback)`                     | 遍历数组，返回满足回调函数条件的元素组成的新数组             | ❌ 否           | 筛选后的新数组                             |
|                 | `reduce(callback, initialValue)`       | 累计遍历数组，返回最终累计值（常用于求和、对象转换）         | ❌ 否           | 最终累计结果                               |
|                 | `reduceRight(callback, initialValue)`  | 从右到左累计遍历数组                                         | ❌ 否           | 最终累计结果                               |
| **查找元素**    | `find(callback)`                       | 查找第一个满足回调函数条件的元素                             | ❌ 否           | 找到的元素（无则返回`undefined`）          |
|                 | `findIndex(callback)`                  | 查找第一个满足回调函数条件的元素的索引                       | ❌ 否           | 找到的索引（无则返回`-1`）                 |
|                 | `indexOf(value)`                       | 查找元素第一次出现的索引（严格相等匹配）                     | ❌ 否           | 索引（无则返回`-1`）                       |
|                 | `lastIndexOf(value)`                   | 查找元素最后一次出现的索引（严格相等匹配）                   | ❌ 否           | 索引（无则返回`-1`）                       |
|                 | `includes(value)`                      | 判断数组是否包含指定元素（严格相等匹配）                     | ❌ 否           | `true`/`false`                             |
| **排序 / 反转** | `sort(compareFn)`                      | 对数组元素排序（默认按字符串 Unicode 码排序，需传 compareFn 自定义规则） | ✅ 是           | 排序后的原数组                             |
|                 | `reverse()`                            | 反转数组元素顺序                                             | ✅ 是           | 反转后的原数组                             |
| **拼接 / 截取** | `concat(arr1, arr2...)`                | 拼接多个数组 / 值，返回新数组                                | ❌ 否           | 拼接后的新数组                             |
|                 | `slice(start, end)`                    | 截取数组（start：起始索引；end：结束索引，不包含 end）       | ❌ 否           | 截取后的新数组（原数组不变）               |
| **转换 / 判断** | `join(separator)`                      | 将数组元素用 separator 连接成字符串（默认用逗号）            | ❌ 否           | 连接后的字符串                             |
|                 | `toString()`                           | 将数组转为字符串（等价于`join()`）                           | ❌ 否           | 转换后的字符串                             |
|                 | `isArray(value)`                       | 判断一个值是否是数组（静态方法，需通过`Array.isArray()`调用） | ❌ 否           | `true`/`false`                             |
| **其他**        | `flat(depth)`                          | 扁平化数组（depth：扁平化深度，默认 1；传`Infinity`可彻底扁平化） | ❌ 否           | 扁平化后的新数组                           |
|                 | `flatMap(callback)`                    | 先`map`处理元素，再`flat(1)`扁平化                           | ❌ 否           | 处理后的新数组                             |
|                 | `fill(value, start, end)`              | 用 value 填充数组（start：起始索引；end：结束索引，不包含 end） | ✅ 是           | 填充后的原数组                             |
|                 | `some(callback)`                       | 判断数组是否有至少一个元素满足回调函数条件                   | ❌ 否           | `true`/`false`                             |
|                 | `every(callback)`                      | 判断数组所有元素是否都满足回调函数条件                       | ❌ 否           | `true`/`false`                             |

## npm  pnpm yarn

三者都是处理依赖管理的包管理工具，主要区别是在磁盘存储占用、解析速度、锁文件机制和生态兼容等方面差异显著

npm、pnpm、yarn 作为 Node.js 主流包管理器，核心目标一致但各有侧重，核心总结如下：

### 1. 核心差异核心

| 维度         | 核心结论                                                     |
| ------------ | ------------------------------------------------------------ |
| 性能 / 速度  | pnpm 最优（硬链接复用 + 并行安装），yarn 次之，npm（v7+）最慢 |
| 磁盘占用     | pnpm 极致节省（全局存储池 + 硬链接），yarn 中等，npm 最高（重复存储依赖） |
| 依赖管理     | npm/yarn 扁平化 hoist（存在幽灵依赖---依赖的依赖会提升到根目录，然后都会进行一个下载，造成资源浪费）每个项目都有同样的node_modules，pnpm 非扁平化（几乎无幽灵依赖---常用在monorepo架构当中，多个项目可以复用node_modules） |
| 锁文件一致性 | yarn/pnpm 更可靠（跨平台一致），npm v7+ 修复后较好、v6 较差  |
| 兼容性       | npm 最好（官方默认），yarn 优秀，pnpm 良好（极少数依赖 hoist 的包需兼容） |

### 2. 适用场景速选

- **npm**：新手入门、老旧项目、对性能无高要求的小型项目（胜在兼容性和官方默认）；
- **yarn**：追求跨平台依赖一致性、中大型项目（非极致性能）、习惯简洁命令的团队；
- **pnpm**：Monorepo 项目、大型 / 多项目开发、对性能 / 磁盘占用敏感的场景（性能和资源利用率天花板）。

### 3. 关键选择原则

- 优先看性能 / 磁盘：选 pnpm；
- 优先看兼容性 / 上手成本：选 npm；
- 优先看跨平台一致性：选 yarn；
- Monorepo 首选：pnpm（优势最突出）。



## watch处理深度监听

在 Vue 中 `watch` 处理深度监听（监听对象 / 数组的深层属性变化），核心是通过配置 `deep: true` 实现，同时 Vue2 和 Vue3 的写法基本一致，仅语法细节略有差异，我分点说明：

###  核心配置：`deep: true`

当监听的目标是**对象 / 数组**（而非简单类型），默认 `watch` 只监听 “引用地址变化”（比如直接替换整个对象），不会监听内部属性变化；添加 `deep: true` 后，会递归监听对象所有层级的属性变化。



###  优化：精准监听对象单个深层属性（避免全量深度监听）

`deep: true` 会递归监听整个对象，若对象层级深、属性多，可能有性能开销；可通过 “字符串路径” 直接监听单个深层属性，无需开启 `deep: true`，更高效：

###  关键注意事项：

- **数组监听**：数组的 `push/pop/splice` 等方法会触发 `watch`（无需 `deep: true`），但通过 “下标修改元素”（如 `arr[0] = 1`），Vue2 需 `deep: true`，Vue3 因 `Proxy` 可直接监听；
- **性能考量**：全量深度监听（`deep: true`）尽量用在小对象上，大对象优先用 “精准监听单个属性”；
- **引用类型注意**：若直接替换整个对象（如 `user.value = { ... }`），即使不开启 `deep: true`，也会触发 `watch`（因为引用地址变了）。

### 替代方案

同时也可以使用computed去监听具体的属性，提取需要深度监听的属性，computed会响应依赖变化

`watchEffect` 是 Vue3 新增的监听方式，无需显式指定监听目标，会自动追踪回调中用到的**所有响应式依赖**（包括深层属性），天然支持深度监听：



## vue进阶

直接解构reactive声明的响应式对象的属性会丢失响应式，需要使用toRefs将每个对象保留响应式

```
const state = reactive({name:'jice',age:20})
const {name , age} = toRefs(state)
```

defineAsyncComponent声明引入组件可以实现资源优化，只在异步组件需要渲染的时候才进行加载，和vite的按需加载类似，配合<Suspense>内置组件可以实现很好的效果，比如异步组件加载期间可以执行一个过度动画之类的一起放在<suspense>当中，可以减少主包体积，提升用户体验



## flex核心进阶

flex：1是三个属性的整合 flex-grow flex-shrink  flex-basis （1 1 0%）自动放大、缩小、自适应占满

**flex-grow:** 仅在父元素声明display:flex生效，用来放大指定元素，平分`总宽度-基准宽度`然后按**比例进行平分**

```css
.flex-container { display: flex; width: 500px; }
.item1 { width: 100px; flex-grow: 1; }
.item2 { width: 200px; flex-grow: 1; }
```

- 基准尺寸（flex-basis:auto）= `width` → 总基准 = 100+200=300px；

- 剩余空间 = 500-300=200px；

- `flex-grow:1` 均分剩余空间 → 各分 100px；

- 最终宽度：item1=100+100=200px，item2=200+100=300px；

  →此时 `width` 决定基准，`flex-grow` 基于 `width` 瓜分剩余空间

  

**flex-shrink:** 缩小元素，

```css
.container { display: flex; width: 300px; }
.item1 { width: 200px; flex-shrink: 1; }
.item2 { width: 200px; flex-shrink: 1; }
```

- 基准尺寸（`flex-basis: auto`）= `width` → 总基准 = 200+200=400px；

- 需缩小的空间 = 400-300=100px；

- 缩小权重 = `width × flex-shrink` → item1: 200×1=200；item2: 200×1=200；

- 每个项目缩小的尺寸 = 总缩小空间 ×（自身权重 / 总权重）→ 100 × (200/400) = 50px；

- 最终宽度：item1=200-50=150px；item2=200-50=150px；

  →此时 `width` 是缩小的基准，`flex-shrink` 基于 `width` 计算缩小比例

  
  
  flex：0 （0 1 auto） 保持自身宽度，不放大抢占空间，空间不足自动缩放
  
  

**flex-basis**:  元素的基准宽度（`关于主轴上的`），如果为auto的时候，值为width，如果设有数值的话，优先级高于`width`



## type of /instance of

### 3. type of核心作用

- 快速判断**基本数据类型**（除 `null`）：比如判断变量是否为 `undefined`、是否为数字 / 字符串；
- 区分 “函数” 和 “普通对象”：`typeof fn === "function"` 是可靠的判断方式。

### 4. 局限性

- 无法区分 `null`、普通对象、数组（均返回 `"object"`）；
- 无法判断引用类型的具体细分（比如分不清数组和普通对象）。



### instance of

- 精准判断**引用类型的具体类型**：比如区分数组和普通对象（`arr instanceof Array`）；
- 验证类实例、自定义构造函数的实例（比如判断是否为 `Promise` 实例：`p instanceof Promise`）；
- 检测原型链继承关系（比如判断是否继承自某个父类）。

#### 4. 局限性

- 无法判断基本数据类型（比如 `123 instanceof Number` → `false`，但 `new Number(123) instanceof Number` → `true`）；
- 原型链被修改后会失效：比如 `Object.setPrototypeOf(arr, null)` 后，`arr instanceof Array` → `false`；
- 跨窗口 / 跨框架时，因构造函数的原型不共享，可能判断错误（比如 iframe 中的数组 `instanceof Array` → `false`）。



## SSE与Fetch

sse是基于http的现代api，可以实现服务端主动向客户端推送数据，但是只支持推送文本数据，如果想要传输图片需要使用base64解码或者使用url进行传递，减少了轮询实现双向通信导致的服务端压力过大的问题

### 面试标准答案：【fetch 流式和 SSE 的核心区别】（完整话术，无任何错误，背完直接答）

> 面试官您好，fetch 流式响应和 SSE 都能实现打字机效果，两者的核心不是「长连接和短连接」的区别，而是「都是 HTTP 长连接，但连接的生命周期和使用方式不同」，具体区别如下：

1. 连接本质：两者都是 HTTP 长连接，在控制台都只会看到 1 个请求连接，区别在于

   生命周期：

   - fetch 流式是「一次性长连接」：服务端推完本次请求的所有数据后，连接自动关闭，是用完即断的任务型连接；
   - SSE 是「永久长连接」：连接建立后会一直保持打开，除非手动关闭，是常驻型连接；

2. 数据推送：

   - fetch 流式只能通过当前连接，推送「本次请求的单次数据流」，多轮请求需要重新发起新的 fetch 请求；
   - SSE 可以通过同一个常驻连接，推送「多次、任意的数据流」，多轮请求无需重新发起，连接复用；

3. 适用场景：

   - 单轮的流式请求（比如单轮 AI 回答、大文件下载），用 fetch 流式，轻量无残留；
   - 持续的流式推送（比如多轮 AI 对话、实时日志），用 SSE，连接复用更高效；

4. 总结：两者都是长连接，核心差异是「连接是否常驻、是否支持复用」。

### sse兼容性以及解决方案

sse兼容现在95％的浏览器，但是低版本浏览器以及ie全系不支持

解决方案：长轮询、

使用成熟的 SSE 兼容库（如 `eventsource-polyfill`），自动为不支持的浏览器模拟 `EventSource` API，底层仍基于长轮询实现：



### sse连接管理和断线重连

1️⃣ 原生eventsource只支持GET请求，大模型的api基本都要post+header鉴权，所以实际项目大多用fetch的ReadableStream手动解析SSE；

2️⃣ 断线重连不能靠原生自动重连，要自己监听error事件判断是网络断开开始服务端主动关闭；

> 原生SSE支持自动重连（retry ms后重连）
>
> - 但是无法区分是可重连还是不可重连错误（401令牌过期、400参数错误会疯狂刷新接口），并且原生不会拦截无效重试。疯狂重连
> - 原生只支持GET，无法自定义method/body，用不了AI接口
> - 重连策略死板：固定间隔充实，容易触发后端限流
> - 原生没有超时设置，接口卡住会无限pending

3️⃣ 重连时要把已接收的lastEventId带上，让服务端从断点续传，不是从头再生成一遍；

> - lastEventId: 单轮会话只存内存简单高效，长对话持久化存储
> - 服务端处理逻辑：读取lasteventID+sessionID，查询会话的所有流式消息，过滤掉 id <= LasteventID的数据，只把大于的推送给前端，全部推送完成后关闭正常流
> - 后端存储方案：对话消息表，每条记录eventid、sessionid、content持久化存储
> - 前端重连处理逻辑：只追加，不清空内容。前端根据eventId去重，跳过已经渲染过的数据
> - 若Last-event-id传错，后端从头推送：前端做id校验，过滤重复块，避免文字重复刷屏

4️⃣ 不要忘记给连接设超时，AI生成长文可能要一两分钟，但连接本身不该超过10s

>**10s 指【握手建立超时】**
>
>发请求后，如果 10 秒内一条数据都没返回，直接断开。代表：网络不通、服务宕机、接口 500、鉴权失败，不需要无限等。
>
>**一两分钟是【流持续空闲最大时长】**
>
>只要成功收到第一条数据，代表连接正常建立；AI 写长文、代码、小说需要持续输出，允许 1~2 分钟内断断续续返回分片，这段时间不会触发超时。



### 流式渲染的性能坑

1️⃣ 大模型每个chunk就几个字，直接每个chunk更新DOM，生成一片2000字的文章要触发几百次重渲染，页面直接卡死

2️⃣ 用raf或定时器做节流，每50-100ms批量合并chunk再更新一次，用户视觉上没区别但性能差几倍

3️⃣ 长文本渲染用虚拟列表或分段加载，不要一次性把几万次塞进dom

4️⃣ markdown渲染，每个chunk都要执行一遍marked.parse(fullText)，计算直接翻倍，攒一批再解析

>内存 buffer 只存储**原始纯文本字符串**，不做任何 MD 转换；
>
>节流周期到达后，**仅执行一次 parse**，转换完整文本为 HTML；
>
>将解析后的 HTML 一次性更新到视图；
>
>极致优化：
>
>- 拆分轻量 / 重型语法：代码块、latex 等高耗解析逻辑，可做防抖；
>- 超大文本拆分解析，使用 Web Worker 离线解析 MD，完全不占用主线程。



### AI输出内容的安全问题

1️⃣ 大模型输出的markdown里坑会带有script标签或者恶意链接，直接用v-html或dangerouslySetInnerHTML渲染就是XSS漏洞

>AI 返回的 Markdown 经过`marked.parse`会转换成完整 HTML，模型不受控，极容易输出：`<script>`、`onclick/onload`事件、`javascript:`伪协议链接、外部恶意图片、iframe、svg 陷阱等

2️⃣ 一定要用DOMPurify做sanitize，或者用纯markdwon渲染器而非直接插入html

> Markdown 解析器（marked/markedjs）**只负责语法转换，不做安全过滤**，不会过滤脚本、事件、伪协议
>
> ##### DOMPurify 到底是什么？
>
> 1. 底层依赖浏览器原生`DOMParser`，把 HTML 字符串解析成标准 DOM 树；
> 2. 内置**安全白名单**：只允许安全标签（p、h1~h6、ul、code、pre、img、a、table 等）；
> 3. 黑名单拦截所有危险内容：
>    - 全部脚本标签：`<script>`、`<iframe>`、`<svg onxxx>`；
>    - 所有事件属性：`onclick/onload/onerror/onmouseover`；
>    - 危险 URL 协议：`javascript:`、`data:`、`vbscript:`；
>    - 动态执行 API：`eval()`、`window.open`等载体；
> 4. 输出清洗后的干净 HTML，不存在可执行 JS，彻底阻断 XSS。

3️⃣ AI生成的内容引用外部图片也可能有问题，图片链接可能失效也可能追踪用户

> - 要么彻底锁死img标签，不准加载
>
> - 要么拦截外部域名、强制图片走本地中转代理
>
>   ```
>   DOMPurify.addHook('afterSanitizeAttributes', function(node) {
>     if (node.tagName === 'IMG') {
>       const src = node.getAttribute('src')
>       // 1. 空链接、非http直接移除图片
>       if (!src || !src.startsWith('http')) {
>         node.removeAttribute('src')
>         node.style.display = 'none'
>         return
>       }
>       // 2. 外部图片中转：统一替换为后端代理接口，避免直接访问第三方域名追踪
>       node.setAttribute('src', `/api/image-proxy?url=${encodeURIComponent(src)}`)
>     }
>   })
>   ```

4️⃣ 代码块里面的内容也要专业，别让用户复制代码时中招

> - 第一层UI提示可能存在风险
> - 第二层代码语法检测：后端前端轻量正则高危关键词
> - 第三层复制内容清洗：前端js读取代码文本，自动过滤高危指令



### 降级方案：长轮询（兼容性+易用性）

“请求挂起 + 持续监听响应+ 接收响应之后重连”

#### 优点

1. **实时性较高**：服务端有数据时能立即推送，延迟远低于短轮询。
2. **兼容性好**：基于标准 HTTP 协议，无需额外插件，支持所有浏览器和网络环境（包括防火墙、代理服务器）。
3. **资源消耗可控**：相比短轮询的 “高频无效请求”，长轮询大幅减少了请求数和网络传输开销；相比 WebSocket，服务端实现更简单。

#### 缺点

1. **服务器资源占用**：大量客户端的长连接会占用服务端的文件描述符，高并发场景下可能引发连接数瓶颈。
2. **头部开销大**：每次请求都携带 HTTP 头部信息，相比 WebSocket 的帧传输，存在额外的带宽消耗。
3. **无法真正双向通信**：本质是客户端主动拉取，服务端被动响应，不支持服务端主动向客户端发起消息（需客户端先发起请求）。



### 错误处理体系

搞清楚有哪些错误（网络超时、限流等）-> 怎么展示给用户(人性化，不要专业术语) -> 错误后恢复逻辑(自动重连，断点续传)  -> 如何引导用户（重试按钮及文案）



### markdown解析处理

![image-20260106211419994](assets/image-20260106211419994.png)

1️⃣ 别等全部输完再渲染，体验太差

> - 等大模型全部输出完，一次性转成HTML渲染，确实不出错，用户体验差
> - 白屏几十秒，用户以为页面挂了，留存率极低

2️⃣ 流式直接插入，标签不闭合乱版

3️⃣ 增量渲染 + 临时缓存

- 核心思路就是增量渲染，尽量不重绘已经渲染好的内容，普通文本和简单标签来一段渲染一段，不影响已有内容
- 遇到代码块、表格这种需要完整结构的，先缓存起来，等完整输出后再一次性渲染，没输出完的时候可以先显示loading加载态
- 技巧：用临时容器存未闭合的标签，每次新内容来了先拼到临时容器里面，能闭合了再移到正式容器里，这样就不会出现标签不闭合导致的乱版

4️⃣ 性能优化

- 代码高亮不能每次全量重绘，输出一个字就重新高亮整个代码块，长代码的时候直接卡成ppt，要做增量高亮，只高亮新增的内容，或者用web worker在后台计算
- 长内容要做虚拟滚动
- 安全问题：markdown转成html的时候需要做DOMPurify的白名单安全过滤，只渲染安全的标签，防止XSS攻击注入



### 双向通信方案

1. **websocket**：基于tcp的双向通信协议，客户端服务端定义事件进行数据的传输，支持二进制，图片，aac数据传输

2. **长轮询/短轮询**

   短轮询：固定事件一直去问：缺点服务器压力大，无效请求多，优势：实现方案简单

   长轮询：一次请求不断开，直到有回复才返回：缺点：实现较复杂，需要服务端支持长轮询，优点：长轮询在服务器压力、资源消耗上远优于短轮询，实时性更接近 SSE/WebSockt



## 虚拟列表

原理、如何实现、不定高如何处理

适用场景：表格、分页，首屏渲染数据量非常大

### 普通dom（table）绘制巨量数据的极限

![image-20260105160350519](assets/image-20260105160350519.png)

### 虚拟表格

可视区域的内容渲染  --- 跟鼠标的滚轮事件绑定

一、单元格定宽定高

```
overscan = 5行 //预渲染的行数,白屏优化
/*当前起始渲染行(scrollTop)
当前结束渲染行(scrollBottom)
在onMounted里面监听滚动盒子的scroll
计算总行数：height*length
计算起始行和结束行
起始行= Math.max(0,Math.floor(scrollTop/ rowHeight) - overscan)
结束行= Math.min(tableData.length-1,Math.floor((scrollTop +tableHeight) + overScan)
*/

```

虚拟表格的性能优化：节流（普通throttle函数和window.requestAnimation）、使用lru缓存优化全量数据存储

```
 class LRUCache {
  constructor(capacity) {
    this.capacity = capacity;
    this.cache = new Map();
  }
  
  get(key) {
    if (!this.cache.has(key)) return undefined;
    const value = this.cache.get(key);
    this.cache.delete(key); // 删除后重新添加，更新为「最新访问」
    this.cache.set(key, value);
    return value;
  }
  
  set(key, value) {
    if (this.cache.has(key)) {
      this.cache.delete(key); // 已存在则删除，重新添加更新优先级
    } else if (this.cache.size >= this.capacity) {
      // 删除「最久未使用」的缓存项（Map的第一个key）
      const firstKey = this.cache.keys().next().value;
      this.cache.delete(firstKey);
    }
    this.cache.set(key, value);
  }
}
```





requestAnimation

```
关于 requestAnimationFrame 的面试题答案要点：

基本概念：

requestAnimationFrame 是浏览器提供的API，用于在下次重绘前执行动画函数
独立于js单线程，采用的是系统时间间隔
它会告诉浏览器需要执行动画，并请求浏览器在下一次重绘前调用指定函数
为了解决定时器时间间隔不稳定的问题
工作原理：

与显示器刷新率同步（通常是60Hz/16.67ms一次）
浏览器会优化多个rAF调用，合并到一次重绘中
页面不可见时会自动暂停，节省资源
性能优势：

与显示器刷新率同步，避免掉帧
智能调度，页面不可见时暂停执行
自动优化，减少不必要的重绘
与setTimeout/setInterval的区别：

setTimeout/setInterval 不考虑浏览器重绘时机
rAF 保证在重绘前执行，更流畅
rAF 由浏览器智能调度，性能更好
在虚拟滚动中使用 requestAnimationFrame 可以确保滚动处理函数在浏览器的重绘周期内执行，避免不必要的重复计算，同时确保流畅的用户体验。
```

setTimeout在宏任务队列，需要进行时间循环，不能确保时间准确



二、定高不定宽

三、定宽不定高

四、宽高都不定



### Canvas绘制表格

没有响应点击事件，使用监测计算位置

```
/*绘制上下文
1、起始绘制
2、移动画笔
3、绘制
4、结束绘制

ctx = canavns

*/
```



canvas只负责渲染 overlay层负责查找计算修改之类的



点击功能是查找算法（hit detection算法）空间索引的映射







## 大文件上传

分片、断点续传、web worker

![exported_image](assets/exported_image.png)

### web worker

**先算文件 hash 定 “身份”，再逐片算 MD5 保 “完整”，前者是前提，后者是细节，两者配合实现 “全局标识 + 局部校验” 的完整体系**。

- [x] 项目中webworker方面

JS 是单线程模型，主线程同时负责「DOM 渲染、用户交互、业务逻辑执行」，如果在主线程处理**大文件分片、MD5 加密这类计算密集型任务**，会阻塞主线程的事件循环，导致页面 UI 卡顿、点击无响应甚至浏览器提示 “页面未响应”，这也是 Web Worker 的核心解决场景。

在大文件上传场景中，我会这样落地 Web Worker：

1. **创建独立的 Worker 线程**：新建`upload.worker.js`文件，把文件分片、MD5 加密的核心逻辑抽离到这个子线程中，主线程仅负责触发上传、接收结果、更新 UI；
2. **线程间通信**：主线程通过`worker.postMessage()`把文件对象、分片大小等参数传给 Worker，Worker 内部通过`self.onmessage`接收参数；加密完成后，Worker 再通过`self.postMessage()`（或`this.postMessage()`，两者等价）把分片后的 MD5 值、文件片段传回主线程；
3. **优化通信效率**：我在 Excel 大文件上传中，优先采用结构化克隆算法传递 File 对象 —— 浏览器对 File/Blob 的结构化克隆仅共享二进制引用，实现零拷贝传输，同时主线程保留文件访问权，可灵活控制上传流程（如取消、断点续传）；仅在处理 500MB 以上超大文件时，才会将 File 转为 ArrayBuffer 并通过可转移对象转移所有权，平衡性能和灵活性
4. **资源回收**：分片加密完成后，主线程调用`worker.terminate()`销毁 Worker，避免内存泄漏。

另外补充：

> Worker 线程中没有`window`对象，`this`和`self`都指向 Worker 的全局对象`DedicatedWorkerGlobalScope`，无法访问 DOM、`alert`等主线程 API，仅能执行纯计算逻辑。
>
> 默认情况下`postMessage`采用「结构化克隆」机制（复制数据），大文件分片的`ArrayBuffer`数据拷贝会产生大量内存开销，因此我会使用**Transferable Objects（可转移对象）** 实现 “零拷贝”—— 把数据的内存所有权从一个线程转移到另一个线程（类似 “剪切粘贴”），避免拷贝损耗，比如传递分片的`ArrayBuffer`时，在`postMessage`第二个参数声明可转移对象：`postMessage({ buffer }, [buffer])`；

![image-20260106214249239](assets/image-20260106214249239.png)



- [x] Web Worker 有哪些使用限制？（必考）

**回答**：

Web Worker 的核心限制集中在「环境隔离」和「通信约束」，主要有 4 点：

1. **DOM 访问限制**：Worker 线程中没有`window`/`document`/`body`等对象，无法操作 DOM、调用`alert`/`confirm`等浏览器 UI API；
2. **通信限制**：只能和创建它的主线程通信，无法跨 Worker 通信（多 Worker 通信需通过主线程中转）；
3. **资源限制**：不能读取本地文件（如`file://`协议），只能加载同源脚本，无法访问`localStorage`（可通过`IndexedDB`实现数据持久化）；
4. **生命周期限制**：Worker 是 “一次性” 的，任务完成后需手动`terminate()`销毁，否则会一直占用线程资源。





- [x] 用了 Transferable Objects “零拷贝” 后，主线程还能访问原数据吗？

**回答**：

不能。「零拷贝」的本质是**内存所有权转移**，数据一旦从主线程转移到 Worker 线程，主线程的原数据会被清空（变为`null`），反之亦然。

比如主线程传递`const buffer = new ArrayBuffer(1024)`给 Worker，并用`postMessage({ buffer }, [buffer])`声明转移后，主线程的`buffer`会变成`null`，只有 Worker 能访问该内存。

如果需要主线程和 Worker 共享数据（而非转移），可以用`SharedArrayBuffer`（需配置跨域头`Cross-Origin-Opener-Policy`和`Cross-Origin-Embedder-Policy`）

### shared worker

不同标签页实现数据内存共享





## ajax和fetch

1. 本质差异：Ajax 是基于 `XMLHttpRequest` 的传统异步请求方案，Fetch 是现代 Promise 式异步请求 API，设计更优雅；
2. 核心优势：Fetch 支持 Promise、链式调用、流式处理，代码更简洁；Ajax 兼容性更好、支持进度监控，使用更成熟；
3. 注意点：二者默认都不携带 Cookie，Fetch 需手动处理 HTTP 错误和响应转换，Ajax 需避免回调地狱；
4. 实际开发：现代项目优先使用 Fetch（或封装后的 Axios，基于 `XMLHttpRequest` 封装，兼顾 Fetch 优雅性和 Ajax 功能性），老旧项目保留 Ajax。





| 对比维度         | Ajax（XMLHttpRequest）                                       | Fetch API                                                    |
| ---------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **API 设计风格** | 传统回调式 API，基于构造函数实例化，接口冗余杂乱（如 `open()`/`send()` 分步调用） | 现代 Promise 式 API，基于函数调用，语法简洁直观，支持链式调用 |
| **回调方式**     | 依赖回调函数（`onreadystatechange`/`onload`），易产生 “回调地狱” | 基于 Promise，支持 `then/catch` 链式调用，也可配合 `async/await` 解决回调地狱 |
| **默认请求配置** | 默认不会携带 Cookie，需手动设置 `xhr.withCredentials = true` | 默认也不携带 Cookie，需通过 `credentials` 配置（`same-origin`/`include`/`omit`） |
| **错误处理机制** | 仅在网络层面错误（如断网）时触发 `onerror`，HTTP 状态码 4xx/5xx 不会视为错误（需手动判断 `status`） | 仅在网络错误或请求被中止时会拒绝 Promise，HTTP 4xx/5xx 仍会视为成功响应（需手动通过 `response.ok` 判断） |
| **响应处理**     | 直接返回响应数据，可按需获取 `responseText`/`responseXML`，无需额外转换 | 返回一个 `Response` 对象，需通过 `json()`/`text()`/`blob()` 等方法手动转换为对应数据格式（异步操作） |
| **中断请求**     | 通过 `xhr.abort()` 方法可直接中断请求，使用简单              | 需要通过 `AbortController` + `AbortSignal` 实现中断，相对复杂 |
| **进度监控**     | 支持通过 `onprogress` 事件监控请求 / 响应进度（如文件上传进度） | 原生不支持进度监控，需借助 `ReadableStream` 实现，复杂度较高 |
| **浏览器兼容性** | 兼容性极佳，支持所有现代浏览器及老旧浏览器（如 IE6+）        | 支持现代浏览器，不兼容 IE 浏览器，需通过 polyfill 兼容老旧环境 |
| **流式处理**     | 不支持流式响应，需等待完整响应后才能处理数据                 | 支持 `ReadableStream` 流式处理，可逐步读取响应数据（适合大文件处理） |

###  五、面试标准答案：axios/fetch/ajax 的区别（完整话术，背完直接答，分点清晰，逻辑满分）

> 面试官您好，axios、fetch、ajax 三者的区别，核心是「层级关系 + 本质 + 特性」的区别，我从三个层面说明：

#### 1. 首先是三者的本质和层级关系（定调）

- AJAX 是一种**异步请求的技术思想**，不是具体的 API；实现这个思想的浏览器原生底层 API 有两个：`XMLHttpRequest(XHR)` 和 `fetch`，这两个 API 是平级的，都是浏览器原生能力。
- 我们常说的「原生 AJAX」就是指直接调用 `XMLHttpRequest`，它是老牌的原生请求 API；fetch 是 ES6 推出的新一代原生请求 API，和 XHR 平级，不是对 AJAX 的封装。
- axios 是一个**基于原生 XHR 封装的第三方请求库**，不是浏览器原生 API，是为了解决原生请求的痛点而生的。

#### 2. 其次是三者的核心特性区别

#### ① 原生 AJAX (XHR)

是最底层的原生 API，兼容性无敌，但 API 设计繁琐，基于回调函数，容易产生回调地狱，没有拦截器、超时处理等实用功能，开发成本极高，实际开发中几乎不会手写原生 XHR。

#### ② 原生 fetch

是新一代原生 API，基于 Promise 设计，解决了回调地狱问题，语法更简洁，还支持流式响应。但它有很多反人类的原生缺陷：不会自动捕获 HTTP 错误、不会自动解析 JSON、没有拦截器、没有超时配置，默认不携带 Cookie，这些缺陷导致原生 fetch 必须封装后才能使用，否则开发体验极差。

#### ③ axios

是基于 XHR 封装的第三方库，整合了原生请求的所有优点，规避了所有缺陷。它基于 Promise 设计，支持 async/await，开箱即用：自动捕获 HTTP 错误、自动解析 JSON、内置请求 / 响应拦截器、超时处理、请求取消、并发请求等所有刚需功能，兼容性好，开发成本极低，是目前前端项目开发的首选。

#### 3. 最后补充一个核心区别（加分项）

- axios 是基于 XHR 封装的，不是基于 fetch；因为 fetch 的底层缺陷无法被完美封装解决，而 XHR 的所有缺陷都能通过封装规避。
- 实际开发中：项目开发用 axios，原生开发 / 轻量场景可以用封装后的 fetch，几乎不会手写原生 XHR。

------

#### ✅ 最后补充：开发中的实际使用场景

1. 企业级项目开发 → **首选 axios**：功能全、稳定、开发效率高；
2. 轻量 H5 / 小程序 / 无构建工具的项目 → 封装后的 fetch：体积小，无需引入第三方库；
3. 兼容 IE 等低版本浏览器 → 必须用 axios（原生 fetch 不兼容 IE）；
4. 处理大文件上传 / 下载等特殊场景 → 可以用 fetch 的流式响应能力。

## 标签页通信

![image-20260106214713754](assets/image-20260106214713754.png)

## 1. LocalStorage + storage 事件

> 原理

- **LocalStorage**：浏览器本地存储（同一域名下的标签页共享数据）；
- **storage 事件**：当某一标签页修改 LocalStorage 时，其他标签页会触发 `storage` 事件，从而感知数据变化。

> 代码示例

```
// 标签页 A（发送数据）
localStorage.setItem('msg', '来自标签页A的消息');

// 标签页 B（接收数据）
window.addEventListener('storage', (e) => {
  if (e.key === 'msg') { // 只监听指定 key 的变化
    console.log('收到新消息：', e.newValue); // e.newValue 是新值，e.oldValue 是旧值
  }
});  
```

> 特点

✅ 优点：API 简单，无需额外依赖；

❌ 缺点：

- 同一标签页修改 LocalStorage 不会触发自身的 `storage` 事件（仅其他标签页触发）；
- 只能传递字符串（复杂数据需 `JSON.stringify`/`JSON.parse`）；
- 数据存在本地，可能被篡改。

## 2. BroadcastChannel

> 原理

浏览器原生 API，专门用于**同一域名下的标签页 /iframe 之间广播通信**，直接发送任意类型数据（无需序列化）。

代码示例

```
// 标签页 A（创建频道并发送消息）
const channel = new BroadcastChannel('my-channel'); // 频道名一致才能通信
channel.postMessage({ type: 'greet', content: 'Hello 其他标签页' });

// 标签页 B（监听同一频道的消息）
const channel = new BroadcastChannel('my-channel');
channel.onmessage = (e) => {
  console.log('收到广播：', e.data); // e.data 是发送的原始数据
};

// 关闭频道（可选）
channel.close();
```

> 特点

✅ 优点：

- 支持任意数据类型（对象、数组等），无需序列化；

- 同一标签页也能接收自己发送的消息；

- API 语义化，专门用于跨页通信；

  

  ❌ 缺点：

- 兼容性：IE/Edge 18 以下不支持；

- 仅同一域名下可用（同源策略）。



### service worker（HTTPS）

多标签页共享，支持离线缓存与离线标签页通信

```
// 先注册 Service Worker 
navigator.serviceWorker.register('sw.js')
.then(registration => { 
// 发送消息给 Service Worker  
registration.active.postMessage('来自标签页的消息');  
// 接收 Service Worker 转发的消息  
navigator.serviceWorker.addEventListener('message', (e) => {  
console.log('收到 Service Worker 广播：', e.data);  }); });
```



### shared worker

![image-20260107132325940](assets/image-20260107132325940.png)



### cookie + 定时读取

document.cookie可以实现获取

```
function setCrossTabCookie(key, value) {
  document.cookie = `${key}=${value}; 
    path=/;          // 必配：让同一域名下所有路径的页面共享
    domain=.example.com;  // 可选：子域名共享（如 a.example.com 和 b.example.com）
    max-age=3600;    // 必配：设置有效期（单位：秒，避免会话结束后失效）
    SameSite=Lax;    // 可选：控制跨站请求携带 Cookie（Lax 是默认值）
  `;
}
```



## 后续安排

一、 JavaScript 核心（面试必问，占比最高）

1. 基础语法

- var/let/const 区别（变量提升、块级作用域、暂时性死区）
- 数据类型（基本类型 vs 复杂类型、typeof/instanceof 检测、类型转换规则）
- 复杂数据结构：Set/Map/WeakSet/WeakMap（特性、区别、原型链指向、使用场景）
- 函数（普通函数 / 箭头函数区别、this 指向、闭包原理及应用、柯里化 / 偏函数）

2. 异步编程

- 事件循环（Event Loop）：浏览器端宏任务 / 微任务分类及执行顺序、Node 端与浏览器端差异
- Promise：状态、链式调用、catch/finally 作用、手写 Promise.all/Promise.race
- async/await：语法糖本质、错误捕获、与 Promise 的关系
- 定时器：setTimeout/setInterval 运行机制、手写 setTimeout 模拟 setInterval（带清除）

3. 原型与继承

- 原型链：prototype/**proto**/constructor 三者关系、原型链顶端（Object.prototype → null）
- 继承方式：原型链继承、构造函数继承、组合继承、寄生组合继承、class 继承
- class 相关：constructor 作用、static 静态方法、private/public 修饰符

4. 高频手写题

- 防抖 / 节流函数（原理 + 应用场景）
- 深拷贝（基础版 + 处理循环引用版）
- 简易 Axios 封装（请求 / 响应拦截器）
- 发布订阅模式
- 数组扁平化 / 去重 / 排序（reduce 实现）

二、 Vue3 核心（匹配你的技术栈，重点深挖）

1. 生命周期

- Vue3 生命周期钩子（setup 替代 beforeCreate/created、onMounted 等）
- 父子组件生命周期执行顺序（父 created → 子 created → 子 mounted → 父 mounted）
- 各生命周期使用场景（created 初始化数据、mounted 操作 DOM、onUnmounted 清除定时器）
- nextTick 原理及使用（DOM 更新后执行回调、基于微任务）

2. 响应式原理

- Proxy 对比 Object.defineProperty 的优势
- 依赖收集与触发更新的流程（targetMap → depsMap → dep → watcher）
- 手写简易 reactive/effect 函数
- ref/reactive 的区别、toRef/toRefs 的作用

3. 组件与通信

- 组件通信方式：Props/Emits、Provide/Inject、Pinia/Vuex、事件总线、v-model 自定义
- 组件复用：Mixin 缺点、Composition API 优势、自定义 Hook 封装
- 组件传值注意事项：单向数据流、深层监听

4. 路由与状态管理

- Vue Router：Hash/History 模式原理、路由守卫（全局 / 路由 / 组件）、动态路由
- Pinia：核心概念（state/getters/actions）、与 Vuex 的区别、持久化存储

三、 网络与浏览器（基础 + 高频场景）

1. HTTP 相关

- HTTP 1.1/2/3 特性对比（长连接、多路复用、二进制帧）
- 常见状态码：200/301/302/304/401/403/404/500/502（含义 + 使用场景）
- 强缓存（Cache-Control/Expires）vs 协商缓存（ETag/Last-Modified）、304 原理
- 跨域问题：同源策略、解决方法（CORS、Nginx 反向代理、JSONP）

2. 实时通信

- WebSocket 原理：TCP 全双工通信、握手过程、与 HTTP 的区别
- SSE 特性：基于 HTTP、单向通信、自动重连、与 WebSocket 的适用场景对比
- 长轮询原理及缺点（服务端压力大）

3. 浏览器存储

- localStorage/sessionStorage/cookie 区别（存储大小、有效期、作用域、自动携带）
- 存储上限（localStorage 5MB、cookie 4KB）、过期策略、定时清除实现
- 跨标签页通信：BroadcastChannel、localStorage storage 事件、SharedWorker

4. 浏览器渲染

- 渲染流程：解析 HTML → DOM 树 → CSSOM 树 → 渲染树 → 布局 → 绘制 → 合成
- 回流 / 重绘：触发条件、性能优化（减少 DOM 操作、使用 DocumentFragment）
- 异步渲染：requestAnimationFrame 作用
- Web Worker/Service Worker：区别、使用场景（Web Worker 处理大数据计算、Service Worker 实现离线缓存）

四、 工程化（体现项目能力，加分项）

1. 模块规范

- ESM/CommonJS/UMD 区别（导出导入语法、运行时 / 编译时加载、浏览器兼容性）
- 模块打包：Rollup 配置（入口 / 出口、格式、Tree Shaking）、与 Webpack 的区别
- 包管理器：NPM/Yarn/PNPM 区别（幽灵依赖、硬链接 / 软链接、安装速度）

2. 代码规范与质量

- ESLint/Prettier 配置（extends/rules 作用、自动修复）
- Husky + lint-staged + Commitlint：提交前代码校验、规范 commit 信息
- 单元测试基础：Jest 语法、Vue 组件测试思路（了解即可）

3. 构建工具

- Vite 核心原理：基于 ES Module、依赖预构建（esbuild）、热更新（HMR）
- Vite vs Webpack 对比：启动速度、热更新效率、适用场景

五、 CSS 进阶（高频考点）

1. 布局相关

- Flex 布局：flex 1 的含义（flex-grow:1; flex-shrink:1; flex-basis:0%）、flex-basis 与 width 优先级
- 响应式布局：媒体查询、rem/vw/vh 单位、移动端适配方案（viewport 配置）
- 容器查询：ResizeObserver API 原理及使用（替代 window.resize）

2. 预处理器 / 框架

- Less/Sass 基础：变量、混合宏、嵌套、@import 与 @use 区别
- CSS Modules：作用域隔离、命名规则

3. 性能优化

- CSS 性能优化：减少选择器嵌套、避免通配符、使用 contain 属性

六、 项目相关（面试重点，必须吃透）

1. 大文件上传

- 分片上传：File.slice 切割、MD5 生成文件唯一标识、断点续传逻辑
- 并发控制：Promise.allSettled 限制同时上传切片数量
- 进度展示：切片上传进度计算、合并总进度

2. 流式数据处理

- SSE/WebSocket 接收流式数据：onmessage 事件处理、实时渲染
- Markdown 流式解析：markdown-it 库使用、自定义渲染器处理图片 / 代码块
- 中断请求：AbortController API 实现请求取消

3. 工具库开发

- Monorepo 架构：packages/apps 目录划分、PNPM workspace 配置
- 工具函数封装：localStorage 过期处理、请求工具、防抖节流 Hook
- NPM 包发布：package.json 配置（main/module/types）、.npmignore 文件

4. 性能优化（项目亮点）

- 前端性能优化：虚拟列表、懒加载、图片压缩、缓存策略
- Vue 性能优化：v-show/v-if 选择、v-for 加 key、computed 缓存、避免过度响应式

七、 面试软技能

- STAR 法则：整理项目经历（场景 - 任务 - 行动 - 结果）、量化成果（如 “响应速度提升 50%”）
- 回答结构：结论→原因→方案→避坑点
- 反问环节：团队技术栈、实习生培养计划、核心项目方向

## vue源码解析

###  一、vue和React设计哲学

①Vue 基于**全局化的自动响应式机制**实现视图更新，React 则是基于**手动触发的细粒度更新模式**完成渲染刷新；

②二者核心优势各有侧重且十分鲜明：Vue 凭借自动响应式让前端开发的**心智负担大幅降低**，开发流程更简洁、上手更轻松，极大简化前端开发落地效率；React 依托手动可控的更新逻辑，让复杂业务场景下的前端开发具备更强的**可维护性与可把控性**，更适配大型复杂项目。

③Vue 深度依赖自身的响应式更新体系，在部分场景下对原生 JS 的兼容性适配成本略高；而 React 本身无框架黑魔法、贴合原生 JS 的设计特性，天然具备更优异的**跨端兼容能力**，与原生 JS 生态的适配性、兼容性表现更出色。

④两者无优劣之分，只是设计哲学的取舍：Vue 用**「框架的封装便捷」**换开发效率，React 用**「手动的可控约束」**换工程化能力，均是当前前端领域的最优解之一。



### 二、composition api

options api 代码书写逻辑简单，适合小型项目，但是由于逻辑分散，大型项目不好维护

composition api优势：

逻辑集中、类型推导优秀、逻辑抽离更好



### 三、响应式数据声明（ref、reactive）

**ref**：声明简单数据类型，在script标签中使用需要.value进行操作，ref将声明的简单数据类型包装成了一个特殊的对象

**reactive**:声明复杂数据类型，无需.value操作

**computed**：计算属性，基于已有响应式值进行计算，默认有缓存能力，只读属性

**watch**：侦听变化，传入数据源(count),回调函数（newCount,oldCount）=>{操作}

**immediate: true** 默认关闭， 初始化时执行一次

**deep：true**，默认关闭，开启之后支持引用数据类型的属性

问题：如果代码中多个ref和多个watch，如何让代码组织更清晰呢？

```
可以使用reactive合并ref声明的简单数据类型，watch的话可以使用数组监听多个数据或者监听响应式对象里面的属性，需要开启deep：true属性进行深层监听
```



在庞大项目组织结构里面，composition api会给项目结构设计带来怎么样的帮助？

```
Composition API 为庞大 Vue 项目的结构设计带来本质提升：它将代码组织逻辑从「按语法选项拆分」升级为「按业务逻辑聚合」，彻底解决了逻辑碎片化、复用困难、组件臃肿的核心痛点；通过自定义 Hooks 实现无耦合的逻辑复用，形成标准化的项目架构；完美契合 TS 实现强类型约束，保障项目健壮性；同时统一了编码规范，降低团队协作成本。最终让庞大的 Vue 项目具备极高的可维护性、可扩展性、复用性，是大型 Vue 项目的最优架构选择
```



### 四、响应式丢失

1、解构reactive对象会丢失响应式（toRefs可以解构保持响应式），推荐使用修改原始属性的值来进行对响应式对象属性的修改

2、新增属性保持响应式？  vue3可以，vue2不可以

![image-20260103194134266](assets/image-20260103194134266.png)

其他使用旧的state的值的地方不会保持响应式，而是会保持旧的值

或者使用`Object.assign`合并多个属性

```
Object.assign(state.value,{
		count:state.value.count+1
	})
//和直接更新原来对象的属性没有区别，但是可以修改多个属性以及合并
//浅拷贝：会修改原来对象的引用地址，  深拷贝：生成新的地址空间，不会影响原有数据
```

3、数组索引赋值问题：vue3保持响应式，vue2不支持根据nums[i]直接修改赋值

解决方案：使用splice方法进行数组修改，语义化更强

4、ref和reactive混用：🈲嵌套使用

```
const count = ref(0)
const state = reactive({count})  //🈲，不建议这样写
```

5、直接修改props🈲，用一个新变量来接收or  emit通知父组件修改



### **五、响应式底层原理--Proxy&defineProperty**---都只是信息源告诉我们`数据变化`，但是通知谁去更新需要`依赖追踪（视图变化）`

①**为什么需要响应式？**

在传统原生 JS 开发模式中，修改数据后需要**手动频繁操作 DOM** 来完成页面更新；如果一处数据变更需要同步更新页面多个位置，就会出现大量重复的 DOM 操作代码，不仅冗余繁琐，还容易出现数据与视图不一致的问题。而 Vue 的响应式系统，能**自动监听数据的变化，并在数据更新时，统一完成视图的联动刷新**，无需开发者手动操作 DOM。这一机制极大降低了开发的心智负担，让开发者可以脱离 DOM 操作的细节，将核心精力聚焦于业务逻辑的开发与维护上。

**②如何做到响应式**

**proxy（拦截行为）**：将每个对象new成proxy对象，然后通过handler对象里面的get（读取代理对象时触发）和set方法（在我们写入或修改代理对象的时候触发）

![image-20260103200423462](assets/image-20260103200423462.png)

### **简单实现Proxy**（硬编码，无依赖追踪收集）

```

<p id="count">0</p>
<button id="icon">+1</button>
<script>
const state = {count: 0}
const p = new Proxy(state,{
	get(t,k){
		console.log('get',t,k)
		return t[k]
	},
	set(t,k,v){
		console.log('set',k,v)
		t[k] = v
		document.getElementById('count').innerText = v
		return true
	}
})
document.getElementById("icon").onclick = function(){
	p.count++
}
</script>
```



**Object.defineProperty（拦截属性）:**不是代理对象，而是在原始对象上循环遍历属性一个一个设置getter和setter，这就是为什么在新增属性时需要使用vue.set()进行写入，vue2也对数组原始操作如slice、push这些方法做了重写

```
this.$set(target,属性名/数组索引,value)  //支持新增对象属性和通过数组下标修改元素
```



### 依赖收集（通知哪些视图更新 track/trigger）

**proxy只能告诉我们“数据变了”，不知道哪些地方用到了**

一、**track：收集依赖**，某个副作用执行期间，如果读取了响应式数据，就把这个副作用记录到依赖集合里 ----谁在用我我就把谁记下来

![image-20260103203504901](assets/image-20260103203504901.png)



二、trigger：触发更新，一旦数据被修改，就去找他的依赖集合，把里面的副作用函数全部重新执行----我如果变化了，就通过所有用我的人重新执行

![image-20260103203716269](assets/image-20260103203716269.png)

### 完整proxy实现响应式（proxy,get,track,set,trigger）

#### ✔️ 1. 为什么用两个哈希表？

因为响应式的依赖关系是 **「对象 → 属性 → 副作用函数」** 的三层结构，单层哈希表存不下这种精准的映射关系，必须用「双层嵌套」。

```
targetMap = {
  state: Map = {  //depsMap
    'count': Set { 渲染count的函数 },
    'name': Set { 渲染name的函数 }
  },
  person:Map = {
  	'age': Set{ 渲染age的函数},
  	'gender': Set{ 渲染gender的函数}
  }
}
```

#### ✔️ 2. 两个哈希表的分工（固定搭配）

- 第一层 `WeakMap(targetMap)`：**区分不同的对象** → key = 目标对象 (t)，value = 第二层 Map；
- 第二层 `Map(depsMap)`：**区分同一个对象的不同属性** → key = 属性名 (k)，value = 存副作用函数的 Set；

#### ✔️ 3. track 和 trigger 的核心逻辑

- **track 收集依赖**：顺着「对象→属性」的路径，把副作用函数存到对应的 Set 里；
- **trigger 触发更新**：顺着「对象→属性」的路径，把 Set 里的副作用函数取出来执行；

#### ✔️ 4. 你的代码里的对应关系

你 Proxy 里的 `t` 就是 WeakMap 的 key，`k` 就是第二层 Map 的 key，`activeEffect` 就是 Set 里的内容，**所有参数都能对应上**

```
<p id="count">0</p>
<button id="icon">+1</button>
<script>
// 1. 全局变量：存「当前正在执行的副作用函数」
let activeEffect = null;
// 2. 依赖集合：key是数据对象，value是「属性名: 副作用函数数组」
const targetMap = new WeakMap();


// --------------- track：收集依赖 ---------------
function track(target, key) {
  // 只有当前有activeEffect（有副作用函数在执行），才收集
  if (!activeEffect) return;
  
  ----------------第一层：targetMap（weakMap）键：对象，值：属性集合---------------
  // 给当前数据对象（target）建一个「属性-副作用」的映射
  let depsMap = targetMap.get(target)
  if(!depsMap){
  	depsMap = new Map()
  	targetMap.set(target,depsMap)
  }
  
  ----------------第二层：depsMap（Map）键：属性，值：使用该属性的函数的set集合--------------------
  // 给当前属性（key）建一个「副作用函数数组」
 let deps = depsMap.get(key)
 if(!deps){
 	deps = new Set()
 	depsMap.set(key,deps)
 }
  
  ----------------第三层：值：使用该属性的函数的set集合--------------
  // 把当前副作用函数（activeEffect）加入依赖集合
  deps.add(activeEffect);
}


// --------------- trigger：触发更新 ---------------
function trigger(target, key) {
  // 从依赖集合里，找到当前数据+属性对应的副作用函数
  const depsMap = targetMap.get(target)
  if(!depsMap)return;
  const deps = depsMap.get(key)
  if(!deps)return;
  
  // 执行所有依赖的副作用函数（比如重新渲染页面）
  deps.forEach(effect => effect())
}


// --------------- effect：包装副作用函数 ---------------
function effect(fn) {
  // 把副作用函数设为「当前活跃的effect」
  activeEffect = fn;
  // 执行副作用函数（执行时会读取数据，触发track收集依赖）
  fn();
  // 执行完后清空，避免后续乱收集
  activeEffect = null;
}


// --------------- 你的Proxy代码，加上track/trigger ---------------
const state = { count: 0 };
const p = new Proxy(state, {
  get(t, k) {
    // 读取数据时，收集依赖（记录“谁用了这个数据”）
    track(t, k); 
    return t[k];
  },
  set(t, k, v) {
    t[k] = v;
    // 修改数据时，触发更新（通知“用了这个数据的人”更新）
    trigger(t, k); 
    return true;
  }
});


// --------------- 用effect包装“渲染页面”的副作用函数 ---------------
effect(() => {
  // 这个函数的作用是“渲染页面”，会读取p.count（触发get→track）
  document.getElementById('count').innerText = p.count;
});


// --------------- 点击事件 ---------------
document.getElementById("icon").onclick = function() {
  p.count++; // 修改数据→触发set→trigger→执行effect里的渲染函数→页面更新
};
</script>
```

### vue响应式更新vs React状态更新

①vue和React都默认有批处理机制 【攒一堆修改，一次性渲染】

vue是通过将所有任务放在同一个微任务队列当中，当多次操作数据结束之后，一次性清空微任务队列完成数据更新

强制单独处理：nextTick()  在渲染完再执行后续函数

```
const handleClick = async () => {
  // 1. 连续修改3次状态，Vue把更新任务放进微任务队列
  state.count++;
  state.name = "张三";
  state.age = 20;

  // 2. 调用nextTick，把回调放进同一个微任务队列
  await nextTick();

  // 3. 这个回调会在「所有状态更新完成、视图渲染后」执行
  console.log("此时页面已经是最新的了");
};
```



 react是通过render+commit事件批量处理的，可以使用fluhsync分开处理每一次渲染

```
render 阶段：收集所有状态修改，批量计算「哪些组件需要更新」（生成新的虚拟 DOM）；
commit 阶段：一次性把所有需要更新的组件，渲染到真实 DOM 上。
```

强制单独处理：flushSync  不批量处理修改任务，一次一次更新渲染

```
import { flushSync } from 'react-dom';

const handleClick = () => {
  // 这两次修改会被批处理，合并渲染1次
  setCount(c => c+1);
  setName("张三");

  // 这个修改会「立即单独渲染」，不参与批处理
  flushSync(() => {
    setAge(20);
  });
};
```





②列表局部更新

vue精准更新:细粒度更新

react没有启用`react.memo`,全部兄弟子组件都会更新改变



### vue生命周期

创建 -  组件在内存里被初始化的过程 

 挂载-  组件第一次被渲染到真实dom上的过程(订阅/计时器/第三方库初始化+清理)

onBeforeMount  (先父后子)

onMounted （先子后父）客户端首屏获取数据，非常普遍和常见，任何关于浏览器和window操作的方法不能放在这里，服务端渲染不会执行这个钩子

![image-20260103214958004](assets/image-20260103214958004.png)

更新- 组件的响应式数据被更新需要重新渲染

组件的更新是独立的，子组件的更新不会影响到父组件

onBeforeUpdate  

onUpdated

卸载- 组件被移除掉

onBeforeUnmout

onUnmounted



### vue的副作用清理逻辑

定时器没关、订阅没关、请求没收,下一次副作用执行之后上一次副作用的定时器还在启动，占用后台资源

副作用函数：watch、watchEffect，它们监听的数据变化之后会执行一次

什么时候清理（谁创建、谁清理）？  1、副作用函数准备下一次重新执行之前；2、组件被卸载

场景1、定时器的清理：onInvalidate

```
<script setup>
import { watchEffect } from 'vue';

// 用onInvalidate清理定时器
watchEffect((onInvalidate) => { // 注意：onInvalidate是watchEffect回调的参数
  // 1. 开启定时器
  const timer = setInterval(() => {
    console.log('定时器运行中...');
  }, 1000);

  // 2. 注册清理函数：当副作用被清理时，执行这个函数
  onInvalidate(() => {
    clearInterval(timer); // 关定时器
    console.log('定时器已被清理');
  });
});
</script>
```

场景2、竞态请求：同时发送多个请求，如果没有关闭，那么一个较早的、响应较慢的请求将会覆盖掉最新的请求，导致数据错误

可以在watch的onInvalidate当中执行abortController.abort()停止请求：在下一次请求之前关掉上一次请求

```
<template>
  <div>
    <input v-model="keyword" placeholder="输入关键词搜索" />
    <div v-if="loading">加载中...</div>
    <div v-else>{{ result }}</div>
  </div>
</template>

<script setup>
import { ref, watch } from 'vue'
import axios from 'axios'

// 搜索关键词
const keyword = ref('')
// 接口返回结果
const result = ref('')
// 加载状态
const loading = ref(false)

// 定义AbortController实例（用于控制请求）
let abortController = null

// 监听关键词变化，发起请求
watch(
  keyword,
  async (newKeyword) => {
    // 1. 清空旧结果，显示加载中
    result.value = ''
    loading.value = true

    // 2. 终止上一次未完成的请求
    if (abortController) {
      abortController.abort() // 终止旧请求
    }

    // 3. 创建新的AbortController
    abortController = new AbortController()
    // 获取signal（用于关联请求）
    const { signal } = abortController

    try {
      // 4. 发起请求（携带signal）
      const res = await axios.get('https://api.example.com/search', {
        params: { keyword: newKeyword },
        signal // 关键：将signal绑定到请求
      })

      // 5. 请求成功，更新结果
      result.value = res.data
    } catch (err) {
      // 捕获“请求被终止”的错误（正常流程，无需提示）
      if (err.name !== 'CanceledError') {
        console.error('请求失败：', err)
      }
    } finally {
      // 6. 无论成功/失败，关闭加载状态
      loading.value = false
    }
  },
  {
    // 7. 在watch的onInvalidate中，终止请求（兜底）
    onInvalidate() {
      if (abortController) {
        abortController.abort()
      }
    }
  }
)
</script>
```



### vue常见指令

v-if：元素彻底消失，出现  v-show：元素通过display隐藏

v-bind：为dom节点绑定属性，可以实现动态改变样式 或者行为变化 （简写 v-bind:herf =>  :herf）

 v-model：双向绑定功能,表单输入和应用状态同步

```
<input v-model="text">  文本输入
<input v-model="checked"> 多选
<select v-model="selected"> 下拉
```

实战要点：

```
优先使用v-if控制复杂组件 -- 可以完全销毁组件
v-for 必须加key
v-bind灵活绑定各类属性
v-model简化表单处理
```

vue3的双向绑定流程：

> Proxy代理数据 → 读取数据触发track收集依赖 → 修改数据触发trigger更新视图 → v-model同步视图输入回数据





### 组件通信

父传子：props   @事件(v-on)

子传父：emit，只能读不能改（单向数据流）,使用副本或计算属性（computed）

v-model:不仅能用到输入框同步数据，而且可以在自定义组件当中使用（v-model="value"  === @input + :value）



跨层级通信：响应式更新  可以来用做全局**主题样式切换、全局被指、国际化语言、用户状态**

provide（键值）

inject（键）

```
const user = inject('user', {name:'Guest'}) //后面的name是user为undefined的时候指定的值
```



### 路由传参

动态参数（资源标识）：/user/:id   必传项   可以通过$route.params.id获取到

查询字符串（查询条件）： /search?q=vue&page=1  可以通过$route.query.q获取到，不是必填项



### 路由守卫

全局守卫：beforeEach全局前置,做身份验证

```
router.beforeEach((to,from,next) =>{
	if(to.meta.requiresAuth){
		next('/login')
	}else{
		next()
	}
})
```

```
//配置元信息
const routes = [
{
	path:"/admin",
	component:Admin,
	meta:{	requiresAuth:true , title:"管理首页" }
}
]

//可以通过to.meta.title获取 、route.meta获取
```



路由独享守卫：beforeEnter独享,针对某个路由做定制化，比如数据预加载

```
{
	path:'/admin',
	component:Admin,
	beforeEnter:(to,from,next) =>{
	if(isAdmin()) next()
	else next('/login')
	}
}
```



组件内守卫：进入前，更新前，离开前

```
//离开前:适合用在编辑页，防止用户丢失内容
beforeRouteLeave(to,from,next){
	if(hasUnsavedChanges()){
		confirm('有未保存内容，确认离开此页面吗？')
	}
	next()
}
```

执行顺序：从宏观到微观



router负责路由跳转，代表整个路由对象 -----------  route负责获取路由中的信息管理



### pinia

一、创建使用pinia          defineStore('name',{state: {}, actions:{}})

二、组件使用                    导入，调用const  counterStore =  useCouterStore()

**响应式的**



三、state：全局响应式     getter：类似computed，有缓存特性   actions ：业务逻辑，支持异步操作

四、响应式丢失：🈲解构赋值会丢失响应式变量，解构出来只是一个固定的值，与响应式系统没有关系了

```
const { count } = storeToRefs(store)  //解决方法
```

🈲解构action方法，用法：store.action



五、最小可变点原则

🈲![image-20260104172343544](assets/image-20260104172343544.png)🈲

应该精准更新某个字段

### 自定义指令

```
全局注册：app.directive()
局部注册：derectives选项
局部语法：包含钩子函数对象

app.directive('focus',{
	mouted(el){
		el.focus()
	}
})

app.directive('color',{
	mouted(el,binding){
		el.sytle.color = binding.value
	},
	updated(el,binding){
		el.style.color = binding.value
	}
})

app.directive("debounce",() => {
	mounted(el,binding){
		const delay = binding.value || 1000 //默认1000ms
		let timer = null
		el.addeventListener('click',()=>{
			if(timer) clearTimeout(timer)
			timer = setTimeout(() =>{
				binding.arg && binding.arg()
			},delay)
		})
	}
})

<div v-color="binding" />
```

### 模板编译

解析-》优化-》生成

解析：html转化成ast语法树，<template>中的元素转化成ast树

优化：静态节点标记（纯文本、不变得内容） -》打标签 -》跳过静态节点

生成：ast转函数（render函数） -》 性能优化



## 缓存策略

概念、资源存储、特定场景（比如刷新之类的）

### bfcache

浏览器自动开始，为同一域名的前进后退网页资源做一个“全量缓存”，但是可能会出现一些问题，比如安全问题（用户输入框中的密码也缓存了）、数据不同步的问题。。。开发者需要通过监听`pageShow和pageHide`做一些清理敏感数据的措施，以及在用户回退的时候请求新的数据

bfcache 的核心是 **“缓存页面完整状态，加速前进 / 后退切换”**，开发者可以：

1. 利用它提升页面切换体验；
2. 通过 `pageshow/pagehide` 事件处理缓存恢复的逻辑；
3. 避免操作导致 bfcache 失效，最大化发挥其性能优势。



### Memory Cache

1. **Memory Cache 核心价值**：浏览器在内存中临时缓存资源，是最快的缓存方式，优先于磁盘缓存；
2. **管理规则**：浏览器自动缓存、按 LRU 淘汰、标签页关闭即清空；
3. **验证方法**：Chrome Network 面板看 `from memory cache`；
4. **优化技巧**：减小资源体积、复用资源、合理设置缓存头，让高频资源留在内存中；
5. **关键认知**：开发者无法手动操作 Memory Cache，只能通过优化资源让浏览器更高效地利用它。

简单记：Memory Cache 是浏览器给前端的 “免费性能红利”，你只需要做好资源优化，就能让它帮你提升页面加载速度。









## 极端性能优化

1、长任务-大于50ms

performanceObeserver  api

2、通过fps进行排查

1000/60(帧) === 16.67ms

3、内存泄露排查





任务调度器：将任务放入任务队列，然后根据优先级执行

`requestIdleCallback`在js线程空闲的时候一直执行

`requestAnimation`在下一帧渲染之前执行，可以解决宏任务setTimeout定时不准的问题



## 工程化

### package.json中字段解读

**锁定环境版本：**

1. engine:   锁定环境版本，不主动校验，只是在npm i 的时候检测到不满足的版本进行警告，依然会继续安装

   ```
   {
     "engines": {
       "node": ">=18.0.0 <20.0.0",  // 要求 Node.js 版本在 18.x 到 20.x 之间（不含 20.x）
       "pnpm": ">=8.0.0",            // 要求 pnpm 版本 ≥8.0.0
       "npm": ">=9.0.0",             // 要求 npm 版本 ≥9.0.0
       "yarn": "^1.22.0"             // 要求 yarn 版本 1.22.x（^ 表示小版本兼容）
     }
   }
   ```

   如果需要强制检测警告更新,需要在
   ```
   .npmrc \ .yarnrc \ .pnpmrc里面配置
   engines-strict = true
   ```

2、配合nvmrc写入指定版本，然后通过nvm use使用指定版本

3、使用docker容器固定镜像，保证从操作系统层面上版本一致



## 面试题汇总

1. > vue hook   （vs）   utils  （vs ）react hook

依赖vue生态，生命周期，响应式数据更新，聚焦业务

utils 纯函数  只关注输入输出，聚焦于某一特有功能

react  的hook是一些内置函数，需要在特定场景调用，比如useEffect这些

> 2、倒计时(setTimeout实现setInterval)

```
function mySetInterval(fn, delay) {
  let timerId; // 用于存储 setTimeout 的 ID
  function loop() {
    fn();
    // 每次 setTimeout 都会返回新的 ID，更新到 timerId
    timerId = setTimeout(loop, delay);
  }
  loop(); // 启动
  // 返回“清除函数”，模拟 clearInterval
  return function clearMyInterval() {
    clearTimeout(timerId);
  };
}

// 使用示例：
const clear = mySetInterval(() => {
  console.log('自定义定时器执行了');
}, 1000);

// 3秒后清除定时器
setTimeout(clear, 3000);
```

> `for...in` 是 “遍历名字”（属性名 / 索引），`for...of` 是 “遍历值”（元素值）



> - [ ] 3.编译原理

首先是将代码转换成 AST抽象语法树，抽象语法树描述的是代码的属性描述，然后根据规则将抽象语法树进行变化，再通过抽象语法树生成最新的代码 （先通过tokenizer分词 -> 然后转化成抽象语法树 -> 进行特定转化生成新的ast树 -> 生成对应代码）

主要编译拓展分为plugin和preset,  plugin是针对特定功能，preset是对某一模块（多个plugin的集合）

“学习是一个将任务不断拆分的过程”

```
@babel/parser  //将代码转成ast树
@babel/traverse  //将ast转换成新ast
@babel/generator  //将新ast 转换成目标代码
```

```
@babel/traverse  //将ast转换成新ast ,访问者模式在原来ast树上进行操作
const visitor ={
	VariableDeclaration(path){
		cosole.log(path.node.kind)
		if(path.node.kind === 'const'){
			path.node.kind = 'var'
		}
	}
}
traverse(ast,visitor)

@babel/traverse  //将ast转换成新ast
const generator = require('@babel/traverse')
const newCode = egnerator(newAst)

```

## 编译器原理详解

### babel 基础

环境搭建

@babel/core@7.28.3
@babel/cli@7.28.3


插件化方式管理对于语法转换支持
预设来管理插件集合，预设就是一组插件的集合

### babel 执行过程进阶

1. @babel/parser 将源代码转为 ast
2. @babel/traverse 遍历 ast 节点，进行转换，转换为目标代码的 ast
    访问者模式，逐个访问 ast 节点，今儿进行操作
    状态机模式
3. @babel/generator 将 ast 转为目标代码


### 自定义插件

储备以上关于 babel 执行过程的知识点

### 编译原理

实现公式编译执行器，大的重难点
1. 编译原理
    1. 词法分析
    2. 语法分析
    3. 访问者模式解决执行问题
2. 访问者模式
3. 策略模式
4. 插件化机制

"学习源码是为了写更好的代码"

## 安全攻击

### 1、xss（通过输入框拼接违法代码注入盗取cookie模拟用户操作）

反射型：构造特殊url，里面有恶意代码

存储型：将恶意代码存到数据库中，用户执行解析后恶意代码也执行

DOM型：由浏览器完成，构造特殊URL



来源：攻击者提交恶意代码、浏览器执行恶意代码

解决：  过滤恶意代码    

### 2、csrf

引导受害者进入三方网站，在三方网站向被攻击网站发送跨域请求

解决：同源检测

### 3、sql注入

找数据库版本破解



## 0117

vue2的`Object.defineProperty`对已存在的每个属性使用set/get劫持监听，不能对新增属性监听并且不能无法监听数组长度/索引值修改（没有get/set），所以对数组的方法进行了重写

总结：

Vue2 的 “劫持” 不是 “全量劫持”，而是**针对性拦截**：对对象劫持「已有属性的 get/set」，对数组劫持「7 个可变方法的调用」；

所谓 “无法劫持内部属性”，本质是 `Object.defineProperty` 的天然限制（只能拦截已有属性的读写，无法拦截新增 / 删除 / 数组索引）；

Vue2 响应式的 “补丁方案”（`this.$set`/`this.$delete`、数组用 splice 改索引），都是为了绕开这些 “无法劫持” 的场景，手动触发响应式。



vue3基于proxy监听整个对象，拦截内部方法，不需要动原型，但是比如改动数组长度相当于删除做了处理



总结：![image-20260117104544804](assets/image-20260117104544804.png)

## React学习

### 基础夯实

1. jsx分为编译时和运行时，原理分别是开发环境`jsxdev`和生产环境执行函数`jsxProd`,最后调用`ReactCreateElement函数`

2. `useCallback`是实现数据的缓存hook，但是使用需要注意不要传递变化项进行监听，否则会导致无效，以下是**优化**：

   ```
   const [count , setCount] = useState(0)
   
   const handleAdd = useCallback(() => {
   	setCount((c) => c+1 )
   },[])
   
   const handleSub = useCallback(() => {
    //就近取值count，缓存上一次的值
   	setCount((c) => c-1 )
   },[])
   
   /**  下面的写法会导致无效缓存（从外部count取值，无效）
   const handleSub = useCallback(() => {
   	setCount(count + 1)
   },[count])
   */
   
   //如果父组件传递函数需要更新count，修改count会产生副作用，我们不需要一个一个写关于count的修改,使用useEffect通知父组件更新（同步给外部）
   useEffect(() => {
   	props.onChange(count);
   },[props, count])
   
   return ( 
   	<div>
   		<div>{count}</div>
   		<button onClick={handleAdd} />
   		<button onClick={handleSub} />
   	</div>
   )
   ```

3. 条件渲染语法：

   - 三目运算符（a ？b : c）

   - switch  case

     ```
     const type : number | string = "number"
     
     switch(type){
     	case "number":
     	data = count
     	break;
     	
     	case "string":
     	data: prop.title
     	break;
     	
     	default:
     	break;
     }
     ```

   4.事件处理：

   - onClick（按钮）、onChange（输入框）、onDrag、onMove、onScroll
   - ...

   5.组件传值：

   - props
   - 在React里面所有数据都可以传递：函数也可以传递

   6.表单处理 ----react19关于表单写法（action  \  submit  \  get("name"）)

   - **受控组件**：表单数据由react状态来管理

     对表单项绝对控制

     ```
     const Form = (props: FromProps) => {
     	const [value,setValue] = useState("")
     	return (
     		<form>
     			<input 
     				type = "text"
     				value = {value} //受value控制
     				onChange((e) => setValue(e.taregt.value))
     			/>
     		</form>
     	)
     }
     ```

   - **非受控组件**，表单数据由DOM本身来管理

     获取DOM再清空值

   ```
   const Form = (props: FromProps) => {
   	//React中获取DOM的方式
   	const inputRef = useRef<HTMLInputElement>(null)
   	return (
   		<form onSubmit = {(e) => e.preventDeafult()}>
   			<input ref={inputRef} type = "text"/>
   			<button
   				onClick = {() => {
   					console.log(inputRef.current?.value)
   				}}
   			>
   				提交
   			</button>
   		</form>
   	)
   }
   ```

   7. react  hooks

      - useState : 状态初始化，状态修改

      - useEffect： 监听变量副作用，执行函数

        ```
        useEffect(() => {
        	console.log('useEffect')
        },[]) //没有监听，等同于挂载时执行
        //严格模式下`会执行两次useEffect`,开发环境下开启严格模式挂载时会有严格类型检测，挂载完成之后会卸载一次再挂载,设计目的是为了发现潜在的问题
        ```

      - useContext ： 

      - useReducer： 

      - useRef： 1、获取DOM元素 ， 2、缓存任意数据  3、避免重复渲染（标识组件的挂载情况方便处理）

        ```
        //避免重复渲染
        const useRef3 = () => {
        	//这个取值不会导致视图的变化
        	const mountedRef = useRef<boolean>(false);
        	
        	useEffect( () => {
        		mountedRef.current = true
        		
        		return () => {
        			mountedRef. current = false
        		}
        	})
        	
        	return (
        	<div>
        		{mountedRef.current && <h1>hello world</h1>}
        	</div>
        	)
        }
        ```

        

      - useCallback：缓存函数（就近取值）

        ```
        const handleAdd = useCallback(() => {
        	setCount( (c) => c+1)
        },[])
        ```

      - useMemo：用于缓存派生新的状态（缓存耗时计算的结果）

        ```
        const [count, setCount] = useState<number>(0);
        
        //1、派生新状态
        const doubleCount = useMemo (() => {
        	return count*2
        },[count])
        
        //2、count没有变，函数也缓存
        const fn = useMemo(() => {
        	return (count) => {
        		console.log(count)
        	}
        },[count])
        
        return (
        	<div  onclick={fn(count)} />  //优化写法，支持缓存函数
        	<div  onClick={() => fn(count)} />
        )
        ```

   ### 进阶特性

   1. ref（useRef声明）
   
      可以用来**获取DOM**：inputRef.current.value
   
      **也可以用来声明变量**:  声明的变量与组件的生命周期相关（useEffect === watchEffect）
   
      技巧：可以用同一个`useRef`声明多个DOM节点，useRef<Record<sting, HTMLelement | null>>({}) 声明
   
      ```
      ['input1','input2','input3'].map((item) => {
      	//操作dom
      	<input
      		key={item}
      		ref = {(el) => {
      			inputsRef.current[item] = el
      		}}
      })
      
      
      return (
      <input  ref={(el) => {inputsRef.current['input1'] = el}}
      <input  ref={(el) => {inputsRef.current['input2'] = el}}
      <input  ref={(el) => {inputsRef.current['input3'] = el}}
      )
      ```
   
   2. 多层ref获取问题(**forwardRef、**直接从**props**里面获取)
   
      ```
      const Input = (props) => {
      	return (
      		<div>
      			<input ref={ref} />
      		</div>
      		)
      }
      
      const Form = () => {
      	const inputRef = useRef<HTMLInputElement>(null)
      	return(
      		<div>
      		//	！！！这样无法获取ref，不能通过props值传递
      			<Input ref={inputRef} />
      		</div>
      	)
      }
      
      
      //----------老版本写法(forWardRef)-------------
      const Input = forwardRef((props,ref) => {
      	return (
      		<div>
      			<input ref={ref} />
      		</div>
      		)
      })
      
      
      //----React19中实现: 直接将useRef解构出来-------
      const Input = ({ref}) => {
      	return (
      		<div>
      			<input ref={ref} />
      		</div>
      		)
      })
      ```
   
   3. **Suspence**
   
      - **组件的导入优化**: 用Suspence 来做过渡过程的降级处理loading
   
      ```
      //全量导入渲染
      import {childComp} from './components/childCom'
      
      //优化：按需加载，异步组件+代码拆分chunk（build产物里面分包）
      import {lazy, Suspence} from "react"
      cosnt Child = lazy(() => import('/components/childCom'))
      
      return (
      	<div>
      	//动态加载组件并且在没有渲染完成之前loading
      	<Suspence fallback={<div>loading</div>}>
      		<Child />
      	</Suspence>
      	</div>
      )
      ```
   
      - **动态加载数据**：等待异步处理完之后，渲染内部组件
   
        1. ```
           import {Suspence, use} from 'react'
           
           const Detail = () => {
           	const data = use(fetchData(/topic))
           	
           	return(
           		<div>
           			{data}
           		</div>
           	)
           }
           
           export const Com = () => {
           	return (
           		<div>
           		//等待use执行完获取数据之后再处理渲染
           		<Suspence fallBack= {<div>loading</div>}>
           			<Detail />
           		</Suspence>
           		</div>
           		)
           }
           ```
   
   4. useEffect
   
      - 依赖项为[]
   
      ```
      useEffect(() => {
      	//模拟componentDidMount（onMounted），只在挂载结束后执行一次
      },[])
      ```
   
      - 没有依赖项
   
        ```
        useEffect(() => {
        	//只要变化了，整个副作用都会执行
        })
        ```
   
      - 指定[count]
   
      ```
      useEffect(() => {
      	//模拟count变化的componentUpdated(onUpdated)
      },[count])
      ```
   
   5. useContext、useReduser
   
   6. useMemo：缓存计算结果
   
   7. useCallback: 优化缓存函数
   
   8. useLayoutEffect: 和useEffect类似，但是是在所有DOM变更之后同步调用
   
   
   
   ### 状态管理
   
   - `Redux/RTK`：集中式 store、不可变更新、严格数据流、Provider 注入、DevTools 强、生态完善
   - `Zustand`：多 store、默认可变更新（可配合 immer）、按选择器精确订阅、高度简洁、无需 Provider



## TS

1、`interface`: 支持继承、重复声明自动合并、只支持对象或类对象结构声明

​	`type`： 支持合并(交叉类型&)、支持声明所有数据结构（联合类型、元组、类型别名、工具类型<partial>			可选、readonly只读）

2、`unkown` ->  `any` :  初始化变量为`unkown` , 后续需要进行类型收窄

3、 `void`： 函数执行没有`return`返回值

​	`never`： 函数永远循环 ｜ 抛出异常

4、`Generics` : 定义函数、类或接口不育险指定具体的类型，而是在使用时指定具体类型

5、`declare`:  **声明全局函数、变量或类**，统一放在`.d.ts`全局文件里面方便ts使用

​			 **定义模块**： 导入svg图片 ｜ 为没有类型定义的npm包提供类型

```
import logo form "./logo.svg"

//image.d.ts
declare module '*.svg'{
			const content: string;
			export default content;
}
```

6、`函数重载`:  组成（1️⃣重载签名 2️⃣ 实现签名）

​			步骤： 1、函数实现前定义调用方式   ->  2、编写支持重载签名的函数实现  ->  3、函数怒诶不使用					类型守卫判断传参执行对应逻辑

### 总结

1. 函数重载的核心是「**多类型签名匹配**」：为同一个函数定义多个「入参 - 返回值」类型规则，TS 按入参类型匹配对应的规则；
2. 结构上分为「重载签名（多个，仅声明）」和「实现签名（一个，实际逻辑）」，实现签名必须兼容所有重载签名；
3. 相比单纯的联合类型，重载能**精准推导返回值类型**，让类型校验更严格、更贴合业务逻辑。

```
//1.define
//签名一： 传入string，输出string
function reverse(str: string): string;
//签名二： 传入 T 类型，输出 T 类型
function reverse<T>(arr: T[]): T[];

//2.实现
function reverse(input: string | any[]): string | any[]{
	//3.内部类型守卫
	if(typeof input === 'string'){
		return input.split('').reverse().join('')
	}
	
	if(Array.isArray(input)){
		return [...input].reverse();
	}
	
	throw new Error('不支持的类型')
}
```

### 冒泡/捕获

捕获是「从上到下」（祖先→目标），冒泡是「从下到上」（目标→祖先），事件执行顺序：先捕获→目标元素→后冒泡；

冒泡是默认行为，核心用于**事件委托**；捕获用于**提前拦截事件**；

大部分 UI 事件（click、input 等）支持冒泡 / 捕获，`focus`/`blur`/`mouseenter` 等不支持；

`e.stopPropagation()` 可阻止事件传播，区分「阻止传播」和「阻止默认行为」（`preventDefault`）。



### postgresql

启动指令： brew services start postgresql@16

psql -U jayden -d doubao_db

验证版本：psql --versio

```
-- 创建用户（密码设为123456，和后续Prisma配置一致） CREATE USER Jayden WITH PASSWORD '123456'; 

-- 创建豆包项目的数据库 CREATE DATABASE doubao_db OWNER Jayden; 

-- 给用户赋权 GRANT ALL PRIVILEGES ON DATABASE doubao_db TO Jayden; 

-- 退出命令行 \q
```



```
# 安装 Prisma 依赖
pnpm add -D prisma
pnpm add @prisma/client

# 初始化 Prisma（生成 prisma 目录和 schema.prisma 文件）
npx prisma init
```



```
function myNew(Func,...args){
	const obj = {}
	obj._proto_ = Func.prototype
	const result = Func.apply(obj,args)
	return result = typeof result === 'object' ? result : obj
}
```

## Agent开发

### 1. 认知

1.ReAct思想

![image-20260424140442050](/Users/apple/Library/Application Support/typora-user-images/image-20260424140442050.png)

2. 一般来说我们不会用像ChatGPT这种模型直接作为我们的后端服务引擎，OpenAI也不允许，而是基于大语言模型封装对应的api暴露出模型的能力，期间会去处理Memory等等

3. agent的主要分类，可能有交叉

   | 类型                             | 核心特点                                                     |
   | -------------------------------- | ------------------------------------------------------------ |
   | **Action**              行动代理 | 模型直接调用方法拓展能力（比如调用 API、操作文件、控制机器人） |
   | **Simulation **    模拟代理      | 角色扮演，通过promt不断训练模型，训练成为一个专家角色        |
   | **Autonomous**  自主智能体       | 完全自主，还需迭代，整合更多工具调用。 Auto-gpt              |

4. function Call ： 模型调用方法去拓展自身能力 （查数据库之类的能力）



## 知识点

### node的文件查找逻辑

```
写法：require('./a')

文件查找：去寻找有没有这个文件，默认补齐.json || .js
文件夹查找: 去找这个文件夹下面的index.js || package.json - main

写法：reuqire('a')
内置模块：fs、path，没有的话从node_modules里面使用文件查找的方式，直到返回磁盘根目录
第三方模块
```



> 函数的本质是一个可以被调用的对象



### RAG的前端交互

1️⃣ 检索过程的交互，别让用户傻等

- 分阶段状态展示：问题理解中、知识库检索中，生成答案中，每个阶段都要明确提示，别一直转圈圈
- 检索进度可视化：比如显示“正在检索第5个文档”，让用户看到系统干活，不是卡住了
- 可以中途干预：比如检索到一半用户发现问题出错了，可以随时停止、修改问题重新检索，别等全部检索完才能改，浪费时间

2️⃣ 引用来源的展示，增强可信度

- 答案要标注引用：答案里的每句话都要标上来源编号，点击可以跳转到对应原文片段，用户可以自己核对，才敢相信答案
- 来源卡片要清晰：每个来源要有标题、来源文档、相似度、片段预览，用户一眼就能看到是从哪里来的，相关度高不高
- 支持原文跳转：点击引用直接跳转到原文当对应位置

3️⃣ 多轮对话的上下文，怎么结合知识库

- 上下文要可追溯：多轮对话的时候，要告诉用户当前回答用了哪些知识库内容，降低用户疑惑
- 支持重置上下文：用户可以选择“只问知识库”、“结合历史对话”、“全新问题”三种模式，不同场景用不同模式，更灵活
- 追问优化提示：如果用户问的问题太宽泛，检索效果不好，前端要给出优化建议，比如“建议加上具体场景”、“可以更具体一点”，引导用户

4️⃣ 个性化与反馈

- 点赞/点踩反馈：每个答案下面都有有用/没用的反馈按钮，用户觉得不对可以踩，还可以输入原因，这些数据可以用来优化检索效果
- 收藏与历史：用户可以收藏好的回答，查看历史问答记录，还可以基于历史回答继续追问，不用每次都重新问
- 个性化对尖：根据用户的部门岗位、历史提问，推荐相关知识库内容与常见问题，用户不用每次都从零开始问

> 总结：检索过程可视化、引用来源要清晰、多轮上下文可控、用户反馈要做好



## 长对话的上下文处理

1️⃣ token计算和管理不只是后端的事 

- 前端必须实时估算当前上下文的token数，不能等后端返回报错了才知道超了
- 粗略估算：中文一字越1.5Token，英文1词约 1 Token，代码和特殊字符Token更多
- 精确计算用tiktoken的WebAssembly版本，前端可以直接跑，不要依赖后端
- 每次发送前检查Token总量，接近模型上限时主动提示用户“上下文呢即将超限”，别等炸了才处理

2️⃣ 上下文截断策略

- 截断不是简单砍掉前面的消息，要保证上下文对话仍然有意义
- 常用策略：保留系统提示词+最近N轮对话 + 关键历史摘要
- 系统提示词永远不能阶段，这是AI角色的定义，砍了人格就没了
- 最近N轮优先保留，越近的对话和当前问题越线骨干
- 关键历史摘要时把早期对话压缩成一段总结，用大模型生成摘要后替换原是消息

3️⃣ 对话历史的持久化和恢复

- 别只存内存，用户刷新页面历史全丢，这是最低级的错误
- 短期存储用sessionStorage，跨标签页不第一；长期存储用IndexedDB或localStorage
- IndexedDB更合适，因为体积够大
- 恢复对话时从存储读取历史，截断后再发给模型



## 登录态无感刷新JWT并发陷阱

1️⃣ 第一层：刷新token必须有单飞机制

- 同一时刻智能有一个refreshToken请求在路上。不能每个401都去刷新一次，否则十几个接口同时过期，就会打出十几个刷新请求
- 前端要维护一个全局的 isRefreshing状态。第一个401出发刷新，后续401不再重复刷新，而是进入等待队列
- 刷新成功后，用新的 accessToken 统一重访队列里的请求。刷新失败后，在统一清理登录态并跳转登录页面
- 刷新token不是普通接口，是全局登录态的状态切换，必须串行处理

2️⃣ 第二层： 失败请求要排队，不能各自乱重试

- 刷新过程中，其他接口不能直接报错，也不能立即跳登录也，而应该等刷新结果
- 如果刷新成功就给原请求重新挂上新token，再重放一次。注意不是所有请求都适合重放。
- 查询类请求可以重放。保存草稿、提交订单、支付、审批这类写操作，要谨慎重放，必须依赖业务幂等或前端提交锁。防止401自动重放，变成重复保存、下单

3️⃣ 第三层：登录态变化要有全局一致性

- token更新不能散落在各个请求里面随便写，必须封装统一的token管理器，负责读取、更新、清理、广播
- 多标签页场景也要处理。用户在一个标签页退出登录，另一个标签页不能继续拿旧的token请求接口。可以通过 storage事件或BroadcastChannel同步登录态变化
- 刷新失败时，不要粗暴地清空页面数据。对应正在填写表单页的用户，要尽量保留当前路径和草稿数据，登录后能回到原页面继续操作



## 大模型如何稳定输出JSON

1️⃣ 提示词

> - Few-shot示例学习
> - 强制输出格式指令

2️⃣ 底层能力

> - 模型侧的能力支撑，开启结构化输出/Json Mode
> - 调用fuction callding函数的时候强制走工具入参输出JSON schema

3️⃣  兜底机制

> - 正则剔除所有非json的干扰内容，比如移除md标识
> - 清洗字符串，使用json.parse校验，解析失败进入重试分支，多次重试仍然失败，返回兜底json



## Redis

1️⃣ Redis为什么这么快

> - 纯内存操作（避开了磁盘I/O的读写）
> - 单线程模型（无上下文切换和锁竞争）
> - I/O多路复用 （基于epoll等机制，一个线程高效处理多连接）

2️⃣ Redis支持哪些数据类型？分别用在哪里？

> - String : 缓存、计数器、分布式锁（SETNX）
> - List：消息队列、最新列表
> - Hash：存储对象（用户信息、购物车）
> - Set：去重场景（点赞、抽奖）
> - ZSet：各类排行榜

3️⃣ Redis的持久化机制RDB和AOF有什么区别？

> 都是redis防止数据丢失的备份手段
>
> - RDB: 就像给数据拍照片 ，redis在指定时间内将内存中的数据集快照写入磁盘，触发方式包括SAVE和BGSAVE
>
>   优点：文件紧凑、恢复速度快，适合做全量备份和灾难恢复
>
>   缺点：如果宕机，会丢失最后一次快照之后的所有写入数据
>
> - AOF：记流水账，每执行一条写命令都会写入AOF缓冲区，再更具配置的刷盘策略写入磁盘
>
>   优点：做到秒级甚至逐条的数据安全，数据丢失风险极低
>
>   缺点：文件体积大，恢复时需要逐条重放命令速度慢
>
> - 生产环境用RDB做全量冷备，AOF做增量热备

4️⃣ 什么是缓存的穿透、击穿、雪崩，如何解决？

> - 穿透（查询不存在的数据，请求直接穿过缓存层打到数据库，由于缓存始终不命中，恶意攻击或爬虫使用大量不存在的key恶意压垮数据库）：
>
>   解决办法：缓存空对象，将空值也缓存起来设置较短过期时间。
>
>   二是使用布隆过滤器可以判断哪些对象是不存在的
>
> - 击穿（某一个热点key在过期期间，大量并发请求直接打到数据库）
>
>   解决办法：一是设置热点数据用不过期，通过异步线程定期刷新其值
>
>   二是加互斥锁，让第一个请求去查库填充缓存，其余请求等待或返回旧值，避免数据库被瞬间压垮
>
> - 雪崩（大面积key在同一时间段内集中过期或者redis服务宕机，导致所有流量瞬间打到数据库 ）
>
>   解决办法：一是为key的过期时间加上一个随机值，让他们分散在不同时间点失效；二是搭建redis哨兵模型或集群模式，保证服务自身的高可用，避免单点故障

5️⃣ Redis的过期键删除策略是什么？

> **惰性删除（访问时检查） + 定期删除（每100ms随机抽查），平衡CPU和内存**
>
> 一 、请求时才检查是否过期，如果过期才删除并返回空，这种方式对CPU友好，但大量过期key长时间没有被访问会占用内存
>
> 二 、默认100ms扫描添加了TTL的数据，通过限制扫描规模和频率，既可以定期删除一些过期key，也不会长时间阻塞CPU

6️⃣ Redis的内存淘汰策略有哪些？内存满了怎么办？

> - Alleys-lru: 淘汰所有key中最近最少使用的（最常用）
> - Volatile-lru: 仅淘汰有过期时间的LRU数据
> - noeviction：内存满时直接报错

7️⃣ 如何保证Redis和数据库的数据一致性（更新DB时，更新缓存还是删除缓存）

- **绝对不要更新缓存，一律删除缓存**，并发灾难的场景会导致脏数据
- 推荐：先更新DB，再删除缓存

7️⃣ - 1️⃣ **基础方案**流程：一、执行SQL更新数据库 -> 二、删除Redis对应Key -> 三、下次用户查询时，查DB最新数据回填缓存

7️⃣ - 2️⃣ **增强方案延时双删**流程：一、删除缓存 -> 二、更新数据库 -> 三、延时等待几百ms（「读线程查询 DB + 回填缓存」） -> 四、再删除一次缓存

7️⃣ - 3️⃣ **Canal 监听Binlog 异步更新缓存**： MySQL Binlog（记录数据库增删改事件）→ Canal 服务监听抓取日志 → 投递变更消息到 MQ → Java 消费者异步删除 / 更新缓存

8️⃣ Redis实现分布式锁有什么办法？

| 锁方案                  | 适用范围   | 可重入 | 自动续期     | 原子性                 | 生产是否推荐     |
| ----------------------- | ---------- | ------ | ------------ | ---------------------- | ---------------- |
| synchronized            | 单机 JVM   | 支持   | 无           | JVM 原子               | 仅单机           |
| ReentrantLock           | 单机 JVM   | 支持   | 无           | JVM 原子               | 仅单机           |
| SET NX EX 原生 Redis 锁 | 分布式集群 | 不支持 | 无           | 加锁原子，续解锁非原子 | 测试 / 简单 demo |
| Redisson RLock          | 分布式集群 | 支持   | 看门狗自动续 | Lua 全流程原子         | 生产标准方案     |

9️⃣ 什么是锁？什么是分布式锁？为什么需要他们？

> - 锁（单机锁）：在**单台服务器、单 JVM 进程**内部，多个线程同时竞争同一个共享变量 / 共享资源时，用来实现**互斥访问**的机制
>
> - 分布式锁：分布式锁是**多台服务器争抢同一个共享资源时，用来互斥控制**的工具
>
> - 原因：保护共享资源，防止脏数据，超卖，重复执行
>
>   保证临界区代码串行执行，规避线程安全问题



## JDK

JDK: java开发工具包，包含JRE

JRE：java运行环境

只需要运行Java项目，安装JRE 需要开发Java项目，必须安装JDK，包含JRE 

Java 不是解释型语言

 HTML JS Java 

需要先编译成字节码文件，

再通过JVM（Java虚拟机）对字节码文件进行进一步的解析，生成机器码，底层操作系统才能识别机器码进行程序的运行

 Java 为什么先需要编译？ 

为了实现Java的一大特性，一次编译，到处运行 跨平台

如何实现跨平台？

编译：源码 -》 字节码文件 -〉不同操作系统由jvm识别解析运行

Test.java -> Test.class

java编译成字节码的命令

```
javac Test.java
```

java运行字节码的命令

```
java Test
```

## 变量

为什么要使用变量？变量是为了解决什么问题？

数据存储地址的问题

数据都是在内存中进行存储的，存在一个问题？

程序会随机分配内存区域

内存地址是 16 进制的数据，随机分配，地址很难记住

如何解决这个问题？

使用变量

变量就是为了解决内存地址很难记忆的问题

相当于给内存地址起了一个别名，取数据的时候直接通过变量名取即可，不需要再通过内存地址来取数据了

1、声明变量的数据类型和变量名

Java 中有多少种数据类型？

Java 有两大类数据类型

- 基本数据类型（不是对象，可以通过包装类将它改造成对象，8 种）

- 引用数据类型（对象，JDK 类库、框架类库、开发者自定义类，无数种）

  

  Java 开发中使用的对象共有三种来源

- JDK 类库 String Date Integer

- 第三方类库 框架 Application SpringBootApplication DispatcherServlet

- 自定义的类

2、变量的值

```
int i = 1;
String str = new String("Hello world");
```

基本数据类型和引用类型在内存中的区别

所有的变量都是存储在栈内存中的，无论是基本数据类型还是引用类型，基本数据类型和引用数据类型的区别在于是否使用了堆内存

基本数据类型不需要使用堆内存，直接将变量的值存入栈内存

引用类型需要使用堆内存，对象是创建在堆内存中的，将堆内存的地址引用赋值给变量

#### 变量类型

数值类型：byte、short、int、long、float、double

非数值类型：char、boolean

byte 空间 1 个字节

byte KB MB GB TB

1KB = 1024 byte

1MB = 1024 KB

1GB = 1024 MB

1TB = 1024 GB

1 个字节 = 8 位二进制

short 空间 2 个字节

int 空间 4 个字节

long 空间 8 个字节

float 空间 4 个字节

double 空间 8 个字节

char 空间 2 个字节

boolean 空间几个字节？

1/8 个字节 1 位 1bit   1/8 byte

#### 数据类型的转换

1、基本数据类型的转换

- 自动转换
- 强制转换

尽量不使用强制类型转换，因为存在数据精度损失的隐患

```
double num = 10.5;
int i = (int)num;
```

byte -> short -> int -> long -> float -> double

从小向大转

2、引用数据类型的转换 继承

前提是需要转换的两种数据类型必须有继承的关系

- 自动转换
- 强制转换

```
public class Test {
    public static void main(String[] args) {
        Student student = new Student();
        Person person = new Person();
        //自动类型转换
        person = student;
        //强制类转换
        student = (Student) person;
    }
}
```



## router

React createHashHistory

> 简单说，createHashHistory 就是 history 库里用来创建哈希路由的方法，也是 React Router 里 Hash 模式的底层实现。它靠 URL 里 # 后面的那段内容来模拟路由路径，最大的好处就是不用后端配合配路由回退规则，纯静态页面扔上去就能用。
>
> 关于它的底层变化，我是这么理解的：
>
> 早期的时候，哈希路由和浏览器路由是两套完全分开的实现。浏览器路由靠 popstate 事件监听前进后退，用 pushState、replaceState 改路径；而哈希路由专门监听 hashchange 事件，直接改 location.hash 来实现跳转。俩各玩各的，代码也完全不共用。
>
> 后来 history 库做了架构重构，现在底层基本是一套逻辑走通了。原因也很简单：现在的浏览器里，哈希段变化同样会触发 popstate 事件，那就没必要分开写两套了。
>
> 所以现在两种路由模式，底层都是统一监听 popstate，都用 pushState、replaceState 来管理浏览器历史栈，像前进后退、路由订阅这些逻辑全是复用的。唯一的区别就剩「路径读写」这一小块：读路径的时候，哈希模式就从 location.hash 里把 # 后面的真实路径抠出来；写路径的时候，把路径拼到 # 后面再更新。除此之外，对外的接口、行为表现全都是统一的。
>
> 这么设计的好处也很明显，不用重复写两套逻辑，两种模式的行为也更一致。上层用的时候完全感知不到区别，比如 React Router 里不管用 BrowserRouter 还是 HashRouter，写法都一模一样，就是入口处选一下模式就行。
>
> 当然哈希模式也留了点专属的兼容处理，比如页面第一次打开没有 # 的话，会自动补个 #/ 当根路径，特别老的浏览器也还留着 hashchange 当兜底，但主流程已经完全统一了。

React createMemoryHistory

>1. **基础定义**
>
>   React Router v6.4+ 推出的内存路由模式，与 Browser 路由、Hash 路由同级；路由状态完全保存在内存中，不绑定浏览器地址栏，也不依赖浏览器原生历史记录。
>
>2. **功能完整性**
>
>   完整支持 React Router 全部核心能力：嵌套路由、动态路由参数、404 兜底、`useNavigate`/`useParams`/`useLocation` 等 API 均可正常使用，业务代码层无需感知底层路由模式差异。
>
>3. **专属配置参数**
>
>- `initialEntries`：数组类型，用于预设内存历史栈的路由记录，可直接模拟用户已访问过多页面的状态
>- `initialIndex`：数字类型，指定初始展示的路由在 `initialEntries` 中的索引（从 0 开始），页面初始化直接定位到对应路由
>
>1. **路由切换特性**
>
>   支持正常的编程式跳转、前进、后退操作，但所有变化仅作用于内存中的历史栈；浏览器地址栏不会同步更新，浏览器原生的前进 / 后退按钮也无法控制该路由。
>
>2. **核心应用场景**
>
>- 单元测试：Node 测试环境（Jest/Vitest）下的标准路由方案，无需依赖浏览器环境即可测试路由逻辑
>- SSR 服务端渲染：服务端无浏览器 API，通过内存路由完成服务端页面渲染与 HTML 生成
>- 非浏览器端：React Native、桌面端内嵌等无浏览器地址栏的运行环境
>- 隔离内嵌路由：弹窗、分步向导、侧边栏内部的独立路由系统，不影响页面主 URL
>
>1. **设计本质**
>
>   
>
>   将「浏览器地址栏 + 原生历史栈」替换为「内存数组」，在保留完整路由能力的同时，实现了与浏览器环境的完全解耦。

### React createStaticHistory

>`createStaticHistory` 是「极简只读版」：只能固定在一个路径，没有任何交互能力，专门给一次性渲染的 SSR/SSG 场景设计，性能开销更小
>
>React Router 中的 `<StaticRouter>` 组件，底层就是基于 `createStaticHistory` 封装的。服务端渲染时你不需要手动调用它，只需给 `<StaticRouter>` 传入 `location` 属性指定路径，组件内部会自动创建静态历史对象。

### 虚拟dom

>- 直接操作真实dom的成本很高：高频的重排重绘导致UI线程被长时间占用，页面无响应
>- 虚拟dom是一个javascript对象，操作成本低
>- 核心工作流：setState触发更新  -> 内存中创建一颗新的虚拟Dom树  -> diff差异化比较  -> 计算最小变更 -> 将错有差异一次性、有针对性更新到真实dom上
>- 跨平台多端渲染：UI层的描述，可以运行在多端（react、reactNative）

### JSX

> 是js的**语法拓展**
>
> - jsx需要被编译：浏览器无法识别这种语法，通常使用Babel编译，等同于React.createElement || _jsx函数执行
> - jsx的产物是Js对象
>
> ```
> {
> 	type: "h1",
> 	props:{
> 			className:"greeting",
> 			children:"hello,world"
> 	}
> 	// ...其他内部属性
> }
> 
> ```
>
> - 无缝嵌入Javascript的全部能力：可以通过 {} 嵌入任何有效的js表达式 

### 函数组件和类组件的区别

> 1️⃣ 心智模型
>
> - 类组件（面向对象）：我们操作的是一个长期存在的、状态可变的“实例”
> - 函数组件（函数式编程）：我们操作的是一次性的渲染函数，捕获特定时刻的值
>
> 2️⃣ 生命周期 & 副作用
>
> - 类组件：关注时间点(componentDidMount、componentDidUpdate、componentDidUnmount)，这类方式的关注点是实践，不易维护 
> - 函数组件：关注同步（useEffect）
>
> 3️⃣ 值捕获
>
> - 类组件：this
> - 函数组件：闭包捕获某一时刻的值

## Props不可变

> 1️⃣ 维护单向数据流：父组件流向子组件的数据，状态变化可预测单一数据源的核心保障，如果修改的话导致状态难以追踪和调试
>
> 2️⃣ 性能优化：让dom的更新变的高效简单，通过高效的浅比较去跳过不必要的渲染
>
> 
>
> 如果变了的话react被迫进行“深比较”，去递归遍历所有属性和新旧值的差异。
>
> 3️⃣ 如何修改：将props值作为组件内部的state初始值进行修改

## Fiber架构

> - 前提：之前是用的堆栈执行机制，是一个不可打断的渲染任务，在处理复杂任务或组件时，长时间阻塞主线程导致页面卡顿
>
> - Fiber解决问题：
>
>   1️⃣ 增量渲染，避免主线程阻塞：将任务拆分到多个浏览器帧执行，Fiber极大减少了单次JS执行时间，让应用保持流畅
>
>   2️⃣ 实现更新的优先调度级：任务可中断，React能为不同更新赋予不同优先级
>
>   最高优先级： 用户输入（打字、点击）
>
>   中等优先级：动画更新
>
>   低优先级：网络请求数据更新
>
>   3️⃣ 为并发特性铺平道路
>
>   Fiber是一种底层架构，为React的未来诸多新特性提供了可能性
>
>   `Suspence\uesTransition\useDeferredValue`等并发特性，都建立在Fiber**可中断和可调度**的能力上
>
> 
>
> **1、定性：**
>
> react15之前都是递归diff，一条路走到黑，碰到复杂的组件树将会阻塞渲染主线程，导致用户点击行为卡死，Fiber解决的问题就是使用链表的数据结构来处理虚拟dom的diff，将原本不可拆分的渲染过程拆成一个一个小片段，并且支持根据任务优先级方式调度；
> **2、为什么可中断：**
>
> 链表结构天然可以支持中断，下一次重断点处继续；同时有双缓冲技术，同时在内存中维护渲染可见树和正在构建的新树，更新时在新构建的树上渐进式工作，完成后一次性吧渲染可见树指向它，这个过程不会阻塞用户看到的内容渲染；同时配合scheduler任务调度器根据任务紧急程度打断插入任务；
> **3、解决了什么问题：**
>
> 从“不可中断的同步递归”编程“可中断的异步调度”，本质上是渲染策略的升级，fiber没有使react完成一次完整的渲染过程变快，只是把工作拆成小片并且可中断穿插高优先级的任务，让页面看起来更流畅



## Key属性

> 1️⃣ 为什么不推荐用数组index作为key
>
> 可能会出现视图与内部数据状态不符的bug（多项数组，删除第一项之后第二项变成了第一项，diff算法就会以为第一项还在但是数据是第二项的，就保留原来第一项的dom只不过数据是第二项的）
>
> ![image-20260702104137381](/Users/apple/Library/Application Support/typora-user-images/image-20260702104137381.png)
>
> 2️⃣ 极端情况可以接受 key ={index}
>
> - 列表和项目完全静态，他们的内容和顺序永远不变
> - 列表中的项目没有 稳定唯一的ID 可用
> - 列表永远不会重排序、过滤和在开头/中间插入数据
>
> 3️⃣ key的核心亮点
>
> - 核心作用：作为Dom diff更新的“唯一标识”，高效地更新以及节点复用
> - 最佳实践：始终使用数据中稳定且唯一的标识作为key
> - 避免使用index作为key：如果列表数据发生重排或者数据修改可能会导致错误的节点和数据状态的bug
> - 强制重载：同时也可以通过改变key来实现强制组件的重新挂载并重置其内部状态

## React合成事件机制

> ##### 事件挂载位置改变（不再统一绑根节点）
>
> - React16 及更早：所有合成事件统一**挂载到 document / #root 根 DOM**，靠根节点统一捕获冒泡；
>
> - React17+：**事件直接绑定到对应 DOM 元素本身**，和原生事件挂载逻辑一致。
>
>   每个 DOM 的
>
>   ```
>   onClick
>   ```
>
>   ```
>   onChange
>   ```
>
>   等合成事件，都会在真实 DOM 上注册原生监听。
>
> ##### 移除事件池（SyntheticEvent 不再复用回收）
>
> 16 版本会复用`SyntheticEvent`，事件执行完清空属性放回池子里，导致异步拿`e.target`会丢失；
>
> 17+ 每次触发事件**新建独立合成事件对象**，执行后不回收，异步代码（setTimeout/Promise）里也能正常访问`e`。

## 受控和非受控组件

> - 受控组件：亲力亲为，调用setState更新数据源
>
>   优点：即时验证+条件化逻辑+单一数据源
>
> ![image-20260702110453193](/Users/apple/Library/Application Support/typora-user-images/image-20260702110453193.png)
>
> - 非受控组件：直接使用 useRef 附加到Dom元素上，获取Dom自身的值
>
>   优点： 代码简洁 + 接近原生HTML + 集成方便+ 避免了react的重渲染
>
> - 如何选择
>
> 1️⃣ 受控组件：
>
> - 需要对用户输入实时验证或者格式化 
> - 需要根据一个输入框的值动态改变另一个UI元素
> - 多个表单字段存在依赖关系
>
> 2️⃣ 非受控组件
>
> - 非常简单的表单，只关系最终值
> - 一次性取值，不需要追踪它每一次的值
> - **上传文件组件**，默认不可读value，默认是非受控组件

## 严格模式

> 只开开发模式下的辅助工具
>
> - 通过额外的检查，帮助我们提前识别代码中的**不安全事件、潜在副作用和 废弃API**
> - 在开发中集成能够有效提升当前代码的 **质量和稳定性**
> - 双重调用检查副作用

## useState的更新是异步的嘛

先看一个例子

```
function Counter(){
		const [count , setCount] = useState(0)
		
		const handleClick = () => {
				setCount(count + 1)
				console.log(count)  // 打印输出0，而不是1
		}
}
```

> 闭包：SetCount(count+ 1)的作用不是立即修改count，而是向React“**提交一个更新请求”，并告知React在未来某个时刻执行更新**

> 批量更新（batching - createRoot API）: 将多次事件处理函数放到一个队列里面，在事件处理函数执行完毕之后，将所有更新合并，只进行一次重渲染

**回答**：

- **同步提交，延迟执行**：`setState`函数本身是同步的，但他触发的状态更新和组件重渲染，会被React延迟处理并进行批量合并
- **性能最优化**：通过将多次状态更新合并成单次渲染，避免不必要的计算和DOM操作，最大化应用性能
- **状态一致性**：保证在一次事件处理流程中，所有状态变更能够同步生效，防止出现不完整的中间 UI状态，提升应用健壮性

## 函数式更新

1️⃣ 直接传入新值：最终结果1

```
const [count , setCount]= useState(0)

const handleIncrement = () => {
		//直接处理数值
		setCount(count +1)
		setCount(count +1)
		setCount(count +1)
}
```

> ![image-20260702144335795](/Users/apple/Library/Application Support/typora-user-images/image-20260702144335795.png)



2️⃣ 传入一个函数（为了解决“setState闭包陷阱”）最终结果3

```
const [count , setCount]= useState(0)

const handleIncrement = () => {
		//告诉React 如何基于旧状态计算新状态
		setCount(prevCount => prevCount +1)
			setCount(prevCount => prevCount +1)
				setCount(prevCount => prevCount +1)
}
```



## 状态提升

- 首先状态提升时react解决兄弟组件之间的通信的核心模式，将需要共享的状态，移动到他们最近的共同父组件中统一管理。
- 优点：让子组件更加存粹，数据源单一可控
- 缺点：大型应用中面临过度渲染和代码维护的挑战

## useReducer 和 useState

>useReducer 不能完全替代 useState，二者互补。如果组件状态复杂、多处存在繁琐的更新逻辑，用 useState 会让代码混乱，这时就适合使用 useReducer。
>
>它把所有状态更新逻辑抽离到纯函数 reducer 里，实现视图和业务逻辑分离，状态更新流程可预测、方便调试；同时搭配 Context 做多层组件传值时更友好，最终提升代码可维护性。
>
>useState 本质底层也是封装了简易版 reducer，二者底层同源；只是 useReducer 人为把更新逻辑抽离出来，适合多分支、复杂联动的状态。

dispatch

>dispatch 是 useReducer 提供的派发函数，我们传入带 type 的 action 对象去触发状态更新。
>
>它最大优势是引用稳定，搭配 Context 多层传参时不用额外缓存；所有更新逻辑统一收在 reducer 里，相比零散的 setState，复杂业务下更好维护。



## 如何优化useContext带来的性能问题



- 性能陷阱

> useContext的核心机制是“广播”：当Provider 的 value 改变时，所有消费该Context的组件都会重新渲染

> 意味着即使该组件只依赖value的一小部分数据，只要vulue对象的引用发生变化，组件会被强制更新（不必要重渲染）

- 解决方案

> ##### 场景 1：Context 存放多类无关 / 更新频率差异大的数据
>
> - 推荐方案：**拆分多个独立 Context**
> - 核心原理：实现按需订阅，组件仅订阅自身需要的数据片段，一处数据更新不会触发全部消费组件重渲染。
>
> ##### 场景 2：Provider 包裹了大量不依赖上下文的重型静态组件
>
> - 推荐方案：**children 组件抽离组合**
> - 核心原理：把静态组件通过 children 参数传入 Provider，静态组件脱离 Provider 更新逻辑，不会随上下文变化重复渲染。
>
> ##### 场景 3：子组件仅修改状态、不读取状态数据
>
> - 推荐方案：**分离状态数据与更新 API（搭配 useReducer）**
> - 核心原理：useReducer 的`dispatch`函数引用永远稳定，单独放到独立 Action 上下文；只调用 dispatch 的组件订阅稳定函数上下文，不会因数据变更触发多余渲染。

##   Redux核心思想 

 流程：

> ![image-20260702154556787](/Users/apple/Library/Application Support/typora-user-images/image-20260702154556787.png)

- 单一数据源 store
- 状态只读：通过Action表达意图
- 纯函数变更：reduce负责计算新状态
- 单向数据流

**Immer.js**: 解决了手动维护深层的状态

> 存在的问题：Redux中的state结构非常复杂，如果要修改一个深层的值，需要手动复制从根节点到目标节点的每一层对象做一个浅拷贝

- Immer 的 produce 函数提供 draft 草稿，我们可以直接写可变修改逻辑，内部自动生成全新 state，代码简洁、意图直观、减少复制漏写 bug。

```
const nextState = produce(原state, (draft) => {
  // 直接像修改普通对象一样操作draft
  draft.user.profile.settings.theme = 'dark'
})
```

- **draft** 依赖的是ES6 的 **Proxy**和 “写时复制”机制

><img src="/Users/apple/Library/Application Support/typora-user-images/image-20260702155253338.png" alt="image-20260702155253338" style="zoom:67%;" />

## Zustand Jotai （Poimandres社区）

- Zustand：自上而下的**全局状态管理**
  - 单一数据源 和 不可变更新
  - 一个简单好用的管理，使用create API，没有Actions，reducers，Dispatch
  - 不需要Provider，状态时全局的，不需要<Provider>包裹
  - 性能优化：只有被订阅的状态变化时，组件才会重新渲染，避免了不必要的渲染
  - 支持中间件
- Jotai：自下而上的**原子状态化管理**
  - 模仿useState，使用atom定义状态单元
  - 自动性能优化，按需渲染，减少不必要渲染

```
// 基础原子状态
const countAtom = atom(0);

// 创建“派生 Atom”（计算属性，依赖其他atom自动更新）
const doubleCountAtom = atom((get) => get(countAtom) * 2


function Counter() {
  // 用法和useState结构完全一致
  const [count, setCount] = useAtom(countAtom);
  // ...
}
```

## SWR 和 React Query

 1️⃣ SWR流程

![image-20260702161350086](/Users/apple/Library/Application Support/typora-user-images/image-20260702161350086.png)

#### 一、原生 useEffect 请求方案五大痛点

1. **模板代码冗余**

   每个组件都要重复定义 

   ```
   data
   ```

   ```
   isLoading
   ```

   ```
   error
   ```

    三套状态，配套完整更新逻辑。

2. **缺乏全局缓存去重**

   组件卸载再挂载会重复发起相同接口请求，浪费带宽、增加用户等待时间。

3. **数据陈旧**

   请求一次后数据永久不变，无法同步服务端最新变更。

4. **竞态条件问题**

   Props 快速切换发起多次请求，旧请求晚于新请求返回，页面数据错乱。

5. **用户体验差**

   切换标签页、网络重连不会自动刷新最新数据。

#### 二、核心思想：区分客户端状态 & 服务端状态

##### 1. 客户端状态 Client State

- 来源：用户本地交互操作
- 特点：同步、完全可控、可预测
- 示例：表单输入、弹窗开关、主题模式
- 管理工具：`useState` / `useReducer`

##### 2. 服务端状态 Server State

- 来源：后端远程接口
- 特点：异步、不受前端控制、服务端可被他人随时修改
- 示例：列表数据、用户信息、库存、文章详情
- 管理工具：React Query / SWR（专门配套缓存、同步、更新机制）

> 核心理念：SWR、React Query 把服务端数据当成**需要持续同步的特殊状态**，而非一次性接口请求。

#### 三、核心设计思路

基于 `Stale-While-Revalidate` 缓存策略，提供**声明式 API**，开发者只需要传入两个参数：

1. `queryKey`：唯一标识当前请求数据
2. `queryFn`：真正发起网络请求的函数

#### 四、核心优势（对应解决五大痛点）

##### 1. 自动管理加载、错误状态，简化代码

- useEffect 写法：手动维护三套 state、大量重复样板代码，逻辑分散；
- useQuery 写法：直接解构 `data` / `isLoading` / `error`，代码精简、逻辑集中、业务意图清晰。

##### 2. 全局缓存 + 请求去重

- 第一次请求：接口拉取数据存入全局缓存；
- 其他组件使用相同 `queryKey`：直接读取缓存，不重复发请求；
- 收益：页面加载速度更快，大幅降低后端接口压力。

##### 3. 自动重新验证（自动刷新，解决数据陈旧）

满足以下场景自动重新拉取最新数据：

- 浏览器窗口重新聚焦
- 网络从断开恢复重连
- 组件重新挂载
- 可自定义定时轮询

##### 4. 内置处理竞态条件

框架内部自动处理请求时序，保证最终渲染最新请求的数据，不会出现旧接口覆盖新数据。

##### 5. 内置高级场景能力

1. **分页 / 无限滚动**：`useInfiniteQuery` 封装分页加载逻辑；
2. **乐观更新**：提交修改时先更新 UI，接口失败再回滚，交互流畅；
3. **请求自动重试**：接口报错自动重试，提升应用健壮性。

#### 五、最终结论：新一代服务端数据获取范式

1. SWR / React Query 不只是请求工具，是**服务端状态管理器**；

2. 范式升级

   旧方案：

   ```
   useEffect
   ```

    命令式、手动、繁琐、易出错

   新方案：声明式、自动同步、高效健壮；

3. 三大价值：

   - 提升开发效率：删除大量重复模板代码；
   - 优化用户体验：缓存 + 自动刷新，页面更快、数据实时；
   - 增强应用健壮性：内置竞态处理、失败重试等边界逻辑。

## Signals：更新的不是组件，而是Signal本省（slide.js）

>key 只是 VDOM diff 阶段用来复用列表 DOM 节点的标识，它只能优化最后的 DOM 更新步骤，无法阻止状态变更后**整棵组件树全部执行 render、生成 VDOM**的前置开销；
>
>而 Signals 是细粒度响应式模型，编译时收集 Signal 与 DOM 的依赖，状态变化只精准通知订阅它的 DOM 节点，直接更新 DOM，全程绕开组件重渲染和 VDOM 比对，这才是 “精准更新、告别整体渲染” 说法的来源，二者优化层面完全不一样。

### Signals 与 React 渲染模型核心区别

#### 1. React 整体渲染流程（key 的局限性）

流程：状态变更 → 全组件树重渲染执行 render → 生成完整 VDOM → key 参与 diff 比对 → 更新 DOM

- key 作用：仅优化 diff 阶段 DOM 节点复用，**不能阻止组件执行 render**；
- 缺陷：无关组件依然会完整执行渲染逻辑，存在大量无用计算开销。

#### 2. Signals 精准更新流程

流程：Signal 值变更 → 仅通知订阅该 Signal 的 DOM 节点 → 直接更新 DOM

- 底层：编译期自动收集细粒度依赖；
- 优势：完全跳过组件重渲染、VDOM 生成、diff 全过程，无多余计算。

#### 3. 核心差异总结

- key 属于**粗粒度后置优化**，仅优化 DOM 操作，无法解决组件批量 render 开销；
- Signals 是**底层细粒度响应模型**，从源头避免无关代码执行，是新一代渲染范式。

## Hook调用顺序

- 始终在组件顶层调用Hooks
- 不要再循环、条件或嵌套函数中调用
- 条件逻辑放在hook内部，而非调用层面

## useEffect useLayoutEffect

1️⃣ useEffect

- 异步执行，不阻塞浏览器渲染，渲染之后执行
- 网络请求、事件监听、日志记录

2️⃣ useLayoutEffect：解决“闪烁问题”

- 同步执行，dom更新完成之后，渲染之前执行
- 阻塞渲染、可能导致页面卡顿

![image-20260702164119502](/Users/apple/Library/Application Support/typora-user-images/image-20260702164119502.png)

### useEffect依赖项（useMemo/useCallback优化）

- Object.is()比较算法判断依赖项比较的是引用地址

![image-20260702164926346](/Users/apple/Library/Application Support/typora-user-images/image-20260702164926346.png)

![image-20260702164943569](/Users/apple/Library/Application Support/typora-user-images/image-20260702164943569.png)

### 如何正确处理竞态异步请求？

 ❌ 不能直接在useEffect里面使用async/await

- useEffect要么不返回任何东西，要么返回清理函数
- async返回了Promise，违反了设计规则

✅ 利用useEffect清理机制

- 组件卸载时执行
- 依赖项变化，下一次effect执行之前会执行清理函数

方法一：使用isMounted标识。无法真正取消请求，只是拦截了结果处理

```
useEffect(() => {
//发起请求
  let isMounted = true;
  fetchData(id).then(result => {
    // 只有组件没卸载才更新state
    if (isMounted) setData(result);
  });

  // 组件卸载/依赖变化时，把标记置为false
  return () => {
    isMounted = false;
  };
}, [id]);
```

方法二：AbortController

```
useEffect(() => {
  const controller = new AbortController();
  const signal = controller.signal;

  fetchData(id, { signal }).then(result => {
    setData(result);
  }).catch(err => {
    if (err.name !== 'AbortError') {
      // 只处理非主动取消的真实错误
    }
  })

  return () => {
    // 卸载/依赖变化，直接中断请求
    controller.abort();
  }
}, [id])
```

方法三：React Query/SWR 成熟库





### useCallback/useMemo

来源：父组件的重渲染导致子组件也会重新渲染（内存中的地址是新的导致React会重新渲染子组件）

**useMemo**适用场景：还有一种情况就是子组件使用了Reat.memo，但是子组件还是重新渲染了，原因：父组件传递的props虽然值没有变，但是重加载它的内存里的地址变了，也被判定为新数据需要重新渲染。

 useCallback：缓存函数本身，避免因为函数的引用变化导致重渲染

滥用后果：

- 内存开销增加
- 初始渲染性能增加
- 心智负担
- 依赖项陷阱

##### ✅ useMemo 应该使用的场景

1. 存在真正昂贵的计算（大数量数据遍历、处理等重运算）
2. 需要向 `React.memo` 包裹的子组件传递对象 / 数组
3. 避免每次渲染时重复生成全新复杂对象

##### ✅ useCallback 应该使用的场景

1. 向 `React.memo` 组件传递函数作为 props
2. 函数需要作为 `useEffect` / `useMemo` 等 Hooks 的依赖项
3. 事件处理函数需要长期保持引用稳定



### useRef（数据的变化不需要用户看到）

> 更新useRef的值不会触发组件的重新渲染

- 访问和操作DOM元素（domRef.current）
- 跨渲染周期的可变值，管理定时器减少不必要的重渲染

### forwardRef 和 useImperativeHandle

1️⃣ forwardRef

把父组件的ref转发到子组件内部，能够操作子组件

```
//子组件封装
import { forwardRef } from 'react'

// forwardRef 包裹组件，第二个参数接收父组件传来的 ref
const MyComponent = forwardRef((props, ref) => {
  // 将 ref 绑定到内部原生 input DOM 元素
  return <input {...props} ref={ref} />
})
```

```
// 父组件使用
import { useRef } from 'react'

function App() {
  // 创建 ref 对象
  const inputRef = useRef(null)

  const handleFocus = () => {
    // 通过 ref.current 调用原生 DOM 方法聚焦输入框
    inputRef.current?.focus()
  }

  return (
    <>
      {/* 直接给自定义组件传递 ref */}
      <MyComponent ref={inputRef} />
      <button onClick={handleFocus}>聚焦输入框</button>
    </>
  )
}
```



2️⃣ useImperativeHandle

> forwardRef权限太大了，当前hook可以自定义暴露给父组件的方法



### React18的并发特性

1️⃣ useTransition：区分高低优先级任务

```
const [isPending, starTransition] = useTransition()
```

低优先级任务使用`starTransition`包裹

2️⃣ useDeferredValue ：延迟更新一个值,值的比较判断

```
const defferedValue = useDeferredValue(value)
```

   比如把请求回来的res做延时更新，如果新的值还没有变的话就做loading效果



### React19新特性

1️⃣ Suspence

- 数据代码就绪之前显示的 fallbackUI
  - 代码分割 - React.lazy
  - 数据获取的请求期间

![image-20260702184146665](/Users/apple/Library/Application Support/typora-user-images/image-20260702184146665.png)

2️⃣ Activity

- 平滑过渡到新页面

 		- 不关心数据是不是加载好
 		- 只关心UI的活跃程度

![image-20260702184313605](/Users/apple/Library/Application Support/typora-user-images/image-20260702184313605.png)

### Electron

1、 主进程可以访问nodejs api 以及本地文件系统，子进程不可以访问

2、子进程可以通过全局变量`window.ElectronAPi`来调用挂载在上面父进程封装的方法

3、通信方式：

- 渲染进程-> 父进程（单向）：子进程通过` ipcRenderer.send`发送给父进程，预加载脚本通过`ipcMain.on`监听事件的变化
- 渲染进程 <->父进程（双向）：主进程中定义好方法，然后通过`ipcMain.handle`将注册的方法暴露给渲染进程，然后通过`preload`预加载脚本中`window.electronAPI`进行桥接转发，渲染进程通过`ipcRenderer.invoke`调用全局对象的方法进行使用
- 主进程 -> 渲染进程（单向）：主进程`webconts.send`, 预加载脚本：`ipcRenderer.on`监听主进程发送

4、preload脚本：独立的js文件，并且作为渲染进程的上下文，不是主进程，但是能够访问到渲染进程的window对象

5、模块

- app模块：监听应用生命周期
- Broswer模块：对应的页面管理

6、本地存储：

- JSON存储
- 本地化的sqlLite

```
ipcRenderer.send(信道，数据)  ---  ipcMain.on（信道，处理函数）    单向：渲染进程 -> 主进程
ipcRenderer.invoke ---  ipcMain.handle  双向：渲染进程 <-> 主进程
webContents.send() --- ipcRenderer.on(信道，回调)
多窗口之间 - 利用主进程作为中介
```

!!   invoke返回值永远是 Promise



## FFmpeg

1、视频必须压缩：一个30帧率的视频，一个像素位24bit，一分钟的大小：

```
1920*1080*24*30 = ...bit 约等于 186 MB/s
每分钟约 11.6GB
```

通过压缩算法比如 h.264 、 h.265 之类的进行压缩，可以将其压缩到 22.5M/s左右

2、压缩的两条主线

- 帧内压缩：视频中的每一帧图片都进行压缩
- 帧间压缩：针对连续帧，只存储影响后续帧率的关键帧。这是 I / P / B帧的基础

3、压缩的核心机制

- I 帧：关键帧，类似于JPEG图像，不依赖其他帧，体积最大(是P帧的10倍，B帧的20倍)，是视频拖拽的唯一入口点

- P帧：预测帧，依赖前置的关键帧和P帧进行压缩，只存储 diff(当前帧  ｜ 前序最近 I帧 / P帧)

- B帧：双向预测帧，依赖前后两帧，算法处理最复杂，延时比较高，通常直播禁用B帧，但是体积压缩最大

- GOP图像组：两个I帧之间的帧数合集

  > 长GOP：压缩率高，文件小，随机访问慢
  >
  > 短GOP：压缩率低，文件大，随机访问快

假设 25fps 拍 10 秒，共 250 帧：

| 方案                            | I 帧数 | P 帧数 | B 帧数 | 大小近似              |
| ------------------------------- | ------ | ------ | ------ | --------------------- |
| 长 GOP（一个 GOP 250 帧）       | **1**  | ~80    | ~169   | `1×I + 80×P + 169×B`  |
| 短 GOP（10 个 GOP，每个 25 帧） | **10** | ~80    | ~160   | `10×I + 80×P + 160×B` |

**P 帧和 B 帧的数量几乎一样**——视频内容没变，预测出来的差异帧数量也不变。**真正拉开差距的是 I 帧的数量**。I 帧是"全图 JPEG"，10 个 I 帧就比 1 个 I 帧贵 10 倍。

4、编码格式（算法）：视频：H.264（老牌、兼容性好）；音频：AAC、MP3

5、文件格式（容器）：MP4、webM

6、音频播放原理：

- 解协议：获取音频文件
- 解封装：从容器文件中分离出音频流、视频流、字幕流
- 解码：将编码后的编码流还原成原始像素数据（YUV）和音频数据（PCM）
- 根据时间戳同步显示音频和视频

6.1 、 PTS & DTS

- PTS(Presitation Time Stamp): 显示时间戳，决定帧何时显示
- DTS(Decode Time Stamp): 解码时间戳，决定帧何时解码

>  播放视频必须由PTS决定帧的播放顺序！ 根据DTS调度解码器

7、FFmpeg：音视频处理的工具库

```
brew install ffmpeg
ffmpeg -version
```

8、常见的API指令

```
//格式转换
ffempeg [全局选项][输入选项] -i  输入 [输出选项]  输出

- 全局选项 ： -y 覆盖原有的文件  -n 不覆盖原有的 level quiet  不进行日志输出
- 输入选项 ： -ss 00:00::01 截取指定的视频秒数
- 输出选项 ： 指定输出文件的编码格式、封装
```

9、ffporbe：查看音视频的详细参数、元数据

10、进阶：**滤镜**可以对音视频数据进行任意修改，所有操作都基于原始数据，是计算量最大的阶段

1️⃣ **简单滤镜（-vf）**：只有一个输入和一个输出流，并且**输入输出类型相同**

- 简单滤镜智能处理一些简单的、单一的操作：缩放、裁剪、旋转

  ```
  -vf 后指定滤镜的处理"scale=640:-1, format = yuv420p"（宽640高自适应，色彩420p）
  
   `-c copy`（流复制模式）无法使用任何滤镜，直接跳过了解码编码的阶段
  ```

2️⃣ **复杂滤镜(-filter_complex)**：支持多输入和多个输出流，并且输入输出类型任意 

- 复杂滤镜能处理复杂的操作：画中画、多轨输出
- **Filtergraph**：**流标记**[n(从0开始): t（视频、音频、字幕）]、**滤镜**（scale、format）、**滤镜链**（多个滤镜串联而成，前一个滤镜的输出作为后一个滤镜的输入）、滤镜图（多个滤镜交叉并行，使用`;`分隔）
- 复杂滤镜产生的输出流需要通过`-map`参数映射输出文件

```
水印位置放在主视频右下角距离边界10px的位置： "overlay=W-w-10:H-h-10"
```

 

3️⃣视频核心复杂滤镜分类

-  视频的基础变换和滤镜的使用：缩放`scale = 600：高度`、裁剪`crop`、旋转`transpose`、水平翻转`hflip`、边框`pad`
-  **叠加类滤镜(overlay)**：水印、定时显示(:enable=between(t,2,8)) 、淡入淡出、定时显示、文字叠加（一定要指定字体不然要乱码）
- 多个流拼接类滤镜：沿着垂直线拼接（hstack）、 沿着水平线叠加（vstack）、网格拼接（xstack）

 

4️⃣ 音频核心滤镜处理(-af)

- 格式转换：`-ca copy`直接复制音频流，不进行解码编码
-  音频倍速：`atempo`
- 背景音乐设置0.5倍大小，人声混合 ✅ 



5️⃣ 复杂媒体视屏剪辑处理

1、将原视频缩放到合适大小

2、添加上下模糊背景

3、添加顶部标题文字

4、添加logo水印

5、添加底部滚动字幕

> 语音转字幕：SmartSub     文字转语音（B站开源）： indexTT  s2







## Nodejs全栈

1、从零搭建：

- 工程组织monorepo
- 规范化：eslint、stylelint、spellcheck、commitlint
- 构建打包：vite、esbuild、webpack
- 测试：jest、vitest、**playwright**



Monorepo优势：

- 配置文件
- 分析子包结构
- 划分子包与子模块  





1、百度面经：大模型应用 React + ts

2、自我介绍

3、开源项目功能

4、性能优化怎么做的

5、浏览器渲染层面、js这些执行优化

6、事件循环机制

7、React Fiber架构的理解

8、schduler任务调度是如何实现的

9、时间切片如何实现的

10、setState是同步还是异步的

11、react的事件委托机制

12、react的生命周期

13、react如何进行错误处理 （errorBoundray能捕获哪些错误？）

14、代码题：

```
[[1,2,3],[4,5,6],[7,8,9]] 输入 5， 输出横纵坐标[1,1] 可能横向升序，纵向降序。
```





Redis

```
redis 启动： redis-server
redis客户端：redis-cli
GUI工具： redis-insight
命名：SET增、GET查、DEL删、EXITES存在、TTL查过期时间、EXPIRE设过期时间、SETEX name 5 jayden设置过期时间、SETNX只有当键不存在时才设置值，否则不操作

1️⃣ List: 
- LPUSH 左侧推入，支持多个: LPUSH letter c b a
- LRANGE 查询，0标识开始，-1标识最后： LRANGE letter 0 -1  - 查询全部
- RPUSH 列表的右侧推入
- LPOP 删除列表左侧的元素 ： LPOP letter 2 - 删除左侧两个
- RPOP 删除列表右侧的元素 ： RPOP letter 2 - 删除右侧两个

2️⃣ Set：无序不能重复
- SADD course Redis  创建course并插入“Redis”
- SMEMBERS course    查询所有元素
- 再次插入Redis 返回0，false
- SISMEMBER couser Redis 判断set里是否有“Redis”
- SREM course Redis    删除表中“Redis”
- 集合支持求交集、并集运算

3️⃣ ZSet： 有序集合
- ZADD result 680清华 660北大 650复旦  -插入数据
- ZRANGE result 0 -1  	 						 -查询所有大学名称
- ZSCORE result 清华                  -查询清华的分数
- ZRANK result 清华                   -查询清华的排名index 从小到大丛0开始
- ZREVRANK result 清华                -从大到小排名 0

4️⃣ Hash：
- HSET person name jayden  - 存储name ： jayden 到person里
- HDEL 删除
- HKEYS 获取键
- HGET 根据键查值
- HGETALL 获取所有键值对
- HEXISIT 判断键是否存在

5️⃣ 发布订阅： PUBLISH  SUBSCRIBE  通过channel进行通信
缺点：无法持久化、无法获得历史记录

6️⃣ Stream流：消息队列 XADD
洪峰时使用消费者组和消费者实现分流，并且支持延时持久化，方便后续空闲存储

7️⃣ GEOADD 地理位置信息处理
		GEOPOS 查询地点的经纬度
		GEODIST 计算两个地点的距离
		GEOSEARCH 支持按照圆形、矩形查询指定范围内的城市
		
8️⃣ 事务：一次请求支持多个命令
Redis在事务执行之前会将所有命令缓存到队列，队列中任意任务执行失败不影响其他命令
MULTI 开启事务   
EXPEC 执行队列任务

9️⃣数据持久化
- RDB快照：将Redis数据库存储到磁盘当中 配置/执行 Save 控制执行快照频率
缺点：- 如果服务器在最后一次快照期间宕机了，就丢失那一次的数据
		 - 如果数据庞大，直接执行save会导致redis阻塞，bgsave 可以单独开一个进程去备份数据不会阻塞主进程
！但是fork一个子进程期间也是有损耗的，会阻塞


-AOF： 执行写命令到内存中时同时将命令写入一个特定文件，AOF文件。后续重启redis将重新执行恢复

🔟主从节点
- 主写入从，从负责读. 主节点的数据变化会自动同步到从节点
- 配置：修改redis.conf 配置文件  - PID  dbFileName  
- 配置主节点：replicaof 127.0.0.1 6379 ，当前子节点的主节点
- 启动从节点：redis-server redis-6380.conf


1️⃣1️⃣ 哨兵模式：redis-server sentinel.conf
- 哨兵节点以独立的进程运行在redis集群当中
- 监听所有节点是否正常
- 如果主节点故障，进行故障转移，将其中一个子节点提升为主节点
- 哨兵节点也可能故障，需要多哨兵节点来互相监督，选取领导者
```



​	  ![image-20260726152348669](/Users/apple/Library/Application Support/typora-user-images/image-20260726152348669.png)





```
function myNew(Func,..args){
	  const obj = Object.create(Func.prototype);
	  
	  let result = Func.apply(this,args)
	  
	  if(typeof result === 'object' && result !== null){
				return result
		}
	  
	  return obj
}
```





```
class EventEmitter{
		constructor(){
				this.events = {}
		}
		
		on(event,cb){
				(this.events[event] ||= []).push(cb)
				return this
		}
		
		emit(event,...args){
				(this.events[event] || []).forEach(cb => cb(...args))
				return this
		}
		
		off(event,cb){
				if(!cb) this.events[event] = []
				else this.events[event] = (this.events[event] || []).filter(fn => fn !== cb && fn.raw !==cb)
				this
		}
		
		
		once(event,cb){
				const wrapper = (...args) => {
						cb(...args)
						this.off(event,wrapper)
				}
				wrapper.raw = cb
				return this.on(wrapper)
		}
}
```



## 面试准备

1、三方库缺少类型

- 首选方案：npm info 查看库的types是否内置类型
- 次选方案：安装@types/库名，有限选官方维护的类型包
- 备用方案：自己写 .d.ts 
- AI辅助：编辑器内部集成了模型类型推断其，鼠标悬停，自动补全，生成精确率极高的类型快照，保存在项目.ts-types-cache里
- 不推荐：declare module    变成anyscript

2、样式相关

- vue：scoped（编译成 data -v-id）、父子样式透穿（:deep）
- react：Css-in-js编译成唯一类名、css原子类Tailwind**无作用域冲突风险**、css-module构建工具编译成唯一hash值

3、请求接口涉及前端知识点

- 请求方法与异步处理
- 请求拦截器与身份鉴权
- 跨域问题
- 缓存策略
- 错误处理与监控

4、CDN

- CDN不是存储服务器，是缓存代理网络
- 缓存有生命周期
- 不仅加速“静态资源”，还有动态内容
- 部署时需要生成带hash的文件名

<img src="/Users/apple/Library/Application Support/typora-user-images/image-20260726183632253.png" alt="image-20260726183632253" style="zoom:50%;" />

5、router路由库的原理

- URL变更与拦截：

  - hash模式：`hashChange`自动捕获所有hash变化
  - History模式：主动跳转`history.pushState()`     前进/后退：`popstate`响应浏览器导航操作

  > 历史记录栈有浏览器内核维护，路由库仅消费其状态变化

- 路由匹配过程：
  - 路由表是静态配置：pash - component构成映射表
  - 动态匹配逻辑：URL变化时，从`window.location`提取当前路径，按照顺序匹配路由表
- 视图更新触发条件；
  - Hash模式：`hashChange`触发后，直接执行路由匹配并更新视图
  - History模式：调用`pushState`后立即手动触发匹配，前进后退：`popstate`触发匹配
  - 共同逻辑：匹配结果通过响应式系统（Vue）或Context（React）通知<router-view/> / <Outlet/>组件，动态替换渲染的组件

>前端路由库的**核心价值是解耦 URL 与组件渲染**。它通过监听浏览器原生事件捕获 URL 变化，结合静态路由表进行动态匹配，最终利用框架的响应式系统驱动视图更新。**Hash 模式依赖事件全覆盖简化逻辑，History 模式需显式处理导航方向，但二者均不维护历史栈，仅消费其状态**。





渲染过程：请求资源-解析html生成DOM树 - 解析css生成cssom树-合成最终渲染树-dom变化（重排重绘）

回流：dom元素大小、位置变化；重绘：dom节点样式变化

BFC：块级格式上下文，一块独立区域，防止垂直方向margin重叠。 触发方式：开启定位、overflow：hidden

DOCTYPE作用？指定html版本

盒模型有标准盒模型（content-box）和怪异盒模型(border-box)。宽度差异：标准width=content-width，怪异width=content-width+border+padding

原型和原型链：每个实例对象都有个原型，实例对象的proto指向构造函数的prototype，共享构造函数的方法，所有原型对象的尽头指向object，object的原型对象指向null

js事件传播的三个阶段：捕获（父-》子）、目标（触发事件）、冒泡（子到父）。

防抖和节流：防抖（多次短时间触发由最后一次决定执行时机）适合搜索框输入。节流（多次执行按照设置的事件有规律的执行）窗口缩放、鼠标滑动等

Promise是请求返回的对象，解决了回调地狱的问题，改成链式调用。三种状态，链式调用使用.then,.catch,.finally调用执行

Async/await的本质是generator的语法糖，同步的方式写异步代码，async包裹异步函数，await需要等待返回的Promise处理完毕才继续执行

常见http状态码：101协议升级 200响应成功  304缓存命中 401未登陆 403权限不足 500服务器错误 502网关超时

GET和POST的区别：都是http的请求方法，POST更安全。GET将参数携带在url后面进行传输查询，post是复杂请求，先要进行预检请求，预检通过才发送请求数据进行操作

浏览器缓存：强缓存（Cache-control: max-age \ Expires设置过期时间），协商缓存：（Last-Modified/Etag）(If-none-match\If-?)，如何通过响应头设置？

Vue双向绑定的原理：基于响应式系统。api层：v-model, @input + :子传父事件

Vue组件通信方式：props/emit , provide/inject，vuex/pinia，eventBus（mitt）

虚拟DOM是js对象，是真实dom的抽象，是为了优化diff更新时的性能，同时支持跨平台。

性能优化：资源-压缩、CDN、分包、Tree-shaking、按需加载-懒加载、预加载；

​		   网络-预连接、http2多路复用、缓存

​		   运行时：内存泄漏、线上监控

​		  服务端渲染

模块化：ESM + CJS，ESM现代浏览器支持，可以实时编译，按需加载Vite，只能顶部导入；CJS是同步，运行时规范，可以任意位置导入，是值拷贝，会影响原模块代码

Ts和JS优势：类型安全、灵活推导

检测内存泄漏：performance面板录视频分析面板

前端调试工具：控制台网络、标签元素、性能、React/vue-devTools、Pinia、Zustand工具、performance API性能



**Monorepo好处：**1、代码复用，无需发布私有包，避免兼容性问题；2、消除版本依赖冲突并且可以通过pnpm:workspace硬链接共享依赖；3、避免多仓库配置碎片化；4、为AI辅助开发提供完整上下文





1、小鹏面经：仿真部门，前端组5、6个人

2、自我介绍

3、说说性能优化

4、React常见hooks

5、事件循环

```
console.log(1)
setTimeout(() => console.log(2))
promise.then(() => resolve(3))
RAF(console.log(4))
console.log(5)
```

6、两个手写：

1️⃣ React写一个弹窗

2️⃣ 找公共元素

7、怎么了解到的小鹏

8、问问平时如何用AI

8、提问：AI提效

9、面试感受：不会写React代码。。。AI写的，用vue实现了弹窗，结果用了useRef，服了，面试官不熟悉vue，我坚定有这个API，搞混了，哈哈哈，面完感觉挺轻松的吧，不过也没关系，面试官真的很和蔼，一直笑嘻嘻的



## Java

### 1、基础篇

1、写一个接口的核心注解

```
@RestController
@RequestMapping("/api/user")
public class UserController {

    // GET 路径参数
    @GetMapping("/{id}")
    public String getInfo(@PathVariable Long id){
        return "用户id："+id;
    }

    // GET 查询参数
    @GetMapping("/page")
    public String page(@RequestParam Integer pageNum, @RequestParam Integer pageSize){
        return "页码："+pageNum;
    }

    // POST JSON请求体
    @PostMapping("/add")
    public String add(@RequestBody User user){
        return "新增用户："+user.getName();
    }
}
```

2、提供了三种配置修改方式，优先级： property > yml > yaml.  共存叠加

<img src="/Users/apple/Library/Containers/com.tencent.qq/Data/tmp/QQ_1785746929269.png" alt="QQ_1785746929269" style="zoom:33%;" />

3、Docker操作

>  Host path：宿主机服务器上的目录，**真正持久保存数据的位置**，容器销毁，该目录文件依然存在。

> Container path：容器内部的挂载点路径，对应容器程序读写的目录；对于 mysql，固定填`/var/lib/mysql`，这是 mysql 进程读写数据的容器内目录。
>
> 挂载本质是双向文件映射，容器读写 container path 的文件，实际落地存储在 host path 宿主机磁盘，以此实现容器删除后数据不丢失。

4、mqsql数据库

- 编码格式：utf8万国码 支持所有语言的文字   mb4支持表情符号
- 主键：唯一id标识，支持主键查询 - 快,提升查询效率
- 类型：

>MySQL 分为数值、字符串、时间、二进制几大类。
>
>整数：TINYINT、SMALLINT、MEDIUMINT、INT、BIGINT；注意 int 括号是显示宽度，不限制存储。
>
>小数：FLOAT/DOUBLE 浮点有精度丢失；财务金额用 DECIMAL 定点数。
>
>字符串：CHAR 定长；VARCHAR 变长；TEXT、MEDIUMTEXT、LONGTEXT 用于大段文本。
>
>时间：DATE、TIME、DATETIME、TIMESTAMP，优先 DATETIME，TIMESTAMP 有 2038 时区问题。
>
>BLOB 系列存储二进制，图片不建议入库，存 url。

- 表：记得要注释
- 数据库范式
  - <img src="/Users/apple/Library/Application Support/typora-user-images/image-20260806135255905.png" alt="image-20260806135255905" style="zoom:50%;" />



- 事务（Engine：InnoDB）：一系列sql语句操作，要么全部成果，要么失败直接回滚取消之前的成功执行

- 外键：

  - **外键是数据库的约束，用来保证两张表之间的数据引用是合法的，维护表与表之间的关系。**

  > 举例：订单表有字段 `user_id`，这个字段存储用户表的 id。
  >
  > `user_id` 就是**外键字段**；它引用另外一张表（user）的主键 id。

- 关联表设计

  - 一对多：把外键放在多的那一张表里面，作为关联

  ```
  -- 用户表（一方）
  user(id, name, phone)
  
  -- 订单表（多方！！外键放在多这边）
  order(id, order_no, price, user_id) 
  -- user_id 就是外键，关联 user.id
  ```

  - 一对一：

  ```
  1️⃣ 直接合并两个关联表
  2️⃣ 其中一方暴露出外键，并且在另一张表里加上 Unique()唯一约束
  
  -- 用户主表
  user(id,name,phone)
  
  -- 用户详情表 一对一
  user_profile(id, user_id, id_card, address)
  -- user_id 外键关联 user.id，**给user_id加UNIQUE唯一约束！！**
  ```

  - 多对多：需要使用中间表，中间表存储两张表的外键用来关联

  >博客系统：文章、标签
  >
  >一篇文章可以打多个标签；一个标签可以被多篇文章使用。是什么关系？怎么建表？
  >
  >答案：多对多。文章表、标签表，中间表 article_tag (article_id,tag_id)

- 多表关联查询：LEFT JOIN ｜ RIGHT JOIN ｜ INNER JOIN ｜ OUTER JOIN

>> 判断左右表看 SQL 位置：`A LEFT JOIN B`，A 左表，B 右表。
>>
>> LEFT JOIN 以左表为准：左表全部行保留，右表匹配不到填充 NULL；
>>
>> RIGHT JOIN 以右表为准，右表全部保留；
>>
>> INNER JOIN 内连接，只返回两边匹配成功的数据；
>>
>> MySQL 不支持 FULL 全外连接。
>
>### 思考题
>
>需求：查询**所有用户，以及该用户发布的文章，没有文章的用户也要查出来**，用什么 join？
>
>> 答案：`user LEFT JOIN article`



5、java

- JDK 和 JRE
  - JDK负责编译 ： 将 .java -> .class 可运行文件
  - JRE负责运行：java的运行
- .class 命名使用大驼峰命名
- 数据类型
  - 一般不直接用基本数据类型声明，不能进行数据类型转换，一般使用`包装类`进行声明
- 类型转换
  - 字符串转数字： parseShort - parseInt ... 存价格一般用BigDecimal
  - 字符单引号、字符串双引号
  - 各类型转字符串：.toString()
  - long 用 L 标记、float用F标记、Doubule用D标记
- 类：com.goldpankit 代码都写在这下面是为了区分别人的代码和我们的，类比npm的包名
  - <img src="/Users/apple/Library/Application Support/typora-user-images/image-20260806162835074.png" alt="image-20260806162835074" style="zoom:30%;" />
- extends 继承     final不可继承  meum 枚举所有情况
- **重载 (Overload)：同名不同参，发生在本类，编译期绑定**
  - **重写 (Override)：同名同参，父子继承，运行时动态绑定（多态）**
- collection集合

<img src="/Users/apple/Library/Application Support/typora-user-images/image-20260807100501870.png" alt="image-20260807100501870" style="zoom:50%;" />

<img src="/Users/apple/Library/Application Support/typora-user-images/image-20260807101002583.png" alt="image-20260807101002583" style="zoom:50%;" />

- 异常处理

  - 系统error： 
  - 代码错误：运行时异常 、 非运行时异常（必须使用try catch 进行捕获）

- 流、线程、反射

  - 进程：
  - 线程：执行顺序不确定，CPU处理
  - 线程池：方便管理我们的线程对象，防止无限线程导致服务器崩溃
  - 如何开线程

  ```
  Java 如何开一个新线程？ 
  - Lambda表达式
  - Thread t1 = new Thread(runnable)
  - class MyThread extends Thread, 并重写 run 方法
  	Thread t2 = new MyThread ();
  //线程池API
  public class Demo1 {
      public static void main(String[] args) {
          // Executor 只负责执行任务，不关心线程怎么创建
          Executor executor = Runnable::run;
          // execute 接收Runnable，无返回值
          executor.execute(() -> {
              System.out.println("线程执行：" + Thread.currentThread().getName());
          });
      }
  }
  ```

- 并发以及如何处理

  - 并发问题例子<img src="/Users/apple/Library/Application Support/typora-user-images/image-20260807111617951.png" alt="image-20260807111617951" style="zoom:50%;" />
  - 解决方法：1️⃣将一个完成操作当作一个指令（**synchoronized**）

  <img src="/Users/apple/Library/Application Support/typora-user-images/image-20260807111942297.png" alt="image-20260807111942297" style="zoom:33%;" />

  2️⃣ ReentrantLock锁住操作<img src="/Users/apple/Library/Application Support/typora-user-images/image-20260807112208878.png" alt="image-20260807112208878" style="zoom:33%;" />

- 流、反射

  - 流：输入输出流、字节流、字符流
  - 反射：作用-动态创建实例、调用实例方法、动态代理的基础  应用-动态代理
  
- 运行启动类之后**内置tomcat服务器**就启动了

- @RestController 返回序列化的json字段给前端

- @Controlller 可以返回任意格式，如HTML

-  @RequestMapping 指定请求路径

-  @GetMapping / PostMapping 指定请求方法

- 所有请求添加前缀API ：application.yml 配置

```
server:
	servlet:
		context-path: /api
port: 8080 //修改端口
```

- 获取接口请求路径定义的参数：@PathVariable
- 获取请求的请求体：@RequestBody
- 水印：resources/banner.txt
-  开发/生产的多环境配置

```
1️⃣ 配置 application-dev/-prod.yml 两个文件
2️⃣ 使用application.yml 指定:
	spring:
		profiles:
			active:prod/dev
```

- 集成mybatis-plus和mysql进入项目

```
1️⃣ 添加对应的依赖 pom.xml
2️⃣ mysql：.yml里面配置database驱动类型、账号密码
3️⃣ orm： .yml配置
4️⃣ 启动类上添加@MapperScan注解以及mapper文件路径
5️⃣ 代码书写
  -三部分同层：entity/模块 -> @Data对象
```

- `@Autowired/@Resurse`注解自动注入对象，可以使用其方法
-  注入对象的方式：`@RestController / @Service/@Bean/@Component`
- service里面的多种拓展对象放在 `service/impl/UserServiceImpl`
-  规范返回给前端的格式:`/common/Result`里面定义响应类

```
//一个controller实例
```

<img src="/Users/apple/Library/Application Support/typora-user-images/image-20260810103353639.png" alt="image-20260810103353639" style="zoom:50%;" />

- 分页请求
  - 主要是使用`LIMIT`关键字实现的
  - 如果需要使用mybatis的插件（分页），需要在springboot当中注入插件
- 配置类：@Configuration ，`/config/mybatisConfig`
- 自动填充数据的创建时间和修改时间，官网还有其他类补充，使用@JSONFomat进行时间转换

```
@TableField(value="",fill = FieldFill.INSERT)
```

- 条件查询：

```
// mysql使用where进行查询
// mybatis使用条件构造器（LambdaQueryWrapper）
```

- 集成Redis

```
1️⃣ .xml依赖
2️⃣ .yml 文件添加redis配置
3️⃣ 创建redis配置类：config/RedisConfig
 -注入连接对象、操作对象重新配置onfig
4️⃣ util/RedisUtil：判断key是否存在、批量删除、读取这些方法的封装
5️⃣ @Resource 注入RedisUtil对象使用方法
```

- 集成Sa-Token: 请求路径拦截、token鉴权

```
1️⃣ .xml依赖配置
2️⃣ 导包和方法 ： login、isLogin
3️⃣ 可以将用户的token存到Redis里面
4️⃣ 跨域解决：@configuration 
```

- 全局过滤器：定义放行和拦截路径

- 用户权限信息：RBAC权限模型
- 全局异常拦截处理：注入全局异常对象

`@RestControllerAdvice`





## 苍穹外卖全栈项目学习笔记

1、nginx必须放在没有中文的目录当中运行

- 好处：提高访问速度、进行负载均衡（upstream webservers）、服务安全
- 负载均衡策略：轮训、权重weight、ip-hash分配、least-connection最少连接数...

2、DTO 数据传输对象，用户各层的传输数据 前端 ->  后端

3、 VO 视图对象，前端展示数据提供的对象

4、POJO：普通java对象，只有属性和对应的getter和setter

5、登陆功能完善

- 密码明文存储  -密码加密存储如MD5（不可逆） 存到数据库
- 代码书写 `DigestUtils.md3DigestAs`类进行加密

6、接口文档 Swagger - Knife4j

- 导入Knife4j的依赖
- 配置类加入配置，扫描controller包
- 设置静态资源映射
- 相关注解：@Api(tag =" 描述")

7、BeanUtils.copyProperties(DTO，对象数据结构)

- 直接拷贝，DTO里面没有补全的属性自己设置默认值（状态、密码等等）

8、//TODO 注释：后期需要修改完善

9、 sql、全局异常捕获：@ExceptionHandler

10、ThreadLocal：并不是一个线程，是线程的局部变量

- 为每个线程提供单独一份存储功能，具有线程隔离的效果，只有在线程内获取当前值
- 场景：全局token拦截器生成唯一id 传给service层
- 使用：set、get、remove

11、DTO封装的是前端传递过来的数据结构

12、分页查询：pagehelper插件基于ThreadLocal实现，然后使用LIMIT关键字进行拼接

```
PageHelper.startPage()
```

13、时间格式处理

- @JsonFormat 
- WebMvcConfig配置类里面统一对日期类型进行格式化转换 -对象转换器

14、sql书写：

- 简单sql：直接在 mapper 接口方法上
- **XML 文件（复杂 SQL）** —— 写在 mapper 同名 xml 里





















